// Generated by scripts/build-dashboard-client.js.
// Source of truth: src/api/dashboard-client/entry.ts and its imports.
// Do not edit this file by hand.
export const GENERATED_DASHBOARD_CLIENT_JS = "\"use strict\";\n(() => {\n  // src/api/dashboard-client/config.ts\n  var DASHBOARD_CONFIG = globalThis.__SYMPHONY_DASHBOARD_CONFIG__ || {\n    dashboard_enabled: true,\n    refresh_ms: 4e3,\n    render_interval_ms: 1e3,\n    phase_stale_warn_ms: 45e3\n  };\n  var ACTION_REQUIRED_CODES = globalThis.__SYMPHONY_ACTION_REQUIRED_CODES__ || {};\n  var OPERATOR_TRANSITION_RULES = globalThis.__SYMPHONY_OPERATOR_TRANSITION_RULES__ || { detailMap: {}, eventMap: {} };\n\n  // src/api/dashboard-client/dom.ts\n  var elements = {};\n  function resolveDashboardElements(doc = document) {\n    return {\n      connectionBadge: doc.getElementById(\"connection-badge\"),\n      connectionDetail: doc.getElementById(\"connection-detail\"),\n      lastUpdated: doc.getElementById(\"last-updated\"),\n      refreshButton: doc.getElementById(\"refresh-button\"),\n      refreshStatus: doc.getElementById(\"refresh-status\"),\n      healthMessage: doc.getElementById(\"health-message\"),\n      lastError: doc.getElementById(\"last-error\"),\n      actionRequiredBanner: doc.getElementById(\"action-required-banner\"),\n      actionRequiredTitle: doc.getElementById(\"action-required-title\"),\n      actionRequiredSummary: doc.getElementById(\"action-required-summary\"),\n      actionRequiredGroups: doc.getElementById(\"action-required-groups\"),\n      runtimeStaleBanner: doc.getElementById(\"runtime-stale-banner\"),\n      runtimeStaleTitle: doc.getElementById(\"runtime-stale-title\"),\n      runtimeStaleSummary: doc.getElementById(\"runtime-stale-summary\"),\n      runtimeUpdateBanner: doc.getElementById(\"runtime-update-banner\"),\n      runtimeUpdateTitle: doc.getElementById(\"runtime-update-title\"),\n      runtimeUpdateSummary: doc.getElementById(\"runtime-update-summary\"),\n      runtimeUpdatePrepareButton: doc.getElementById(\"runtime-update-prepare-button\"),\n      runtimeUpdateApplyButton: doc.getElementById(\"runtime-update-apply-button\"),\n      runtimeUpdatePanel: doc.getElementById(\"runtime-update-panel\"),\n      runtimeUpdatePreparePanelButton: doc.getElementById(\"runtime-update-prepare-panel-button\"),\n      runtimeUpdateApplyPanelButton: doc.getElementById(\"runtime-update-apply-panel-button\"),\n      runtimeUpdateState: doc.getElementById(\"runtime-update-state\"),\n      runtimeUpdateRecommendation: doc.getElementById(\"runtime-update-recommendation\"),\n      runtimeUpdateStatus: doc.getElementById(\"runtime-update-status\"),\n      runtimeUpdateDetails: doc.getElementById(\"runtime-update-details\"),\n      drainModePanel: doc.getElementById(\"drain-mode-panel\"),\n      drainModeSummary: doc.getElementById(\"drain-mode-summary\"),\n      drainModeBoundary: doc.getElementById(\"drain-mode-boundary\"),\n      drainModeMeta: doc.getElementById(\"drain-mode-meta\"),\n      drainBlockersList: doc.getElementById(\"drain-blockers-list\"),\n      drainControlStatus: doc.getElementById(\"drain-control-status\"),\n      drainEnterButton: doc.getElementById(\"drain-enter-button\"),\n      drainExitButton: doc.getElementById(\"drain-exit-button\"),\n      drainWaitButton: doc.getElementById(\"drain-wait-button\"),\n      drainShutdownButton: doc.getElementById(\"drain-shutdown-button\"),\n      apiDegradedBanner: doc.getElementById(\"api-degraded-banner\"),\n      apiDegradedSummary: doc.getElementById(\"api-degraded-summary\"),\n      snapshotErrorPanel: doc.getElementById(\"snapshot-error-panel\"),\n      snapshotErrorMessage: doc.getElementById(\"snapshot-error-message\"),\n      kpiGrid: doc.getElementById(\"kpi-grid\"),\n      retryStatusSummary: doc.getElementById(\"retry-status-summary\"),\n      rateLimits: doc.getElementById(\"rate-limits\"),\n      throughputPanel: doc.getElementById(\"throughput-panel\"),\n      throughputOutput: doc.getElementById(\"throughput-output\"),\n      runtimeResolutionOutput: doc.getElementById(\"runtime-resolution-output\"),\n      runningRows: doc.getElementById(\"running-rows\"),\n      retryRows: doc.getElementById(\"retry-rows\"),\n      blockedRows: doc.getElementById(\"blocked-rows\"),\n      stoppedRunRecoveryList: doc.getElementById(\"stopped-run-recovery-list\"),\n      stoppedRunRecoveryLoad: doc.getElementById(\"stopped-run-recovery-load\"),\n      projectHistoryProjectKey: doc.getElementById(\"project-history-project-key\"),\n      projectHistoryLoad: doc.getElementById(\"project-history-load\"),\n      projectHistoryStatus: doc.getElementById(\"project-history-status\"),\n      projectHistoryFacts: doc.getElementById(\"project-history-facts\"),\n      projectHistoryRows: doc.getElementById(\"project-history-rows\"),\n      projectHistoryDetail: doc.getElementById(\"project-history-detail\"),\n      statusFilter: doc.getElementById(\"status-filter\"),\n      runningFilter: doc.getElementById(\"running-filter\"),\n      issuePanel: doc.getElementById(\"issue-panel\"),\n      issueInput: doc.getElementById(\"issue-input\"),\n      issueLoad: doc.getElementById(\"issue-load\"),\n      issueOpenJson: doc.getElementById(\"issue-open-json\"),\n      issueSummary: doc.getElementById(\"issue-summary\"),\n      conversationSummary: doc.getElementById(\"conversation-summary\"),\n      conversationList: doc.getElementById(\"conversation-list\"),\n      conversationRoleFilter: doc.getElementById(\"conversation-role-filter\"),\n      conversationDensity: doc.getElementById(\"conversation-density\"),\n      issueExplainerCard: doc.getElementById(\"issue-explainer-card\"),\n      issueExplainerActionability: doc.getElementById(\"issue-explainer-actionability\"),\n      issueExplainerHeadline: doc.getElementById(\"issue-explainer-headline\"),\n      issueExplainerClassification: doc.getElementById(\"issue-explainer-classification\"),\n      issueExplainerReason: doc.getElementById(\"issue-explainer-reason\"),\n      issueExplainerAction: doc.getElementById(\"issue-explainer-action\"),\n      issueExplainerTransition: doc.getElementById(\"issue-explainer-transition\"),\n      issueExplainerVersion: doc.getElementById(\"issue-explainer-version\"),\n      issueExplainerDetail: doc.getElementById(\"issue-explainer-detail\"),\n      threadDetail: doc.getElementById(\"thread-detail\"),\n      threadTimelineLanes: doc.getElementById(\"thread-timeline-lanes\"),\n      threadBlockerCard: doc.getElementById(\"thread-blocker-card\"),\n      threadCapabilityWarnings: doc.getElementById(\"thread-capability-warnings\"),\n      threadRawEvents: doc.getElementById(\"thread-raw-events\"),\n      issueOutput: doc.getElementById(\"issue-output\"),\n      runtimeEventsPanel: doc.getElementById(\"runtime-events-panel\"),\n      eventFeedFilter: doc.getElementById(\"event-feed-filter\"),\n      runtimeEventsList: doc.getElementById(\"runtime-events-list\"),\n      diagnosticsOutput: doc.getElementById(\"diagnostics-output\"),\n      historyList: doc.getElementById(\"history-list\"),\n      constellationIssueList: doc.getElementById(\"constellation-issue-list\"),\n      constellationRefreshPulse: doc.getElementById(\"constellation-refresh-pulse\"),\n      constellationCore: doc.getElementById(\"constellation-core\"),\n      constellationInterlockList: doc.getElementById(\"constellation-interlock-list\"),\n      constellationEvidencePath: doc.getElementById(\"constellation-evidence-path\"),\n      constellationActions: doc.getElementById(\"constellation-actions\"),\n      constellationOperator: doc.getElementById(\"constellation-operator\"),\n      constellationRuntimeClock: doc.getElementById(\"constellation-runtime-clock\"),\n      constellationApiHealth: doc.getElementById(\"constellation-api-health\"),\n      constellationWorkerCount: doc.getElementById(\"constellation-worker-count\"),\n      constellationQueueCount: doc.getElementById(\"constellation-queue-count\")\n    };\n  }\n  function setDashboardElements(nextElements) {\n    elements = nextElements;\n  }\n\n  // src/api/dashboard-client/state.ts\n  var state = {\n    payload: null,\n    lastGoodPayload: null,\n    selectedIssue: \"\",\n    connection: \"offline\",\n    pollTimer: null,\n    runtimeTicker: null,\n    pollDelayMs: DASHBOARD_CONFIG.refresh_ms,\n    streamRetryMs: 1e3,\n    streamConnected: false,\n    streamSnapshotHealthy: false,\n    streamStatus: \"connecting\",\n    streamFallbackReason: \"connecting\",\n    eventSource: null,\n    uiStateLoaded: false,\n    uiStateSaveTimer: null,\n    stoppedRunRecoveryLoaded: false,\n    stoppedRunRecoveryLoading: false,\n    stoppedRunRecoveryPayload: null,\n    projectHistory: {\n      projectKey: \"\",\n      loading: false,\n      detailLoadingTicketKey: null,\n      listPayload: null,\n      healthPayload: null,\n      detailPayload: null,\n      error: null,\n      detailError: null\n    },\n    filter: {\n      query: \"\",\n      status: \"all\",\n      eventFeedSeverity: \"all\",\n      blockedReason: \"all\",\n      conversationRole: \"all\",\n      conversationDensity: \"comfortable\"\n    },\n    panels: {\n      throughputOpen: true,\n      runtimeEventsOpen: true\n    },\n    suppressIssuePanelToggleLoad: false,\n    runtimeResolution: null\n  };\n\n  // src/api/dashboard-client/formatting.ts\n  function formatNumber(value) {\n    if (!Number.isFinite(value)) {\n      return \"0\";\n    }\n    return value.toLocaleString(\"en-US\");\n  }\n  function localTimeZoneLabel() {\n    try {\n      return Intl.DateTimeFormat().resolvedOptions().timeZone || \"local time\";\n    } catch (_error) {\n      return \"local time\";\n    }\n  }\n  function formatUtcIso(timestampMs) {\n    if (!Number.isFinite(timestampMs)) {\n      return \"n/a\";\n    }\n    return new Date(timestampMs).toISOString();\n  }\n  function formatDate(value) {\n    if (!value) {\n      return \"n/a\";\n    }\n    let parsed;\n    if (typeof value === \"number\" && Number.isFinite(value)) {\n      parsed = value;\n    } else {\n      parsed = Date.parse(value);\n    }\n    if (!Number.isFinite(parsed)) {\n      return String(value);\n    }\n    const local = new Date(parsed).toLocaleString(void 0, { timeZoneName: \"short\" });\n    return local + \" [\" + localTimeZoneLabel() + \"] (UTC \" + formatUtcIso(parsed) + \")\";\n  }\n  function formatCanonicalJsonBlock(label, payload) {\n    return label + \" (canonical UTC ISO values preserved)\\\\n\" + JSON.stringify(payload, null, 2);\n  }\n  function formatDurationFromIso(iso) {\n    const parsed = Date.parse(iso || \"\");\n    if (!Number.isFinite(parsed)) {\n      return \"n/a\";\n    }\n    const seconds = Math.max(0, Math.floor((Date.now() - parsed) / 1e3));\n    const minutes = Math.floor(seconds / 60);\n    const remain = seconds % 60;\n    return minutes + \"m \" + remain + \"s\";\n  }\n  function getTurnControlLabel(state2) {\n    switch (state2) {\n      case \"agent_turn\":\n        return \"Agent Turn\";\n      case \"operator_turn\":\n        return \"Operator Turn\";\n      case \"blocked_manual_resume\":\n        return \"Manual Resume Required\";\n      case \"automation_fault\":\n        return \"Automation Fault\";\n      default:\n        return \"Turn Unknown\";\n    }\n  }\n  function getProgressSignalLabel(state2) {\n    switch (state2) {\n      case \"advancing\":\n        return \"Advancing\";\n      case \"heartbeat_only\":\n        return \"Heartbeat Only\";\n      case \"active_but_opaque\":\n        return \"Active But Opaque\";\n      case \"stalled_waiting\":\n        return \"Stalled Waiting\";\n      default:\n        return \"Progress Unknown\";\n    }\n  }\n  function getRetryStateLabel(entry) {\n    if (entry && entry.due_state === \"overdue\") {\n      return \"Retry Overdue\";\n    }\n    return \"Retry Scheduled\";\n  }\n  function getTokenConfidenceLabel(confidence) {\n    switch (confidence) {\n      case \"observed_live\":\n        return \"Live\";\n      case \"backfilled\":\n        return \"Backfilled\";\n      case \"missing\":\n        return \"Missing\";\n      default:\n        return \"Unknown\";\n    }\n  }\n  function formatDurationFromMs(timestampMs) {\n    if (!Number.isFinite(timestampMs)) {\n      return \"n/a\";\n    }\n    const seconds = Math.max(0, Math.floor((Date.now() - Number(timestampMs)) / 1e3));\n    const minutes = Math.floor(seconds / 60);\n    const remain = seconds % 60;\n    return minutes + \"m \" + remain + \"s\";\n  }\n  function formatElapsedMs(durationMs) {\n    if (!Number.isFinite(durationMs)) {\n      return \"n/a\";\n    }\n    const seconds = Math.max(0, Math.floor(Number(durationMs) / 1e3));\n    const minutes = Math.floor(seconds / 60);\n    const remain = seconds % 60;\n    return minutes + \"m \" + remain + \"s\";\n  }\n  function getActionRequiredLabel(code) {\n    return ACTION_REQUIRED_CODES[code] || code || \"unknown\";\n  }\n  function createBlockedRootCauseBlock(entry) {\n    if (!entry || !entry.root_cause) {\n      return null;\n    }\n    const block = document.createElement(\"div\");\n    block.className = \"root-cause-block\";\n    const label = document.createElement(\"div\");\n    label.className = \"root-cause-label\";\n    label.textContent = \"Root cause\";\n    const summary = document.createElement(\"div\");\n    summary.className = \"root-cause-summary\";\n    const rootCauseSummary = entry.root_cause.reason_code === \"worktree_dirty_repo\" ? \"Workspace provisioning failed: repo root has uncommitted or untracked files.\" : entry.root_cause.summary || entry.root_cause.detail || entry.root_cause.reason_code || \"n/a\";\n    summary.textContent = rootCauseSummary;\n    block.append(label, summary);\n    if (entry.root_cause.detail) {\n      const detail = document.createElement(\"div\");\n      detail.className = \"muted\";\n      detail.textContent = \"Failed phase detail: \" + entry.root_cause.detail;\n      block.append(detail);\n    }\n    const remediationHint = entry.root_cause.remediation_hint || (entry.root_cause.reason_code === \"worktree_dirty_repo\" ? \"Clean, commit, or ignore the dirty repo files, then requeue or resume.\" : null);\n    if (remediationHint) {\n      const remediation = document.createElement(\"div\");\n      remediation.className = \"status-pill pending\";\n      remediation.textContent = \"Remediation: \" + remediationHint;\n      block.append(remediation);\n    }\n    return block;\n  }\n  function isActionRequiredCode(code) {\n    return Boolean(code && ACTION_REQUIRED_CODES[code]);\n  }\n  function formatBudgetStatusLabel(status) {\n    switch (status) {\n      case \"warning\":\n        return \"Warning\";\n      case \"hard_limited\":\n        return \"Hard limited\";\n      case \"telemetry_unavailable\":\n        return \"Telemetry unavailable\";\n      case \"ok\":\n        return \"Ok\";\n      default:\n        return \"Not configured\";\n    }\n  }\n  function budgetStatusClass(status) {\n    if (status === \"hard_limited\") {\n      return \"failed\";\n    }\n    if (status === \"warning\" || status === \"telemetry_unavailable\") {\n      return \"pending\";\n    }\n    return \"success\";\n  }\n  function createBudgetBlock(entry) {\n    const container = document.createElement(\"div\");\n    container.className = \"budget-summary\";\n    const status = entry && entry.budget_status ? entry.budget_status : null;\n    const statusPill = document.createElement(\"div\");\n    statusPill.className = \"status-pill \" + budgetStatusClass(status);\n    statusPill.textContent = \"Budget: \" + formatBudgetStatusLabel(status);\n    container.append(statusPill);\n    const detail = document.createElement(\"div\");\n    detail.className = \"muted\";\n    if (status === \"telemetry_unavailable\") {\n      detail.textContent = \"Budget usage unavailable; not counted as zero.\";\n    } else if (typeof entry.budget_usage_tokens === \"number\" || typeof entry.budget_limit_tokens === \"number\") {\n      const usage = typeof entry.budget_usage_tokens === \"number\" ? formatNumber(entry.budget_usage_tokens) : \"n/a\";\n      const limit = typeof entry.budget_limit_tokens === \"number\" ? formatNumber(entry.budget_limit_tokens) : \"n/a\";\n      detail.textContent = usage + \" / \" + limit + \" tokens\";\n    } else {\n      detail.textContent = \"No token budget configured.\";\n    }\n    container.append(detail);\n    const metaParts = [];\n    if (typeof entry.budget_window_minutes === \"number\") {\n      metaParts.push(\"Window \" + formatNumber(entry.budget_window_minutes) + \"m\");\n    }\n    if (entry.budget_policy) {\n      metaParts.push(\"Policy \" + entry.budget_policy);\n    }\n    if (metaParts.length) {\n      const meta = document.createElement(\"div\");\n      meta.className = \"muted\";\n      meta.textContent = metaParts.join(\" \\u2022 \");\n      container.append(meta);\n    }\n    if (entry.budget_message) {\n      const message = document.createElement(\"div\");\n      message.className = status === \"hard_limited\" ? \"status-error\" : \"muted\";\n      message.textContent = \"Budget stopped continuation: \" + entry.budget_message;\n      container.append(message);\n    }\n    return container;\n  }\n  function formatBudgetSummary(entry) {\n    const status = entry && entry.budget_status ? entry.budget_status : null;\n    const parts = [\"Budget: \" + formatBudgetStatusLabel(status)];\n    if (status === \"telemetry_unavailable\") {\n      parts.push(\"usage unavailable\");\n    } else if (entry && (typeof entry.budget_usage_tokens === \"number\" || typeof entry.budget_limit_tokens === \"number\")) {\n      const usage = typeof entry.budget_usage_tokens === \"number\" ? formatNumber(entry.budget_usage_tokens) : \"n/a\";\n      const limit = typeof entry.budget_limit_tokens === \"number\" ? formatNumber(entry.budget_limit_tokens) : \"n/a\";\n      parts.push(usage + \" / \" + limit + \" tokens\");\n    }\n    if (entry && typeof entry.budget_window_minutes === \"number\") {\n      parts.push(\"window \" + formatNumber(entry.budget_window_minutes) + \"m\");\n    }\n    if (entry && entry.budget_policy) {\n      parts.push(\"policy \" + entry.budget_policy);\n    }\n    if (entry && entry.budget_message) {\n      parts.push(entry.budget_message);\n    }\n    return parts.join(\" \\u2022 \");\n  }\n  function formatTokenDimension(value, unavailableLabel) {\n    return typeof value === \"number\" ? formatNumber(value) : unavailableLabel;\n  }\n  function formatOverviewTokenValue(payload, field, splitUnavailable) {\n    const codexTotals = payload && payload.codex_totals;\n    if (!codexTotals) {\n      return \"Unavailable\";\n    }\n    if (splitUnavailable && field !== \"total_tokens\" && field !== \"model_context_window\") {\n      return \"Split unavailable\";\n    }\n    return formatTokenDimension(codexTotals[field], \"0\");\n  }\n  function formatTokenBreakdown(tokens, telemetrySource) {\n    if (tokens && tokens.token_split_status === \"aggregate_only\") {\n      return \"Split unavailable\" + (telemetrySource ? \" \\u2022 \" + telemetrySource : \"\");\n    }\n    const parts = [\n      \"In \" + formatNumber(tokens.input_tokens),\n      \"Out \" + formatNumber(tokens.output_tokens)\n    ];\n    if (typeof tokens.cached_input_tokens === \"number\") {\n      parts.push(\"Cached \" + formatNumber(tokens.cached_input_tokens));\n    }\n    if (typeof tokens.reasoning_output_tokens === \"number\") {\n      parts.push(\"Reasoning \" + formatNumber(tokens.reasoning_output_tokens));\n    }\n    if (typeof tokens.model_context_window === \"number\") {\n      parts.push(\"Context \" + formatNumber(tokens.model_context_window));\n    }\n    return parts.join(\" / \") + (telemetrySource ? \" \\u2022 \" + telemetrySource : \"\");\n  }\n  function formatApiError(payload, fallbackMessage) {\n    if (!payload || !payload.error) {\n      return fallbackMessage;\n    }\n    if (payload.error.code && payload.error.message) {\n      return payload.error.code + \": \" + payload.error.message;\n    }\n    if (payload.error.message) {\n      return String(payload.error.message);\n    }\n    return fallbackMessage;\n  }\n\n  // src/api/dashboard-client/issue-detail.ts\n  var issueDetailRequestSequence = 0;\n  function createStateBadge(stateValue) {\n    const badge = document.createElement(\"span\");\n    badge.className = \"state-badge\";\n    const normalized = String(stateValue || \"\").toLowerCase();\n    if (normalized.includes(\"progress\") || normalized.includes(\"running\")) {\n      badge.classList.add(\"state-active\");\n    } else if (normalized.includes(\"todo\") || normalized.includes(\"backlog\")) {\n      badge.classList.add(\"state-idle\");\n    } else if (normalized.includes(\"done\") || normalized.includes(\"closed\")) {\n      badge.classList.add(\"state-terminal\");\n    } else {\n      badge.classList.add(\"state-neutral\");\n    }\n    badge.textContent = stateValue || \"unknown\";\n    return badge;\n  }\n  function createProvisioningBadge(label, ok) {\n    const badge = document.createElement(\"span\");\n    badge.className = \"mini-badge \" + (ok ? \"mini-badge-good\" : \"mini-badge-bad\");\n    badge.textContent = label + \": \" + (ok ? \"yes\" : \"no\");\n    return badge;\n  }\n  function normalizeActionability(value) {\n    const actionability = String(value || \"none\");\n    return actionability === \"required\" || actionability === \"recommended\" ? actionability : \"none\";\n  }\n  function createOperatorHintBadge(hint) {\n    if (!hint) {\n      return null;\n    }\n    const actionability = normalizeActionability(hint.actionability);\n    const badge = document.createElement(\"span\");\n    badge.className = \"operator-hint actionability-\" + actionability;\n    badge.textContent = String(hint.headline || hint.classification || \"Runtime diagnostics\");\n    badge.title = \"Actionability: \" + actionability + \" \\u2022 Classification: \" + String(hint.classification || \"unknown\");\n    return badge;\n  }\n  function renderIssueExplainer(explainer) {\n    if (!explainer) {\n      elements.issueExplainerCard.classList.add(\"hidden\");\n      return;\n    }\n    const actionability = normalizeActionability(explainer.actionability);\n    elements.issueExplainerCard.className = \"operator-explainer actionability-\" + actionability;\n    elements.issueExplainerActionability.className = \"status-pill actionability-\" + actionability;\n    elements.issueExplainerActionability.textContent = actionability;\n    elements.issueExplainerHeadline.textContent = explainer.headline || \"Runtime diagnostics unavailable\";\n    elements.issueExplainerClassification.textContent = explainer.classification || \"unknown\";\n    elements.issueExplainerReason.textContent = (explainer.reason_code || \"n/a\") + (explainer.reason_detail ? \" \\u2022 \" + explainer.reason_detail : \"\");\n    elements.issueExplainerAction.textContent = Array.isArray(explainer.recommended_actions) && explainer.recommended_actions.length ? explainer.recommended_actions.join(\"; \") : \"No operator action required\";\n    elements.issueExplainerTransition.textContent = explainer.expected_transition || \"No automatic transition expected\";\n    elements.issueExplainerVersion.textContent = explainer.version || \"unknown\";\n    elements.issueExplainerDetail.textContent = explainer.detail || \"\";\n  }\n  function appendDefinitionValue(list, label, value) {\n    const wrapper = document.createElement(\"div\");\n    const term = document.createElement(\"dt\");\n    const definition = document.createElement(\"dd\");\n    term.textContent = label;\n    definition.textContent = value === null || value === void 0 || value === \"\" ? \"n/a\" : String(value);\n    wrapper.append(term, definition);\n    list.append(wrapper);\n  }\n  function renderThreadBlockerCard(blocker) {\n    elements.threadBlockerCard.replaceChildren();\n    if (!blocker) {\n      appendDefinitionValue(elements.threadBlockerCard, \"classification\", \"n/a\");\n      appendDefinitionValue(elements.threadBlockerCard, \"reason_code\", \"n/a\");\n      appendDefinitionValue(elements.threadBlockerCard, \"reason_detail\", \"n/a\");\n      appendDefinitionValue(elements.threadBlockerCard, \"time_since_progress\", \"n/a\");\n      appendDefinitionValue(elements.threadBlockerCard, \"recommended_actions\", \"No blocker actions reported.\");\n      appendDefinitionValue(elements.threadBlockerCard, \"expected_auto_transition\", \"n/a\");\n      return;\n    }\n    appendDefinitionValue(elements.threadBlockerCard, \"classification\", blocker.classification);\n    appendDefinitionValue(elements.threadBlockerCard, \"reason_code\", blocker.reason_code);\n    appendDefinitionValue(elements.threadBlockerCard, \"reason_detail\", blocker.reason_detail);\n    appendDefinitionValue(elements.threadBlockerCard, \"time_since_progress\", blocker.time_since_progress);\n    if (blocker.missing_tool_output_recovery) {\n      const recovery = blocker.missing_tool_output_recovery;\n      appendDefinitionValue(elements.threadBlockerCard, \"recovery_headline\", recovery.headline);\n      appendDefinitionValue(elements.threadBlockerCard, \"recovery_state\", recovery.status);\n      appendDefinitionValue(elements.threadBlockerCard, \"recovery_next_action\", recovery.next_action);\n      appendDefinitionValue(elements.threadBlockerCard, \"original_tool\", recovery.original_tool_name);\n      appendDefinitionValue(elements.threadBlockerCard, \"original_call_id\", recovery.original_call_id);\n      appendDefinitionValue(elements.threadBlockerCard, \"evidence_source\", recovery.evidence_source);\n      appendDefinitionValue(elements.threadBlockerCard, \"elapsed_wait_ms\", recovery.elapsed_wait_ms);\n      appendDefinitionValue(\n        elements.threadBlockerCard,\n        \"active_ownership\",\n        recovery.active_ownership ? [\n          \"issue \" + (recovery.active_ownership.issue_identifier || recovery.active_ownership.issue_id || \"n/a\"),\n          \"thread \" + (recovery.active_ownership.thread_id || \"n/a\"),\n          \"turn \" + (recovery.active_ownership.turn_id || \"n/a\"),\n          \"session \" + (recovery.active_ownership.session_id || \"n/a\"),\n          \"app_server_owned \" + String(Boolean(recovery.active_ownership.app_server_owned))\n        ].join(\" | \") : \"n/a\"\n      );\n      appendDefinitionValue(\n        elements.threadBlockerCard,\n        \"recovery_lineage\",\n        [\n          \"interrupt \" + (recovery.interrupt_cancel_result && recovery.interrupt_cancel_result.status || \"n/a\"),\n          \"replacement_thread \" + (recovery.replacement_turn && recovery.replacement_turn.thread_id || \"n/a\"),\n          \"replacement_turn \" + (recovery.replacement_turn && recovery.replacement_turn.turn_id || \"n/a\"),\n          \"prompt \" + (recovery.guarded_prompt_dispatch && recovery.guarded_prompt_dispatch.status || \"n/a\"),\n          \"outcome \" + (recovery.final_outcome && recovery.final_outcome.result || \"n/a\")\n        ].join(\" | \")\n      );\n    }\n    appendDefinitionValue(\n      elements.threadBlockerCard,\n      \"recommended_actions\",\n      Array.isArray(blocker.recommended_actions) ? blocker.recommended_actions.join(\"; \") : \"n/a\"\n    );\n    appendDefinitionValue(elements.threadBlockerCard, \"expected_auto_transition\", blocker.expected_auto_transition);\n  }\n  function spanLabel(span, kind) {\n    if (kind === \"phase\") {\n      return span.phase || \"phase\";\n    }\n    if (kind === \"tool\") {\n      return span.tool_name || \"tool\";\n    }\n    return span.reason_code || span.status || \"wait\";\n  }\n  function renderTimelineLane(title, spans, kind) {\n    const lane = document.createElement(\"section\");\n    lane.className = \"timeline-lane timeline-lane-\" + kind;\n    const heading = document.createElement(\"h4\");\n    heading.textContent = title;\n    lane.append(heading);\n    const safeSpans = Array.isArray(spans) ? spans : [];\n    if (!safeSpans.length) {\n      const empty = document.createElement(\"p\");\n      empty.className = \"muted\";\n      empty.textContent = \"No \" + title.toLowerCase() + \" spans.\";\n      lane.append(empty);\n      return lane;\n    }\n    const list = document.createElement(\"ul\");\n    for (const span of safeSpans) {\n      const item = document.createElement(\"li\");\n      const label = document.createElement(\"strong\");\n      const meta = document.createElement(\"span\");\n      label.textContent = spanLabel(span, kind);\n      meta.textContent = \" | started \" + formatDate(span.started_at_ms) + \" | ended \" + (span.ended_at_ms === null || span.ended_at_ms === void 0 ? \"open\" : formatDate(span.ended_at_ms)) + \" | duration \" + (span.duration_ms === null || span.duration_ms === void 0 ? \"open\" : String(span.duration_ms)) + \" | status \" + (span.status || \"n/a\") + \" | reason \" + (span.reason_code || \"n/a\") + \" | \" + (span.reason_detail || \"n/a\");\n      item.append(label, meta);\n      list.append(item);\n    }\n    lane.append(list);\n    return lane;\n  }\n  function renderThreadDiagnostics(diagnostics) {\n    if (!diagnostics) {\n      elements.threadDetail.classList.add(\"hidden\");\n      elements.threadRawEvents.textContent = \"Detailed diagnostics are not loaded.\";\n      return;\n    }\n    elements.threadDetail.classList.remove(\"hidden\");\n    elements.threadTimelineLanes.replaceChildren(\n      renderTimelineLane(\"Phase\", diagnostics.phase_spans, \"phase\"),\n      renderTimelineLane(\"Tool\", diagnostics.tool_spans, \"tool\"),\n      renderTimelineLane(\"Wait\", diagnostics.wait_spans, \"wait\")\n    );\n    renderThreadBlockerCard(diagnostics.current_blocker || null);\n    const warnings = Array.isArray(diagnostics.capability_warnings) ? diagnostics.capability_warnings : [];\n    if (!warnings.length) {\n      elements.threadCapabilityWarnings.className = \"capability-warnings muted\";\n      elements.threadCapabilityWarnings.textContent = \"No capability warnings.\";\n    } else {\n      elements.threadCapabilityWarnings.className = \"capability-warnings\";\n      const list = document.createElement(\"ul\");\n      for (const warning of warnings) {\n        const item = document.createElement(\"li\");\n        item.textContent = (warning.reason_code || \"capability_warning\") + \" | source \" + (warning.source_environment || \"n/a\") + \" | tool \" + (warning.attempted_tool_name || \"n/a\") + \" | call \" + (warning.call_id || \"n/a\") + \" | thread \" + (warning.thread_id || \"n/a\") + \" | turn \" + (warning.turn_id || \"n/a\") + \" | \" + (warning.unsupported_capability_message || \"unsupported capability\") + \" | recovery \" + (warning.recommended_recovery_action || \"n/a\");\n        list.append(item);\n      }\n      elements.threadCapabilityWarnings.replaceChildren(list);\n    }\n    const events = Array.isArray(diagnostics.timeline) ? diagnostics.timeline : [];\n    elements.threadRawEvents.textContent = events.length ? events.map(function(event) {\n      return formatDate(event.at_ms) + \" | \" + event.event + \" | thread \" + (event.thread_id || \"n/a\") + \" | turn \" + (event.turn_id || \"n/a\") + \" | session \" + (event.session_id || \"n/a\") + \" | reason \" + (event.reason_code || \"n/a\") + \" | \" + (event.reason_detail || \"n/a\");\n    }).join(\"\\\\n\") : \"No raw event stream entries.\";\n  }\n  function getDiagnosticSummary(entry) {\n    return entry && entry.transcript_tool_call_diagnostic_summary ? entry.transcript_tool_call_diagnostic_summary : null;\n  }\n  function formatDiagnosticSummary(summary) {\n    if (!summary) {\n      return \"Summary diagnostics: unavailable\";\n    }\n    const parts = [];\n    parts.push(summary.detailed_diagnostics_available ? \"detail available\" : \"summary only\");\n    parts.push(formatNumber(summary.total_count || 0) + \" transcript records\");\n    if (summary.active_missing_tool_output && summary.active_missing_tool_output.active) {\n      parts.push(\"missing output \" + (summary.active_missing_tool_output.tool_name || summary.active_missing_tool_output.call_id || \"active\"));\n    }\n    if (summary.recovery && summary.recovery.active) {\n      parts.push(\"recovery \" + (summary.recovery.status || \"active\"));\n    }\n    if (summary.newest_observed_at) {\n      parts.push(\"newest \" + formatDate(summary.newest_observed_at));\n    }\n    return \"Summary diagnostics: \" + parts.join(\" | \");\n  }\n  function formatConversationSummary(conversation) {\n    if (!conversation || !conversation.metadata) {\n      return \"Conversation: no evidence loaded\";\n    }\n    const latest = conversation.latest || {};\n    const counts = conversation.metadata.role_counts || {};\n    const countParts = [\"system\", \"user\", \"assistant\", \"tool\", \"runtime\"].filter(function(role) {\n      return counts[role] > 0;\n    }).map(function(role) {\n      return role + \" \" + counts[role];\n    });\n    const latestText = latest.summary ? \"latest \" + (latest.role || \"message\") + \": \" + latest.summary : \"latest unavailable\";\n    return \"Conversation: \" + formatNumber(conversation.metadata.included_count || 0) + \" shown\" + (conversation.metadata.truncated ? \" of \" + formatNumber(conversation.metadata.total_available_count || 0) : \"\") + (countParts.length ? \" | \" + countParts.join(\", \") : \"\") + \" | \" + latestText;\n  }\n  function renderConversation(conversation) {\n    const roleFilter = state.filter && state.filter.conversationRole || \"all\";\n    const density = state.filter && state.filter.conversationDensity || \"comfortable\";\n    elements.conversationSummary.textContent = formatConversationSummary(conversation);\n    elements.conversationList.className = \"conversation-list conversation-density-\" + density;\n    const messages = Array.isArray(conversation && conversation.messages) ? conversation.messages : [];\n    const visibleMessages = messages.filter(function(message) {\n      return roleFilter === \"all\" || message.role === roleFilter;\n    });\n    if (!visibleMessages.length) {\n      const empty = document.createElement(\"li\");\n      empty.className = \"muted\";\n      empty.textContent = messages.length ? \"No messages match this role filter.\" : \"No conversation evidence is available yet.\";\n      elements.conversationList.replaceChildren(empty);\n      return;\n    }\n    const nodes = visibleMessages.map(function(message) {\n      const item = document.createElement(\"li\");\n      item.className = \"conversation-item conversation-role-\" + (message.role || \"runtime\");\n      const meta = document.createElement(\"div\");\n      meta.className = \"conversation-meta\";\n      const role = document.createElement(\"span\");\n      role.className = \"conversation-role\";\n      role.textContent = message.role || \"runtime\";\n      const source = document.createElement(\"span\");\n      source.textContent = formatDate(message.at) + \" | \" + (message.source || \"unknown\") + (message.event ? \" | \" + message.event : \"\") + (message.thread_id ? \" | thread \" + message.thread_id : \"\") + (message.tool_name ? \" | tool \" + message.tool_name : \"\");\n      meta.append(role, source);\n      const body = document.createElement(\"p\");\n      body.textContent = (message.content || \"n/a\") + (message.truncated ? \" [truncated]\" : \"\");\n      item.append(meta, body);\n      return item;\n    });\n    elements.conversationList.replaceChildren(...nodes);\n  }\n  function formatInputDecisionContext(detail) {\n    if (!detail) {\n      return null;\n    }\n    if (detail.includes(\"input_required_unanswerable\")) {\n      return \"Input handling: unanswerable schema (manual resume required)\";\n    }\n    if (detail.includes(\"non_interactive_fallback\")) {\n      return \"Input handling: non-interactive fallback answer\";\n    }\n    if (detail.includes(\"approval_option_permissive\")) {\n      return \"Input handling: permissive approval option selected\";\n    }\n    if (detail.includes(\"approval_option_exact\")) {\n      return \"Input handling: exact approval option selected\";\n    }\n    return null;\n  }\n  function createActionButton(text, className, onClick) {\n    const button = document.createElement(\"button\");\n    button.type = \"button\";\n    button.className = className;\n    button.textContent = text;\n    button.addEventListener(\"click\", function(event) {\n      event.stopPropagation();\n      onClick();\n    });\n    return button;\n  }\n  async function copyText(value) {\n    if (!value) {\n      setRefreshStatus(\"No value to copy\", true);\n      return;\n    }\n    try {\n      if (navigator.clipboard && navigator.clipboard.writeText) {\n        await navigator.clipboard.writeText(value);\n      } else {\n        const helper = document.createElement(\"textarea\");\n        helper.value = value;\n        helper.setAttribute(\"readonly\", \"readonly\");\n        helper.style.position = \"absolute\";\n        helper.style.left = \"-9999px\";\n        document.body.appendChild(helper);\n        helper.select();\n        document.execCommand(\"copy\");\n        document.body.removeChild(helper);\n      }\n      setRefreshStatus(\"Copied: \" + value, false);\n    } catch (error) {\n      setRefreshStatus(\"Copy failed: \" + String(error), true);\n    }\n  }\n  async function loadIssue(identifier, options) {\n    const issueId = (identifier || \"\").trim();\n    if (!issueId) {\n      return;\n    }\n    const requestSequence = ++issueDetailRequestSequence;\n    const loadOptions = options || {};\n    if (loadOptions.openPanel !== false && !elements.issuePanel.open) {\n      state.suppressIssuePanelToggleLoad = true;\n      elements.issuePanel.open = true;\n      setTimeout(function() {\n        state.suppressIssuePanelToggleLoad = false;\n      }, 0);\n    }\n    try {\n      const payload = await fetchJson(\"/api/v1/\" + encodeURIComponent(issueId));\n      let diagnostics = null;\n      let diagnosticsLoadFailed = false;\n      try {\n        diagnostics = await fetchJson(\"/api/v1/issues/\" + encodeURIComponent(issueId) + \"/diagnostics\");\n      } catch (_diagnosticsError) {\n        diagnosticsLoadFailed = true;\n        diagnostics = null;\n      }\n      if (requestSequence !== issueDetailRequestSequence) {\n        return;\n      }\n      state.selectedIssue = issueId;\n      elements.issueInput.value = issueId;\n      const summaryParts = [];\n      summaryParts.push(\"Status: \" + (payload.status || \"unknown\"));\n      summaryParts.push(\"Snapshot: \" + (payload.snapshot_freshness_state || \"unknown\") + \" age \" + formatNumber(payload.snapshot_age_ms) + \"ms\");\n      if (payload.api_degraded_mode) {\n        summaryParts.push(\"API degraded: \" + (payload.api_degraded_reason_code || \"unknown\"));\n      }\n      if (payload.workspace && payload.workspace.path) {\n        summaryParts.push(\"Workspace: \" + payload.workspace.path);\n      }\n      if (payload.retry && payload.retry.stop_reason_code) {\n        summaryParts.push(\"Stop reason: \" + payload.retry.stop_reason_code);\n      }\n      if (payload.blocked && payload.blocked.stop_reason_code) {\n        summaryParts.push(\"Blocked reason: \" + getActionRequiredLabel(payload.blocked.stop_reason_code));\n      }\n      if (payload.retry && payload.retry.previous_session_id) {\n        summaryParts.push(\"Previous session: \" + payload.retry.previous_session_id);\n      }\n      if (payload.blocked && payload.blocked.previous_session_id) {\n        summaryParts.push(\"Previous session: \" + payload.blocked.previous_session_id);\n      }\n      if (payload.running && payload.running.current_phase) {\n        summaryParts.push(\"Current phase: \" + payload.running.current_phase);\n      }\n      if (payload.running && payload.running.turn_control_state) {\n        summaryParts.push(\"Turn control: \" + getTurnControlLabel(payload.running.turn_control_state));\n        summaryParts.push(\"Progress: \" + getProgressSignalLabel(payload.running.progress_signal_state));\n      }\n      if (payload.running && payload.running.token_telemetry_confidence) {\n        summaryParts.push(\"Token quality: \" + getTokenConfidenceLabel(payload.running.token_telemetry_confidence));\n      }\n      if (payload.running && payload.running.not_blocked_explainer_text) {\n        summaryParts.push(\"Why not blocked: \" + payload.running.not_blocked_explainer_text);\n      }\n      if (payload.blocked && payload.blocked.turn_control_state) {\n        summaryParts.push(\"Turn control: \" + getTurnControlLabel(payload.blocked.turn_control_state));\n      }\n      if (payload.retry && payload.retry.last_phase || payload.blocked && payload.blocked.last_phase) {\n        summaryParts.push(\"Last phase before stop: \" + (payload.retry && payload.retry.last_phase || payload.blocked && payload.blocked.last_phase));\n      }\n      if (payload.operator_explainer) {\n        summaryParts.push(\"Actionability: \" + payload.operator_explainer.actionability);\n      }\n      const runningOrRetry = payload.running || payload.retry || payload.blocked;\n      if (runningOrRetry && runningOrRetry.provisioner_type) {\n        summaryParts.push(\"Provisioner: \" + runningOrRetry.provisioner_type);\n      }\n      if (runningOrRetry && runningOrRetry.branch_name) {\n        summaryParts.push(\"Branch: \" + runningOrRetry.branch_name);\n      }\n      if (runningOrRetry && runningOrRetry.workspace_git_status) {\n        summaryParts.push(\"Workspace git: \" + runningOrRetry.workspace_git_status);\n      }\n      if (runningOrRetry && typeof runningOrRetry.workspace_provisioned === \"boolean\") {\n        summaryParts.push(\"Provisioned: \" + (runningOrRetry.workspace_provisioned ? \"yes\" : \"no\"));\n      }\n      if (runningOrRetry && typeof runningOrRetry.workspace_is_git_worktree === \"boolean\") {\n        summaryParts.push(\"Git worktree: \" + (runningOrRetry.workspace_is_git_worktree ? \"yes\" : \"no\"));\n      }\n      if (runningOrRetry) {\n        summaryParts.push(formatBudgetSummary(runningOrRetry));\n        summaryParts.push(formatDiagnosticSummary(getDiagnosticSummary(runningOrRetry)));\n      }\n      summaryParts.push(\n        diagnostics ? \"Detailed diagnostics: loaded\" : diagnosticsLoadFailed ? \"Detailed diagnostics: unavailable\" : \"Detailed diagnostics: not loaded\"\n      );\n      if (state.runtimeResolution && state.runtimeResolution.workspace_root) {\n        summaryParts.push(\"Runtime workspace root: \" + state.runtimeResolution.workspace_root);\n      }\n      elements.issueSummary.textContent = summaryParts.join(\" \\u2022 \");\n      renderConversation(payload.conversation || null);\n      renderIssueExplainer(payload.operator_explainer || null);\n      renderThreadDiagnostics(diagnostics);\n      const timeline = Array.isArray(payload.phase_timeline) ? payload.phase_timeline : [];\n      const sessionConsole = payload.blocked && Array.isArray(payload.blocked.session_console) ? payload.blocked.session_console : [];\n      const timelineText = timeline.length ? timeline.map(function(marker) {\n        return formatDate(marker.at) + \" | \" + marker.phase + \" | attempt \" + marker.attempt + \" | \" + (marker.detail || \"n/a\") + \" | thread \" + (marker.thread_id || \"n/a\") + \" | session \" + (marker.session_id || \"n/a\");\n      }).join(\"\\\\n\") : \"No phase markers yet.\";\n      const operatorTimelineRows = deriveOperatorTransitionRows(issueId, payload);\n      const operatorActions = Array.isArray(payload.operator_actions) ? payload.operator_actions : [];\n      const availableActions = payload.blocked && Array.isArray(payload.blocked.available_actions) ? payload.blocked.available_actions : [];\n      const availableActionText = availableActions.length ? availableActions.map(function(entry) {\n        return (entry.label || entry.id || \"action\") + \" | \" + (entry.method || \"POST\") + \" \" + (entry.endpoint || \"n/a\");\n      }).join(\"\\\\n\") : \"No backend-advertised issue actions.\";\n      const operatorActionText = operatorActions.length ? operatorActions.map(function(entry) {\n        return formatDate(entry.requested_at_ms) + \" | \" + (entry.actor || \"operator\") + \" | \" + entry.action + \" | \" + entry.result + \" | \" + (entry.result_code || \"n/a\") + \" | \" + (entry.reason_note || \"no reason note\") + \" | \" + (entry.message || \"n/a\");\n      }).join(\"\\\\n\") : \"No operator action outcomes.\";\n      const operatorTimelineText = operatorTimelineRows.length ? operatorTimelineRows.map(function(entry) {\n        return formatDate(entry.at) + \" | \" + entry.label + \" | issue \" + entry.issue_identifier + \" | \" + entry.result + \" | \" + entry.detail;\n      }).join(\"\\\\n\") : \"No operator transition entries.\";\n      const sessionConsoleText = sessionConsole.length ? sessionConsole.map(function(event) {\n        return formatDate(event.at) + \" | \" + event.event + \" | \" + (event.message || \"n/a\");\n      }).join(\"\\\\n\") : \"No session console entries.\";\n      const budgetText = runningOrRetry ? formatBudgetSummary(runningOrRetry) : \"Budget: not configured\";\n      elements.issueOutput.textContent = \"Operator Transition Timeline\\\\n\" + operatorTimelineText + \"\\\\n\\\\nOperator Action Outcomes\\\\n\" + operatorActionText + \"\\\\n\\\\nAvailable Actions\\\\n\" + availableActionText + \"\\\\n\\\\nExecution Timeline\\\\n\" + timelineText + \"\\\\n\\\\nSession Console\\\\n\" + sessionConsoleText + \"\\\\n\\\\nBudget\\\\n\" + budgetText + \"\\\\n\\\\nIssue JSON (canonical UTC ISO values preserved)\\\\n\" + JSON.stringify(payload, null, 2);\n      if (state.payload) {\n        renderRunning(state.payload);\n      }\n      scheduleStateSave();\n    } catch (error) {\n      if (requestSequence !== issueDetailRequestSequence) {\n        return;\n      }\n      elements.issueSummary.textContent = \"Issue detail degraded: fallback mode active.\";\n      renderConversation(null);\n      renderIssueExplainer(null);\n      renderThreadDiagnostics(null);\n      elements.issueOutput.textContent = \"Issue load failed: \" + String(error) + \"\\\\n\\\\nFallback message\\\\nIssue detail payload is unavailable. Use only backend-advertised actions from the state snapshot when available.\";\n    }\n  }\n\n  // src/api/dashboard-client/operator-actions.ts\n  function buildResumeRequest(reasonNote, resumeOverrideReason) {\n    return {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify(\n        resumeOverrideReason ? { resume_override_reason: resumeOverrideReason, reason_note: reasonNote } : { reason_note: reasonNote }\n      )\n    };\n  }\n  function buildCancelRequest(reasonNote, cancelReason) {\n    return {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify({ cancel_reason: cancelReason || reasonNote, reason_note: reasonNote, confirmed: true })\n    };\n  }\n  function buildOperatorActionRequest(reasonNote, destructive) {\n    return {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify({ reason_note: reasonNote, confirmed: destructive ? true : void 0 })\n    };\n  }\n  function buildBlockedInputRequest(pending, reasonNote, answer) {\n    return {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify({\n        request_id: pending.request_id,\n        reason_note: reasonNote,\n        answer\n      })\n    };\n  }\n  function buildDrainControlRequest(reason, extra = {}) {\n    const body = { ...extra };\n    if (reason) {\n      body.reason = reason;\n    }\n    return {\n      method: \"POST\",\n      headers: { \"content-type\": \"application/json\" },\n      body: JSON.stringify(body)\n    };\n  }\n  function describeDrainControlBlockers(payload) {\n    const blockers = Array.isArray(payload && payload.blockers) ? payload.blockers : payload && payload.quiescence && Array.isArray(payload.quiescence.blockers) ? payload.quiescence.blockers : [];\n    return blockers.map(function(blocker) {\n      return blocker.reason || blocker.detail || blocker.category || null;\n    }).filter(Boolean).join(\"; \");\n  }\n  function setDrainControlStatus(message, isError) {\n    if (elements.drainControlStatus) {\n      elements.drainControlStatus.textContent = message;\n      elements.drainControlStatus.className = isError ? \"status-error\" : \"status-ok\";\n    }\n    setRefreshStatus(message, isError);\n  }\n  async function postDrainControl(url, init) {\n    const response = await fetch(url, init);\n    const payload = await response.json();\n    return { response, payload };\n  }\n  async function enterDrainMode() {\n    try {\n      const reason = window.prompt(\"Reason for entering Drain Mode\", \"safe runtime restart\");\n      if (!reason) {\n        setDrainControlStatus(\"Drain Mode entry skipped: reason is required\", true);\n        return;\n      }\n      await fetchJson(\"/api/v1/drain-mode/enter\", buildDrainControlRequest(reason));\n      setDrainControlStatus(\"Drain Mode entry requested\", false);\n      await loadStateViaPoll2();\n    } catch (error) {\n      setDrainControlStatus(\"Drain Mode entry failed: \" + String(error), true);\n    }\n  }\n  async function exitDrainMode() {\n    try {\n      const reason = window.prompt(\"Reason for exiting Drain Mode\", \"restart complete\");\n      if (!reason) {\n        setDrainControlStatus(\"Drain Mode exit skipped: reason is required\", true);\n        return;\n      }\n      await fetchJson(\"/api/v1/drain-mode/exit\", buildDrainControlRequest(reason));\n      setDrainControlStatus(\"Drain Mode exit requested\", false);\n      await loadStateViaPoll2();\n    } catch (error) {\n      setDrainControlStatus(\"Drain Mode exit failed: \" + String(error), true);\n    }\n  }\n  async function waitForDrainQuiescence() {\n    try {\n      const { payload } = await postDrainControl(\"/api/v1/drain-mode/wait\", buildDrainControlRequest(null, { timeout_ms: 3e4 }));\n      if (payload && payload.success) {\n        setDrainControlStatus(\"Drain wait complete: shutdown/restart is safe\", false);\n      } else {\n        setDrainControlStatus(\n          \"Drain wait timed out: \" + (describeDrainControlBlockers(payload) || \"blockers are still present\") + \". No forced cancel was requested.\",\n          true\n        );\n      }\n      await loadStateViaPoll2();\n    } catch (error) {\n      const message = String(error);\n      setDrainControlStatus(\"Drain wait timed out: \" + message + \". No forced cancel was requested.\", true);\n      await loadStateViaPoll2();\n    }\n  }\n  async function requestDrainSafeShutdown() {\n    try {\n      if (!window.confirm(\"Request safe shutdown now?\")) {\n        setDrainControlStatus(\"Safe shutdown skipped: confirmation declined\", true);\n        return;\n      }\n      const { payload } = await postDrainControl(\"/api/v1/drain-mode/shutdown\", buildDrainControlRequest(null, { override: false }));\n      setDrainControlStatus(\n        payload && payload.success ? \"Safe shutdown requested\" : \"Safe shutdown blocked: \" + (describeDrainControlBlockers(payload) || \"blockers are still present\"),\n        !(payload && payload.success)\n      );\n      await loadStateViaPoll2();\n    } catch (error) {\n      setDrainControlStatus(\"Safe shutdown failed: \" + String(error) + \". No forced cancel was requested.\", true);\n      await loadStateViaPoll2();\n    }\n  }\n  function describeRuntimeUpdateResult(payload) {\n    const reason = payload && payload.reason_code ? \" (\" + payload.reason_code + \")\" : \"\";\n    const action = payload && payload.recommended_action ? \" Next: \" + payload.recommended_action.replace(/_/g, \" \") + \".\" : \"\";\n    const restart = payload && payload.restart && Array.isArray(payload.restart.command) ? payload.restart.status === \"restarting\" ? \" Supervisor restart requested; reconnecting.\" : \" Restart command: \" + payload.restart.command.join(\" \") : \"\";\n    return (payload && payload.status ? String(payload.status).replace(/_/g, \" \") : \"runtime update response\") + reason + \".\" + action + restart;\n  }\n  function setRuntimeUpdateStatus(message, isError) {\n    if (elements.runtimeUpdateStatus) {\n      elements.runtimeUpdateStatus.textContent = message;\n      elements.runtimeUpdateStatus.className = isError ? \"status-error\" : \"status-ok\";\n    }\n    setRefreshStatus(message, isError);\n  }\n  async function prepareRuntimeUpdate() {\n    try {\n      const payload = await fetchJson(\"/api/v1/runtime-update/prepare\", buildDrainControlRequest(null));\n      setRuntimeUpdateStatus(\"Runtime update prepare: \" + describeRuntimeUpdateResult(payload), !payload.success);\n      await loadStateViaPoll2();\n    } catch (error) {\n      setRuntimeUpdateStatus(\"Runtime update prepare failed: \" + String(error), true);\n      await loadStateViaPoll2();\n    }\n  }\n  async function applyRuntimeUpdate() {\n    try {\n      const payload = await fetchJson(\"/api/v1/runtime-update/apply\", buildDrainControlRequest(null));\n      setRuntimeUpdateStatus(\"Runtime update apply: \" + describeRuntimeUpdateResult(payload), !payload.success);\n      await loadStateViaPoll2();\n    } catch (error) {\n      setRuntimeUpdateStatus(\"Runtime update apply failed: \" + String(error), true);\n      await loadStateViaPoll2();\n    }\n  }\n  async function resumeBlockedIssue(issueIdentifier, resumeOverrideReason) {\n    try {\n      const reasonNote = window.prompt(\"Reason note for resuming this blocked issue\", \"\");\n      if (!reasonNote) {\n        setRefreshStatus(\"Resume skipped: reason note is required\", true);\n        return;\n      }\n      const payload = await fetchJson(\"/api/v1/issues/\" + encodeURIComponent(issueIdentifier) + \"/resume\", buildResumeRequest(reasonNote, resumeOverrideReason));\n      setRefreshStatus(\"Resume requested for \" + payload.issue_identifier, false);\n      await loadStateViaPoll2();\n      if (state.selectedIssue === issueIdentifier) {\n        await loadIssue(issueIdentifier);\n      }\n    } catch (error) {\n      setRefreshStatus(\"Resume failed: \" + String(error), true);\n    }\n  }\n  async function cancelBlockedIssue(issueIdentifier, cancelReason) {\n    try {\n      const reasonNote = window.prompt(\"Reason note for cancelling this blocked issue\", cancelReason || \"\");\n      if (!reasonNote) {\n        setRefreshStatus(\"Cancel skipped: reason note is required\", true);\n        return;\n      }\n      if (!window.confirm(\"Cancel this blocked issue to backlog?\")) {\n        setRefreshStatus(\"Cancel skipped: confirmation declined\", true);\n        return;\n      }\n      const payload = await fetchJson(\"/api/v1/issues/\" + encodeURIComponent(issueIdentifier) + \"/cancel\", buildCancelRequest(reasonNote, cancelReason));\n      setRefreshStatus(\"Cancel requested for \" + payload.issue_identifier + \" -> \" + payload.moved_to_state, false);\n      await loadStateViaPoll2();\n      if (state.selectedIssue === issueIdentifier) {\n        await loadIssue(issueIdentifier);\n      }\n    } catch (error) {\n      setRefreshStatus(\"Cancel failed: \" + String(error), true);\n    }\n  }\n  async function runOperatorAction(issueIdentifier, actionPath, destructive) {\n    try {\n      const reasonNote = window.prompt(\"Reason note for \" + actionPath.replace(\"-\", \" \"), \"\");\n      if (!reasonNote) {\n        setRefreshStatus(\"Action skipped: reason note is required\", true);\n        return;\n      }\n      if (destructive && !window.confirm('Run destructive action \"' + actionPath + '\" for ' + issueIdentifier + \"?\")) {\n        setRefreshStatus(\"Action skipped: confirmation declined\", true);\n        return;\n      }\n      const payload = await fetchJson(\"/api/v1/issues/\" + encodeURIComponent(issueIdentifier) + \"/\" + actionPath, buildOperatorActionRequest(reasonNote, destructive));\n      if (payload && actionPath === \"clear-automation-fault\") {\n        setRefreshStatus(\n          \"Clear Fault + Retry \" + (payload.status || \"requested\") + \" for \" + (payload.issue_identifier || issueIdentifier) + \": \" + (payload.message || payload.result_code || \"request accepted\"),\n          payload.status === \"held\"\n        );\n      } else {\n        setRefreshStatus(\"Operator action requested for \" + (payload.issue_identifier || issueIdentifier), false);\n      }\n      await loadStateViaPoll2();\n      if (state.selectedIssue === issueIdentifier) {\n        await loadIssue(issueIdentifier);\n      }\n    } catch (error) {\n      setRefreshStatus(\"Operator action failed: \" + String(error), true);\n    }\n  }\n  async function submitBlockedInput(entry) {\n    try {\n      if (!entry.pending_input || !entry.pending_input.request_id) {\n        throw new Error(\"No pending input request payload\");\n      }\n      const pending = entry.pending_input;\n      const firstQuestion = Array.isArray(pending.questions) && pending.questions.length ? pending.questions[0] : null;\n      const questionId = firstQuestion && firstQuestion.id ? firstQuestion.id : void 0;\n      let answer;\n      if (pending.input_schema_type === \"options\" && firstQuestion && Array.isArray(firstQuestion.options) && firstQuestion.options.length) {\n        const labels = firstQuestion.options.map(function(option) {\n          return option.label;\n        });\n        const selected = window.prompt((pending.prompt_text || \"Select option\") + \"\\\\nOptions: \" + labels.join(\", \"), labels[0] || \"\");\n        answer = { question_id: questionId, option_label: selected || \"\" };\n      } else {\n        const text = window.prompt(pending.prompt_text || \"Enter response\", \"\");\n        answer = { question_id: questionId, text: text || \"\" };\n      }\n      const reasonNote = window.prompt(\"Reason note for submitting this blocked input\", \"\");\n      if (!reasonNote) {\n        setRefreshStatus(\"Input submit skipped: reason note is required\", true);\n        return;\n      }\n      const payload = await fetchJson(\"/api/v1/issues/\" + encodeURIComponent(entry.issue_identifier) + \"/input\", buildBlockedInputRequest(pending, reasonNote, answer));\n      setRefreshStatus(\n        \"Input submitted for \" + payload.issue_identifier + \" using \" + (payload.resume_mode || \"unknown\") + \" mode (\" + (payload.resume_reason_code || \"n/a\") + \")\",\n        false\n      );\n      await loadStateViaPoll2();\n      if (state.selectedIssue === entry.issue_identifier) {\n        await loadIssue(entry.issue_identifier);\n      }\n    } catch (error) {\n      setRefreshStatus(\"Input submit failed: \" + String(error), true);\n    }\n  }\n\n  // src/api/dashboard-client/issues.ts\n  function rowMatchesFilter(entry) {\n    if (state.filter.status === \"running\" && entry.state.toLowerCase().includes(\"retry\")) {\n      return false;\n    }\n    if (state.filter.status === \"retrying\" && !entry.state.toLowerCase().includes(\"retry\")) {\n      return false;\n    }\n    if (state.filter.status === \"blocked\") {\n      return false;\n    }\n    if (!state.filter.query) {\n      return true;\n    }\n    return entry.issue_identifier.toLowerCase().includes(state.filter.query.toLowerCase());\n  }\n  function renderRunning(payload) {\n    const rows = payload.running.filter(rowMatchesFilter);\n    if (!rows.length) {\n      const emptyRow = document.createElement(\"tr\");\n      const cell = document.createElement(\"td\");\n      cell.colSpan = 14;\n      cell.className = \"muted\";\n      cell.textContent = \"No running issues match current filters.\";\n      emptyRow.appendChild(cell);\n      elements.runningRows.replaceChildren(emptyRow);\n      return;\n    }\n    const nodes = rows.map((entry) => {\n      const row = document.createElement(\"tr\");\n      row.setAttribute(\"data-issue\", entry.issue_identifier);\n      if (state.selectedIssue === entry.issue_identifier) {\n        row.classList.add(\"selected-row\");\n      }\n      const issueCell = document.createElement(\"td\");\n      const issueLink = document.createElement(\"a\");\n      issueLink.href = \"#thread-detail-\" + encodeURIComponent(entry.issue_identifier);\n      issueLink.textContent = entry.issue_identifier;\n      issueLink.addEventListener(\"click\", function(event) {\n        event.preventDefault();\n        event.stopPropagation();\n        elements.issueInput.value = entry.issue_identifier;\n        void loadIssue(entry.issue_identifier);\n      });\n      issueCell.append(issueLink);\n      const stateCell = document.createElement(\"td\");\n      stateCell.appendChild(createStateBadge(entry.state));\n      const stateFlags = document.createElement(\"div\");\n      stateFlags.className = \"inline-badges\";\n      const turnBadge = document.createElement(\"span\");\n      turnBadge.className = \"status-pill \" + (entry.turn_control_state === \"operator_turn\" ? \"failed\" : \"pending\");\n      turnBadge.textContent = getTurnControlLabel(entry.turn_control_state);\n      stateFlags.append(turnBadge);\n      const progressBadge = document.createElement(\"span\");\n      progressBadge.className = \"status-pill \" + (entry.progress_signal_state === \"stalled_waiting\" ? \"failed\" : \"pending\");\n      progressBadge.textContent = getProgressSignalLabel(entry.progress_signal_state);\n      stateFlags.append(progressBadge);\n      if (entry.awaiting_input) {\n        const awaitingBadge = document.createElement(\"span\");\n        awaitingBadge.className = \"status-pill pending\";\n        awaitingBadge.textContent = \"Awaiting Input\";\n        stateFlags.append(awaitingBadge);\n      }\n      if (entry.stalled_waiting && entry.progress_signal_state !== \"stalled_waiting\") {\n        const stalledBadge = document.createElement(\"span\");\n        stalledBadge.className = \"status-pill failed\";\n        stalledBadge.textContent = \"Stalled Waiting\";\n        stateFlags.append(stalledBadge);\n      }\n      const runningHintBadge = createOperatorHintBadge(entry.operator_explainer_hint);\n      if (runningHintBadge && entry.operator_explainer_hint.actionability !== \"none\") {\n        stateFlags.append(runningHintBadge);\n      }\n      if (stateFlags.children.length > 0) {\n        stateCell.append(stateFlags);\n      }\n      const sessionCell = document.createElement(\"td\");\n      const sessionValue = document.createElement(\"div\");\n      sessionValue.textContent = entry.session_id || \"n/a\";\n      const sessionMeta = document.createElement(\"div\");\n      sessionMeta.className = \"muted\";\n      const sessionMetaParts = [];\n      if (entry.worker_host) {\n        sessionMetaParts.push(\"Host \" + entry.worker_host);\n      }\n      if (entry.workspace_path) {\n        sessionMetaParts.push(entry.workspace_path);\n      }\n      if (entry.provisioner_type) {\n        sessionMetaParts.push(\"Provisioner \" + entry.provisioner_type);\n      }\n      if (entry.branch_name) {\n        sessionMetaParts.push(\"Branch \" + entry.branch_name);\n      }\n      if (entry.workspace_git_status) {\n        sessionMetaParts.push(\"Git \" + entry.workspace_git_status);\n      }\n      sessionMeta.textContent = sessionMetaParts.length ? sessionMetaParts.join(\" \\u2022 \") : \"Host n/a\";\n      sessionCell.append(sessionValue, sessionMeta);\n      const phaseCell = document.createElement(\"td\");\n      const phaseLabel = document.createElement(\"div\");\n      phaseLabel.textContent = entry.current_phase || \"n/a\";\n      const phaseMeta = document.createElement(\"div\");\n      phaseMeta.className = \"muted\";\n      if (entry.current_phase_at) {\n        const phaseAgeMs = Date.now() - Date.parse(entry.current_phase_at);\n        const stale = Number.isFinite(phaseAgeMs) && phaseAgeMs > DASHBOARD_CONFIG.phase_stale_warn_ms;\n        phaseMeta.textContent = (entry.phase_elapsed_ms ? \"elapsed \" + Math.floor(entry.phase_elapsed_ms / 1e3) + \"s\" : \"elapsed n/a\") + \" \\u2022 phase unchanged for \" + formatDurationFromIso(entry.current_phase_at) + (stale ? \" \\u2022 No phase movement yet\" : \"\");\n      } else {\n        phaseMeta.textContent = \"No phase movement yet\";\n      }\n      const threadActivityMeta = document.createElement(\"div\");\n      threadActivityMeta.className = \"muted\";\n      const threadActivity = entry.codex_thread_activity || null;\n      if (threadActivity && threadActivity.updated_at) {\n        const threadStatus = threadActivity.thread_status ? \" \\u2022 \" + threadActivity.thread_status : \"\";\n        threadActivityMeta.textContent = \"Codex thread active \" + formatDurationFromIso(threadActivity.updated_at) + \" ago\" + threadStatus;\n      } else if (entry.thread_id) {\n        threadActivityMeta.textContent = \"Codex thread activity unavailable\";\n      } else {\n        threadActivityMeta.textContent = \"Codex thread n/a\";\n      }\n      phaseCell.append(phaseLabel, phaseMeta, threadActivityMeta);\n      const runtimeCell = document.createElement(\"td\");\n      runtimeCell.className = \"runtime-cell\";\n      runtimeCell.setAttribute(\"data-started-at\", entry.started_at);\n      runtimeCell.textContent = formatDurationFromIso(entry.started_at);\n      if (entry.awaiting_input_since_ms) {\n        const awaitingTimer = document.createElement(\"div\");\n        awaitingTimer.className = \"muted\";\n        awaitingTimer.textContent = \"Awaiting input: \" + formatDurationFromMs(entry.awaiting_input_since_ms);\n        runtimeCell.append(awaitingTimer);\n      }\n      if (entry.stalled_waiting_since_ms) {\n        const stalledTimer = document.createElement(\"div\");\n        stalledTimer.className = \"muted\";\n        stalledTimer.textContent = \"Stalled waiting: \" + formatDurationFromMs(entry.stalled_waiting_since_ms);\n        runtimeCell.append(stalledTimer);\n      }\n      if (entry.not_blocked_explainer_text) {\n        const explainer = document.createElement(\"div\");\n        explainer.className = \"muted\";\n        explainer.textContent = \"Why not blocked: \" + entry.not_blocked_explainer_text;\n        runtimeCell.append(explainer);\n      }\n      const turnsCell = document.createElement(\"td\");\n      turnsCell.textContent = formatNumber(entry.turn_count);\n      const tokensCell = document.createElement(\"td\");\n      const tokenTotal = document.createElement(\"div\");\n      const telemetryStatus = entry.token_telemetry_status || \"unavailable\";\n      const telemetryConfidence = entry.token_telemetry_confidence || (telemetryStatus === \"available\" ? \"observed_live\" : \"missing\");\n      if (telemetryStatus === \"pending\") {\n        tokenTotal.textContent = \"Pending\";\n      } else if (telemetryConfidence === \"missing\") {\n        tokenTotal.textContent = \"Missing telemetry\";\n      } else {\n        tokenTotal.textContent = \"Total: \" + formatNumber(entry.tokens.total_tokens);\n      }\n      const tokenDetail = document.createElement(\"div\");\n      tokenDetail.className = \"muted\";\n      if (telemetryStatus === \"available\") {\n        tokenDetail.textContent = formatTokenBreakdown(entry.tokens, entry.token_telemetry_source);\n      } else {\n        tokenDetail.textContent = telemetryStatus === \"pending\" ? \"Waiting for first usage payload\" : \"No telemetry path detected\";\n      }\n      const tokenBadge = document.createElement(\"span\");\n      tokenBadge.className = \"mini-badge \" + (telemetryConfidence === \"missing\" ? \"mini-badge-bad\" : \"mini-badge-good\");\n      tokenBadge.title = \"Token telemetry confidence/source quality\";\n      tokenBadge.textContent = getTokenConfidenceLabel(telemetryConfidence);\n      tokensCell.append(tokenTotal, tokenBadge, tokenDetail);\n      tokensCell.append(createBudgetBlock(entry));\n      const blockerCell = document.createElement(\"td\");\n      const blockerValue = document.createElement(\"div\");\n      blockerValue.textContent = entry.current_blocker_class || \"n/a\";\n      const diagnosticSummary = document.createElement(\"div\");\n      diagnosticSummary.className = \"muted\";\n      diagnosticSummary.textContent = formatDiagnosticSummary(getDiagnosticSummary(entry));\n      blockerCell.append(blockerValue, diagnosticSummary);\n      const timeSinceProgressCell = document.createElement(\"td\");\n      timeSinceProgressCell.textContent = typeof entry.time_since_progress === \"number\" ? String(entry.time_since_progress) + \" ms (\" + formatElapsedMs(entry.time_since_progress) + \")\" : \"n/a\";\n      const lastSuccessfulStepCell = document.createElement(\"td\");\n      lastSuccessfulStepCell.textContent = entry.last_successful_step || \"n/a\";\n      const eventCell = document.createElement(\"td\");\n      eventCell.textContent = entry.last_event_summary || entry.last_event || \"n/a\";\n      const messageCell = document.createElement(\"td\");\n      const conversationLatest = entry.conversation_latest || null;\n      const messageValue = document.createElement(\"div\");\n      messageValue.textContent = conversationLatest && conversationLatest.summary || entry.last_message || \"n/a\";\n      messageCell.append(messageValue);\n      if (conversationLatest && conversationLatest.role) {\n        const conversationMeta = document.createElement(\"div\");\n        conversationMeta.className = \"muted\";\n        conversationMeta.textContent = \"Conversation latest: \" + conversationLatest.role + \" via \" + (conversationLatest.source || \"unknown\") + (conversationLatest.at ? \" @ \" + formatDate(conversationLatest.at) : \"\");\n        messageCell.append(conversationMeta);\n      }\n      const lastEventAtCell = document.createElement(\"td\");\n      lastEventAtCell.textContent = formatDate(entry.last_event_at);\n      const actionsCell = document.createElement(\"td\");\n      actionsCell.className = \"action-cell\";\n      const lastAction = Array.isArray(entry.operator_actions) && entry.operator_actions.length ? entry.operator_actions[entry.operator_actions.length - 1] : null;\n      if (lastAction) {\n        const actionOutcome = document.createElement(\"div\");\n        actionOutcome.className = \"muted\";\n        actionOutcome.textContent = \"Last action: \" + lastAction.action + \" \" + lastAction.result + (lastAction.result_code ? \" (\" + lastAction.result_code + \")\" : \"\");\n        actionsCell.append(actionOutcome);\n      }\n      const copySession = createActionButton(\"Copy Session\", \"ghost-button\", function() {\n        copyText(entry.session_id || \"\");\n      });\n      const copyThreadTurn = createActionButton(\"Copy Thread/Turn\", \"ghost-button\", function() {\n        if (entry.thread_id && entry.turn_id) {\n          copyText(entry.thread_id + \"/\" + entry.turn_id);\n          return;\n        }\n        setRefreshStatus(\"Thread/turn id unavailable\", true);\n      });\n      const openJson = createActionButton(\"JSON\", \"ghost-button\", function() {\n        window.open(\"/api/v1/\" + encodeURIComponent(entry.issue_identifier), \"_blank\", \"noopener\");\n      });\n      const respondNow = createActionButton(\"Respond Now\", \"ghost-button\", function() {\n        elements.issueInput.value = entry.issue_identifier;\n        void loadIssue(entry.issue_identifier);\n      });\n      respondNow.disabled = !entry.awaiting_input;\n      const investigate = createActionButton(\"Inspect Diagnostics\", \"ghost-button\", function() {\n        elements.issueInput.value = entry.issue_identifier;\n        void loadIssue(entry.issue_identifier);\n      });\n      investigate.disabled = !entry.stalled_waiting;\n      const cancelTurn = createActionButton(\"Cancel Turn\", \"ghost-button\", function() {\n        void runOperatorAction(entry.issue_identifier, \"cancel-turn\", true);\n      });\n      const requeue = createActionButton(\"Requeue\", \"ghost-button\", function() {\n        void runOperatorAction(entry.issue_identifier, \"requeue\", true);\n      });\n      actionsCell.append(copySession, copyThreadTurn, respondNow, investigate, cancelTurn, requeue, openJson);\n      row.append(\n        issueCell,\n        stateCell,\n        sessionCell,\n        phaseCell,\n        runtimeCell,\n        turnsCell,\n        tokensCell,\n        blockerCell,\n        timeSinceProgressCell,\n        lastSuccessfulStepCell,\n        eventCell,\n        messageCell,\n        lastEventAtCell,\n        actionsCell\n      );\n      row.addEventListener(\"click\", function() {\n        elements.issueInput.value = entry.issue_identifier;\n        void loadIssue(entry.issue_identifier);\n      });\n      return row;\n    });\n    elements.runningRows.replaceChildren(...nodes);\n  }\n  function renderRetry(payload) {\n    const rows = payload.retrying.filter(function(entry) {\n      if (state.filter.status === \"running\" || state.filter.status === \"blocked\") {\n        return false;\n      }\n      if (!state.filter.query) {\n        return true;\n      }\n      return entry.issue_identifier.toLowerCase().includes(state.filter.query.toLowerCase());\n    });\n    if (!rows.length) {\n      const emptyRow = document.createElement(\"tr\");\n      const cell = document.createElement(\"td\");\n      cell.colSpan = 10;\n      cell.className = \"muted\";\n      cell.textContent = state.filter.status === \"retrying\" ? \"No retrying issues match current filters.\" : \"No issues are waiting for retry.\";\n      emptyRow.appendChild(cell);\n      elements.retryRows.replaceChildren(emptyRow);\n      return;\n    }\n    const nodes = rows.map((entry) => {\n      const row = document.createElement(\"tr\");\n      const issueCell = document.createElement(\"td\");\n      issueCell.textContent = entry.issue_identifier;\n      const attemptCell = document.createElement(\"td\");\n      attemptCell.textContent = formatNumber(entry.attempt);\n      const dueAtCell = document.createElement(\"td\");\n      const retryState = document.createElement(\"div\");\n      retryState.className = \"status-pill pending\";\n      retryState.textContent = getRetryStateLabel(entry);\n      const retryDue = document.createElement(\"div\");\n      retryDue.className = \"muted\";\n      retryDue.textContent = formatDate(entry.due_at);\n      dueAtCell.append(retryState, retryDue);\n      const errorCell = document.createElement(\"td\");\n      errorCell.textContent = entry.error || \"n/a\";\n      const hostCell = document.createElement(\"td\");\n      hostCell.textContent = entry.worker_host || \"n/a\";\n      const workspaceCell = document.createElement(\"td\");\n      workspaceCell.textContent = entry.workspace_path || \"n/a\";\n      const provisioningCell = document.createElement(\"td\");\n      const provisioningType = document.createElement(\"div\");\n      provisioningType.textContent = entry.provisioner_type || \"n/a\";\n      const provisioningDetail = document.createElement(\"div\");\n      provisioningDetail.className = \"muted\";\n      const provisioningParts = [];\n      if (entry.branch_name) {\n        provisioningParts.push(\"Branch \" + entry.branch_name);\n      }\n      if (entry.workspace_git_status) {\n        provisioningParts.push(\"Git \" + entry.workspace_git_status);\n      }\n      if (entry.workspace_exists === false) {\n        provisioningParts.push(\"Missing workspace\");\n      }\n      provisioningDetail.textContent = provisioningParts.length ? provisioningParts.join(\" \\u2022 \") : \"n/a\";\n      const provisioningFlags = document.createElement(\"div\");\n      provisioningFlags.className = \"inline-badges\";\n      provisioningFlags.append(\n        createProvisioningBadge(\"Provisioned\", Boolean(entry.workspace_provisioned)),\n        createProvisioningBadge(\"Git worktree\", Boolean(entry.workspace_is_git_worktree))\n      );\n      provisioningCell.append(provisioningType, provisioningDetail, provisioningFlags);\n      const stopReasonCell = document.createElement(\"td\");\n      const stopReasonCode = document.createElement(\"div\");\n      stopReasonCode.textContent = entry.stop_reason_code || \"n/a\";\n      const stopReasonDetail = document.createElement(\"div\");\n      stopReasonDetail.className = \"muted\";\n      stopReasonDetail.textContent = entry.stop_reason_detail || \"n/a\";\n      stopReasonCell.append(stopReasonCode, stopReasonDetail);\n      stopReasonCell.append(createBudgetBlock(entry));\n      const retryHintBadge = createOperatorHintBadge(entry.operator_explainer_hint);\n      if (retryHintBadge) {\n        stopReasonCell.append(retryHintBadge);\n      }\n      const lastPhaseLine = document.createElement(\"div\");\n      lastPhaseLine.className = \"muted\";\n      lastPhaseLine.textContent = \"Last phase: \" + (entry.last_phase || \"n/a\") + (entry.last_phase_at ? \" @ \" + formatDate(entry.last_phase_at) : \"\");\n      stopReasonCell.append(lastPhaseLine);\n      if (entry.last_phase_detail) {\n        const lastPhaseDetailLine = document.createElement(\"div\");\n        lastPhaseDetailLine.className = \"muted\";\n        lastPhaseDetailLine.textContent = entry.last_phase_detail;\n        stopReasonCell.append(lastPhaseDetailLine);\n      }\n      const inputDecision = formatInputDecisionContext(entry.stop_reason_detail || \"\");\n      if (inputDecision) {\n        const decisionLine = document.createElement(\"div\");\n        decisionLine.className = \"muted\";\n        decisionLine.textContent = inputDecision;\n        stopReasonCell.append(decisionLine);\n      }\n      if (entry.last_input_submit) {\n        const submitModeLine = document.createElement(\"div\");\n        submitModeLine.className = \"muted\";\n        submitModeLine.textContent = \"Last submit: \" + entry.last_input_submit.resume_mode + \" (\" + entry.last_input_submit.resume_reason_code + \") @ \" + formatDate(entry.last_input_submit.submitted_at);\n        stopReasonCell.append(submitModeLine);\n        if (entry.last_input_submit.resume_mode === \"fallback\") {\n          const fallbackBanner = document.createElement(\"div\");\n          fallbackBanner.className = \"status-pill pending\";\n          fallbackBanner.textContent = \"Native continuation unavailable; resumed via prompt context fallback.\";\n          stopReasonCell.append(fallbackBanner);\n        }\n      }\n      if (entry.pending_input) {\n        const pending = entry.pending_input;\n        const requestLine = document.createElement(\"div\");\n        requestLine.className = \"muted\";\n        requestLine.textContent = \"Request: \" + (pending.request_id || \"n/a\") + \" (\" + (pending.input_schema_type || \"unknown\") + \")\";\n        stopReasonCell.append(requestLine);\n        if (pending.prompt_text) {\n          const promptLine = document.createElement(\"div\");\n          promptLine.textContent = pending.prompt_text;\n          stopReasonCell.append(promptLine);\n        }\n      }\n      const previousSessionCell = document.createElement(\"td\");\n      const previousSessionValue = document.createElement(\"div\");\n      previousSessionValue.textContent = entry.previous_session_id || \"n/a\";\n      const previousThreadValue = document.createElement(\"div\");\n      previousThreadValue.className = \"muted\";\n      previousThreadValue.textContent = entry.previous_thread_id ? \"Thread \" + entry.previous_thread_id : \"Thread n/a\";\n      previousSessionCell.append(previousSessionValue, previousThreadValue);\n      const actionsCell = document.createElement(\"td\");\n      const copyPreviousSession = createActionButton(\"Copy Prev Session\", \"ghost-button\", function() {\n        copyText(entry.previous_session_id || \"\");\n      });\n      const copyPreviousThread = createActionButton(\"Copy Prev Thread\", \"ghost-button\", function() {\n        copyText(entry.previous_thread_id || \"\");\n      });\n      const openJson = createActionButton(\"JSON\", \"ghost-button\", function() {\n        window.open(\"/api/v1/\" + encodeURIComponent(entry.issue_identifier), \"_blank\", \"noopener\");\n      });\n      const retryStep = createActionButton(\"Retry Step\", \"ghost-button\", function() {\n        void runOperatorAction(entry.issue_identifier, \"retry-step\", false);\n      });\n      const requeue = createActionButton(\"Requeue\", \"ghost-button\", function() {\n        void runOperatorAction(entry.issue_identifier, \"requeue\", false);\n      });\n      actionsCell.append(copyPreviousSession, copyPreviousThread, retryStep, requeue, openJson);\n      row.append(\n        issueCell,\n        attemptCell,\n        dueAtCell,\n        hostCell,\n        workspaceCell,\n        provisioningCell,\n        stopReasonCell,\n        previousSessionCell,\n        errorCell,\n        actionsCell\n      );\n      return row;\n    });\n    elements.retryRows.replaceChildren(...nodes);\n  }\n  function renderBlocked(payload) {\n    const rows = (payload.blocked || []).filter(function(entry) {\n      if (state.filter.status === \"running\" || state.filter.status === \"retrying\") {\n        return false;\n      }\n      if (state.filter.blockedReason !== \"all\" && entry.stop_reason_code !== state.filter.blockedReason) {\n        return false;\n      }\n      if (!state.filter.query) {\n        return true;\n      }\n      return entry.issue_identifier.toLowerCase().includes(state.filter.query.toLowerCase());\n    });\n    if (!rows.length) {\n      const emptyRow = document.createElement(\"tr\");\n      const cell = document.createElement(\"td\");\n      cell.colSpan = 8;\n      cell.className = \"muted\";\n      cell.textContent = state.filter.status === \"blocked\" ? \"No blocked issues match current filters.\" : \"No issues are blocked on operator input.\";\n      emptyRow.appendChild(cell);\n      elements.blockedRows.replaceChildren(emptyRow);\n      return;\n    }\n    const nodes = rows.map((entry) => {\n      const row = document.createElement(\"tr\");\n      const issueCell = document.createElement(\"td\");\n      issueCell.textContent = entry.issue_identifier;\n      const attemptCell = document.createElement(\"td\");\n      attemptCell.textContent = formatNumber(entry.attempt);\n      const blockedAtCell = document.createElement(\"td\");\n      blockedAtCell.textContent = formatDate(entry.blocked_at);\n      const hostCell = document.createElement(\"td\");\n      hostCell.textContent = entry.worker_host || \"n/a\";\n      const workspaceCell = document.createElement(\"td\");\n      workspaceCell.textContent = entry.workspace_path || \"n/a\";\n      const provisioningFlags = document.createElement(\"div\");\n      provisioningFlags.className = \"inline-badges\";\n      provisioningFlags.append(\n        createProvisioningBadge(\"Provisioned\", Boolean(entry.workspace_provisioned)),\n        createProvisioningBadge(\"Git worktree\", Boolean(entry.workspace_is_git_worktree))\n      );\n      workspaceCell.append(provisioningFlags);\n      const stopReasonCell = document.createElement(\"td\");\n      const rootCauseBlock = createBlockedRootCauseBlock(entry);\n      if (rootCauseBlock) {\n        stopReasonCell.append(rootCauseBlock);\n      }\n      const stopReasonCode = document.createElement(\"div\");\n      stopReasonCode.textContent = rootCauseBlock ? \"Current operator block: \" + (entry.current_operator_block?.reason_code || entry.stop_reason_code || \"n/a\") : entry.stop_reason_code || \"n/a\";\n      const stopReasonDetail = document.createElement(\"div\");\n      stopReasonDetail.className = \"muted\";\n      stopReasonDetail.textContent = rootCauseBlock ? \"Current block detail: \" + (entry.current_operator_block?.detail || entry.stop_reason_detail || \"n/a\") : entry.stop_reason_detail || \"n/a\";\n      stopReasonCell.append(stopReasonCode, stopReasonDetail);\n      stopReasonCell.append(createBudgetBlock(entry));\n      if (entry.stop_reason_code) {\n        const stateLabel = document.createElement(\"div\");\n        stateLabel.className = \"status-pill failed\";\n        stateLabel.textContent = getActionRequiredLabel(entry.stop_reason_code);\n        stopReasonCell.append(stateLabel);\n      }\n      const controlLine = document.createElement(\"div\");\n      controlLine.className = \"inline-badges\";\n      const turnBadge = document.createElement(\"span\");\n      turnBadge.className = \"status-pill failed\";\n      turnBadge.textContent = getTurnControlLabel(entry.turn_control_state);\n      const progressBadge = document.createElement(\"span\");\n      progressBadge.className = \"status-pill failed\";\n      progressBadge.textContent = getProgressSignalLabel(entry.progress_signal_state);\n      controlLine.append(turnBadge, progressBadge);\n      stopReasonCell.append(controlLine);\n      const blockedHintBadge = createOperatorHintBadge(entry.operator_explainer_hint);\n      if (blockedHintBadge) {\n        stopReasonCell.append(blockedHintBadge);\n      }\n      const lastPhaseLine = document.createElement(\"div\");\n      lastPhaseLine.className = \"muted\";\n      lastPhaseLine.textContent = \"Last phase: \" + (entry.last_phase || \"n/a\") + (entry.last_phase_at ? \" @ \" + formatDate(entry.last_phase_at) : \"\");\n      stopReasonCell.append(lastPhaseLine);\n      if (entry.last_phase_detail) {\n        const lastPhaseDetailLine = document.createElement(\"div\");\n        lastPhaseDetailLine.className = \"muted\";\n        lastPhaseDetailLine.textContent = entry.last_phase_detail;\n        stopReasonCell.append(lastPhaseDetailLine);\n      }\n      const inputDecision = formatInputDecisionContext(entry.stop_reason_detail || \"\");\n      if (inputDecision) {\n        const decisionLine = document.createElement(\"div\");\n        decisionLine.className = \"muted\";\n        decisionLine.textContent = inputDecision;\n        stopReasonCell.append(decisionLine);\n      }\n      if (Array.isArray(entry.conflict_files) && entry.conflict_files.length) {\n        const conflictTitle = document.createElement(\"div\");\n        conflictTitle.className = \"muted\";\n        conflictTitle.textContent = \"Conflict files\";\n        stopReasonCell.append(conflictTitle);\n        const conflictChips = document.createElement(\"div\");\n        conflictChips.className = \"inline-badges\";\n        for (const conflict of entry.conflict_files) {\n          const chip = document.createElement(\"span\");\n          chip.className = \"mini-badge \" + (conflict.status === \"staged\" ? \"mini-badge-good\" : \"mini-badge-bad\");\n          chip.textContent = conflict.path + \" (\" + (conflict.status || \"unknown\") + \")\";\n          conflictChips.append(chip);\n        }\n        stopReasonCell.append(conflictChips);\n      }\n      if (Array.isArray(entry.required_actions) && entry.required_actions.length) {\n        const requiredActions = document.createElement(\"div\");\n        requiredActions.className = \"muted\";\n        requiredActions.textContent = \"Required actions: \" + entry.required_actions.join(\", \");\n        stopReasonCell.append(requiredActions);\n      }\n      const countWindow = document.createElement(\"div\");\n      countWindow.className = \"muted\";\n      countWindow.textContent = \"Attempt window: \" + formatNumber(entry.attempt_count_window) + \" in \" + formatNumber(entry.window_minutes) + \" minute(s)\";\n      stopReasonCell.append(countWindow);\n      const progressLine = document.createElement(\"div\");\n      progressLine.className = \"muted\";\n      progressLine.textContent = \"Last progress: \" + (entry.last_known_commit_sha || \"n/a\") + \" @ \" + (entry.last_progress_checkpoint_at ? formatDate(entry.last_progress_checkpoint_at) : \"n/a\");\n      stopReasonCell.append(progressLine);\n      const previousSessionCell = document.createElement(\"td\");\n      const previousSessionValue = document.createElement(\"div\");\n      previousSessionValue.textContent = entry.previous_session_id || \"n/a\";\n      const previousThreadValue = document.createElement(\"div\");\n      previousThreadValue.className = \"muted\";\n      previousThreadValue.textContent = entry.previous_thread_id ? \"Thread \" + entry.previous_thread_id : \"Thread n/a\";\n      previousSessionCell.append(previousSessionValue, previousThreadValue);\n      const actionsCell = document.createElement(\"td\");\n      const lastAction = Array.isArray(entry.operator_actions) && entry.operator_actions.length ? entry.operator_actions[entry.operator_actions.length - 1] : null;\n      if (lastAction) {\n        const actionOutcome = document.createElement(\"div\");\n        actionOutcome.className = \"muted\";\n        actionOutcome.textContent = \"Last action: \" + lastAction.action + \" \" + lastAction.result + (lastAction.result_code ? \" (\" + lastAction.result_code + \")\" : \"\");\n        actionsCell.append(actionOutcome);\n      }\n      const hasPendingInputRequest = Boolean(entry.pending_input && entry.pending_input.request_id);\n      let replyButton = null;\n      if (hasPendingInputRequest) {\n        replyButton = createActionButton(\"Reply\", \"ghost-button\", function() {\n          void submitBlockedInput(entry);\n        });\n      }\n      const copyPreviousSession = createActionButton(\"Copy Prev Session\", \"ghost-button\", function() {\n        copyText(entry.previous_session_id || \"\");\n      });\n      const copyWorkspace = createActionButton(\"Copy Workspace\", \"ghost-button\", function() {\n        copyText(entry.workspace_path || \"\");\n      });\n      const openJson = createActionButton(\"JSON\", \"ghost-button\", function() {\n        window.open(\"/api/v1/\" + encodeURIComponent(entry.issue_identifier), \"_blank\", \"noopener\");\n      });\n      if (replyButton) {\n        actionsCell.append(replyButton);\n      } else if (entry.runtime_state_kind !== \"automation_fault\") {\n        const manualResumeNote = document.createElement(\"div\");\n        manualResumeNote.className = \"muted\";\n        manualResumeNote.textContent = \"Manual resume required; no pending input request.\";\n        actionsCell.append(manualResumeNote);\n      }\n      const availableActions = Array.isArray(entry.available_actions) ? entry.available_actions : [];\n      availableActions.forEach(function(action) {\n        if (!action || !action.id || !action.label) {\n          return;\n        }\n        if (action.id === \"submit_input\") {\n          return;\n        }\n        const button = createActionButton(action.label, \"ghost-button\", function() {\n          if (action.id === \"resume\" && action.label === \"Push Commit + Resume\") {\n            void resumeBlockedIssue(entry.issue_identifier, \"operator_override_push_additional_commit\");\n            return;\n          }\n          if (action.id === \"resume\") {\n            void resumeBlockedIssue(entry.issue_identifier);\n            return;\n          }\n          if (action.id === \"cancel\") {\n            void cancelBlockedIssue(entry.issue_identifier, \"operator_cancel_return_to_backlog\");\n            return;\n          }\n          if (action.id === \"clear_automation_fault\") {\n            void runOperatorAction(entry.issue_identifier, \"clear-automation-fault\", Boolean(action.destructive));\n            return;\n          }\n          void runOperatorAction(entry.issue_identifier, action.id === \"retry_step\" ? \"retry-step\" : action.id, Boolean(action.destructive));\n        });\n        actionsCell.append(button);\n      });\n      if (entry.runtime_state_kind === \"automation_fault\") {\n        const faultNote = document.createElement(\"div\");\n        faultNote.className = \"muted\";\n        faultNote.textContent = \"No-progress redispatch circuit breaker: \" + (entry.breaker_hit_count || 0) + \" hit(s) in \" + (entry.breaker_window_minutes || 0) + \"m.\";\n        actionsCell.append(faultNote);\n      }\n      actionsCell.append(copyPreviousSession, copyWorkspace, openJson);\n      row.append(\n        issueCell,\n        attemptCell,\n        blockedAtCell,\n        hostCell,\n        workspaceCell,\n        stopReasonCell,\n        previousSessionCell,\n        actionsCell\n      );\n      return row;\n    });\n    elements.blockedRows.replaceChildren(...nodes);\n  }\n\n  // src/api/dashboard-client/overview.ts\n  var DRAIN_BLOCKER_LABELS = {\n    active_worker: \"Active workers\",\n    live_codex_app_server_process: \"Codex app servers\",\n    pending_retry: \"Pending retries\",\n    in_flight_tracker_write: \"Tracker writes\",\n    persistence_history_write: \"Current persistence/history writes\",\n    unknown_degraded_blocker_source_health: \"Unknown/degraded current blocker source\",\n    stale_runtime: \"Legacy stale-runtime blocker\",\n    unknown_current_build_identity: \"Legacy build-identity blocker\"\n  };\n  var DRAIN_WARNING_LABELS = {\n    stale_runtime_warning: \"Stale runtime warning\",\n    unknown_current_build_identity_warning: \"Build identity warning\",\n    persistence_history_degraded: \"Audit-health degradation\"\n  };\n  var DRAIN_BLOCKER_ORDER = [\n    \"active_worker\",\n    \"live_codex_app_server_process\",\n    \"pending_retry\",\n    \"in_flight_tracker_write\",\n    \"persistence_history_write\",\n    \"unknown_degraded_blocker_source_health\",\n    \"stale_runtime\",\n    \"unknown_current_build_identity\"\n  ];\n  function formatPendingWorkDetail(entry) {\n    const stateName = String(entry && entry.state ? entry.state : \"Unknown\");\n    const count = Number(entry && entry.count) || 0;\n    const countLabel = formatNumber(count);\n    if (entry && entry.maintenance_eligible) {\n      return \"Pending \" + stateName + \" maintenance work \" + countLabel + \": maintenance-eligible, not an active agent.\";\n    }\n    if (stateName === \"Agent Review\") {\n      return \"Pending Agent Review normal review work \" + countLabel + \": blocked until Symphony restarts on the current build, not an active agent.\";\n    }\n    return \"Pending \" + stateName + \" normal work \" + countLabel + \": blocked until Symphony restarts on the current build, not an active agent.\";\n  }\n  function isRecord(value) {\n    return Boolean(value) && typeof value === \"object\" && !Array.isArray(value);\n  }\n  function humanizeRateLimitKey(value) {\n    return String(value || \"Rate limit\").replace(/[_-]/g, \" \").replace(/\\b\\w/g, function(character) {\n      return character.toUpperCase();\n    });\n  }\n  function numberFromFields(source, keys) {\n    for (const key of keys) {\n      const value = Number(source[key]);\n      if (Number.isFinite(value)) {\n        return value;\n      }\n    }\n    return null;\n  }\n  function formatRateLimitValue(value) {\n    return value === null ? \"n/a\" : formatNumber(value);\n  }\n  function valueFromFields(source, keys) {\n    for (const key of keys) {\n      const value = source[key];\n      if (value !== void 0 && value !== null && value !== \"\") {\n        return value;\n      }\n    }\n    return null;\n  }\n  function formatRateLimitDetailValue(value) {\n    if (value === null || value === void 0 || value === \"\") {\n      return \"n/a\";\n    }\n    if (typeof value === \"number\" && Number.isFinite(value)) {\n      if (value > 1e12) {\n        return formatDate(value);\n      }\n      return formatNumber(value);\n    }\n    return String(value);\n  }\n  function parseRateLimitDate(value) {\n    if (value === null || value === void 0 || value === \"\") {\n      return null;\n    }\n    if (typeof value === \"number\" && Number.isFinite(value)) {\n      const timestamp = value > 1e12 ? value : value * 1e3;\n      return Number.isFinite(timestamp) ? timestamp : null;\n    }\n    const parsed = Date.parse(String(value));\n    return Number.isFinite(parsed) ? parsed : null;\n  }\n  function formatCompactDuration(seconds) {\n    if (seconds === null || !Number.isFinite(seconds)) {\n      return \"n/a\";\n    }\n    const rounded = Math.max(0, Math.round(seconds));\n    const days = Math.floor(rounded / 86400);\n    const hours = Math.floor(rounded % 86400 / 3600);\n    const minutes = Math.floor(rounded % 3600 / 60);\n    if (days > 0) {\n      return days + \"d \" + hours + \"h\";\n    }\n    if (hours > 0) {\n      return hours + \"h \" + minutes + \"m\";\n    }\n    return minutes + \"m\";\n  }\n  function windowSecondsFromRateLimit(entry) {\n    const seconds = numberFromFields(entry, [\"window_seconds\", \"windowSeconds\", \"reset_seconds\", \"resetSeconds\"]);\n    if (seconds !== null) {\n      return seconds;\n    }\n    const minutes = numberFromFields(entry, [\"window_minutes\", \"windowMinutes\"]);\n    if (minutes !== null) {\n      return minutes * 60;\n    }\n    const hours = numberFromFields(entry, [\"window_hours\", \"windowHours\"]);\n    if (hours !== null) {\n      return hours * 3600;\n    }\n    const days = numberFromFields(entry, [\"window_days\", \"windowDays\"]);\n    if (days !== null) {\n      return days * 86400;\n    }\n    return null;\n  }\n  function resetTimestampFromRateLimit(entry) {\n    const direct = valueFromFields(entry, [\"reset_at\", \"resetAt\", \"resets_at\", \"resetsAt\", \"reset_time\", \"resetTime\"]);\n    const parsed = parseRateLimitDate(direct);\n    if (parsed !== null) {\n      return parsed;\n    }\n    const resetInSeconds = numberFromFields(entry, [\"reset_in_seconds\", \"resetInSeconds\", \"resets_in_seconds\", \"resetsInSeconds\"]);\n    if (resetInSeconds !== null) {\n      return Date.now() + resetInSeconds * 1e3;\n    }\n    return null;\n  }\n  function describeRateLimitProjection(entry, remaining, limit, resetAtMs, windowSeconds) {\n    if (remaining === null || limit === null || limit <= 0 || resetAtMs === null || windowSeconds === null || windowSeconds <= 0) {\n      return {\n        status: \"schedule unknown\",\n        detail: \"Add reset and window data to project depletion.\",\n        surplusLabel: \"Surplus n/a\",\n        runwayLabel: \"Run-out n/a\"\n      };\n    }\n    const secondsToReset = Math.max(0, (resetAtMs - Date.now()) / 1e3);\n    const expectedRemaining = limit * Math.min(1, secondsToReset / windowSeconds);\n    const delta = Math.round(remaining - expectedRemaining);\n    const used = Math.max(0, limit - remaining);\n    const elapsed = Math.max(0, windowSeconds - secondsToReset);\n    const burnPerSecond = elapsed > 0 ? used / elapsed : 0;\n    const secondsToEmpty = burnPerSecond > 0 ? remaining / burnPerSecond : null;\n    const onSchedule = delta >= 0 || secondsToEmpty !== null && secondsToEmpty >= secondsToReset;\n    return {\n      status: onSchedule ? \"on schedule\" : \"deficit\",\n      detail: onSchedule ? \"Current pace lasts through reset.\" : \"Current pace runs out before reset.\",\n      surplusLabel: (delta >= 0 ? \"Surplus +\" : \"Deficit \") + formatNumber(delta),\n      runwayLabel: secondsToEmpty === null ? \"Run-out not projected\" : \"Run-out in \" + formatCompactDuration(secondsToEmpty)\n    };\n  }\n  function rateLimitWindowProgressPercent(resetAtMs, windowSeconds) {\n    if (resetAtMs === null || windowSeconds === null || windowSeconds <= 0) {\n      return null;\n    }\n    const secondsToReset = Math.max(0, (resetAtMs - Date.now()) / 1e3);\n    const elapsedSeconds = Math.max(0, Math.min(windowSeconds, windowSeconds - secondsToReset));\n    return Math.max(0, Math.min(100, elapsedSeconds / windowSeconds * 100));\n  }\n  function percentFromRateLimit(entry, remaining, limit) {\n    const explicit = numberFromFields(entry, [\"used_percent\", \"usedPercent\", \"usage_percent\", \"usagePercent\", \"percent_used\", \"percentUsed\"]);\n    if (explicit !== null) {\n      return Math.max(0, Math.min(100, explicit));\n    }\n    if (remaining !== null && limit !== null && limit > 0) {\n      return Math.max(0, Math.min(100, (limit - remaining) / limit * 100));\n    }\n    return null;\n  }\n  function rateLimitStatusClass(usedPercent) {\n    if (usedPercent === null) {\n      return \"\";\n    }\n    if (usedPercent >= 90) {\n      return \" rate-limit-card-critical\";\n    }\n    if (usedPercent >= 70) {\n      return \" rate-limit-card-warning\";\n    }\n    return \"\";\n  }\n  function rateLimitStatusLabel(usedPercent) {\n    if (usedPercent === null) {\n      return \"reported\";\n    }\n    if (usedPercent >= 90) {\n      return \"near limit\";\n    }\n    if (usedPercent >= 70) {\n      return \"watch\";\n    }\n    return \"healthy\";\n  }\n  function createRateLimitMetric(label, value) {\n    const metric = document.createElement(\"div\");\n    metric.className = \"rate-limit-metric\";\n    const labelNode = document.createElement(\"span\");\n    labelNode.textContent = label;\n    const valueNode = document.createElement(\"strong\");\n    valueNode.textContent = value;\n    metric.append(labelNode, valueNode);\n    return metric;\n  }\n  function createRateLimitChip(label, value) {\n    const chip = document.createElement(\"span\");\n    chip.className = \"rate-limit-chip\";\n    chip.textContent = label + \": \" + formatRateLimitDetailValue(value);\n    return chip;\n  }\n  function createRateLimitMarker(label, value, tone = \"\") {\n    const marker = document.createElement(\"div\");\n    marker.className = \"rate-limit-marker\" + (tone ? \" rate-limit-marker-\" + tone : \"\");\n    const labelNode = document.createElement(\"span\");\n    labelNode.textContent = label;\n    const valueNode = document.createElement(\"strong\");\n    valueNode.textContent = value;\n    marker.append(labelNode, valueNode);\n    return marker;\n  }\n  function createRateLimitSummary(entries) {\n    const summary = document.createElement(\"div\");\n    summary.className = \"rate-limit-summary\";\n    const label = document.createElement(\"span\");\n    label.textContent = \"Latest snapshot:\";\n    const uniqueSnapshots = Array.from(new Set(entries.map(function([, entry]) {\n      return valueFromFields(entry, [\"updated_at\", \"updatedAt\", \"observed_at\", \"observedAt\", \"reported_at\", \"reportedAt\"]);\n    }).filter(function(value2) {\n      return value2 !== null;\n    }).map(function(value2) {\n      return formatRateLimitDetailValue(value2);\n    })));\n    const value = document.createElement(\"strong\");\n    value.textContent = uniqueSnapshots.length === 0 ? \"current API state\" : uniqueSnapshots.length === 1 ? uniqueSnapshots[0] : uniqueSnapshots.length + \" reported times\";\n    summary.append(label, value);\n    return summary;\n  }\n  function rateLimitEntries(rateLimits) {\n    if (!isRecord(rateLimits)) {\n      return [];\n    }\n    const hasDirectLimitShape = [\"remaining\", \"limit\", \"used_percent\", \"usedPercent\", \"reset_at\", \"resetAt\", \"resets_at\", \"resetsAt\"].some(function(key) {\n      return key in rateLimits;\n    });\n    if (hasDirectLimitShape) {\n      return [[\"Current\", rateLimits]];\n    }\n    return Object.entries(rateLimits).filter(function(entry) {\n      return isRecord(entry[1]);\n    });\n  }\n  function renderRateLimits(rateLimits) {\n    const entries = rateLimitEntries(rateLimits);\n    if (!entries.length) {\n      const empty = document.createElement(\"article\");\n      empty.className = \"rate-limit-card rate-limit-empty\";\n      const title = document.createElement(\"div\");\n      title.className = \"rate-limit-title\";\n      const name = document.createElement(\"strong\");\n      name.className = \"rate-limit-name\";\n      name.textContent = \"No rate limits reported\";\n      const status = document.createElement(\"span\");\n      status.className = \"rate-limit-status\";\n      status.textContent = \"idle\";\n      title.append(name, status);\n      const detail = document.createElement(\"p\");\n      detail.className = \"muted\";\n      detail.textContent = \"No coding-agent rate-limit snapshot has been reported for the current runtime.\";\n      empty.append(title, detail);\n      elements.rateLimits.replaceChildren(empty);\n      return;\n    }\n    const summary = createRateLimitSummary(entries);\n    const cards = entries.map(function([name, entry]) {\n      const remaining = numberFromFields(entry, [\"remaining\", \"remaining_requests\", \"remainingRequests\"]);\n      const limit = numberFromFields(entry, [\"limit\", \"total\", \"total_limit\", \"totalLimit\"]);\n      const usedPercent = percentFromRateLimit(entry, remaining, limit);\n      const used = remaining !== null && limit !== null ? Math.max(0, limit - remaining) : null;\n      const resetAtMs = resetTimestampFromRateLimit(entry);\n      const windowSeconds = windowSecondsFromRateLimit(entry);\n      const projection = describeRateLimitProjection(entry, remaining, limit, resetAtMs, windowSeconds);\n      const windowProgressPercent = rateLimitWindowProgressPercent(resetAtMs, windowSeconds);\n      const card = document.createElement(\"article\");\n      card.className = \"rate-limit-card\" + rateLimitStatusClass(usedPercent);\n      const title = document.createElement(\"div\");\n      title.className = \"rate-limit-title\";\n      const label = document.createElement(\"strong\");\n      label.className = \"rate-limit-name\";\n      label.textContent = humanizeRateLimitKey(name);\n      const status = document.createElement(\"span\");\n      status.className = \"rate-limit-status\";\n      status.textContent = rateLimitStatusLabel(usedPercent);\n      title.append(label, status);\n      const meter = document.createElement(\"div\");\n      meter.className = \"rate-limit-meter\";\n      const fill = document.createElement(\"div\");\n      fill.className = \"rate-limit-meter-fill\";\n      fill.style.width = (usedPercent === null ? 0 : usedPercent).toFixed(0) + \"%\";\n      meter.append(fill);\n      if (windowProgressPercent !== null) {\n        const timeMarker = document.createElement(\"div\");\n        timeMarker.className = \"rate-limit-time-marker\";\n        timeMarker.style.left = windowProgressPercent.toFixed(0) + \"%\";\n        timeMarker.title = \"Window progress \" + windowProgressPercent.toFixed(0) + \"%\";\n        meter.append(timeMarker);\n      }\n      const meterLegend = document.createElement(\"div\");\n      meterLegend.className = \"rate-limit-meter-legend\";\n      const usageLegend = document.createElement(\"span\");\n      usageLegend.textContent = usedPercent === null ? \"Capacity used n/a\" : \"Capacity used \" + Math.round(usedPercent) + \"%\";\n      const progressLegend = document.createElement(\"span\");\n      progressLegend.textContent = windowProgressPercent === null ? \"Window progress n/a\" : \"Window progress \" + Math.round(windowProgressPercent) + \"%\";\n      meterLegend.append(usageLegend, progressLegend);\n      const metrics = document.createElement(\"div\");\n      metrics.className = \"rate-limit-metrics\";\n      metrics.append(\n        createRateLimitMetric(\"Remaining\", formatRateLimitValue(remaining)),\n        createRateLimitMetric(\"Used\", usedPercent === null ? formatRateLimitValue(used) : Math.round(usedPercent) + \"%\"),\n        createRateLimitMetric(\"Limit\", formatRateLimitValue(limit))\n      );\n      const details = document.createElement(\"div\");\n      details.className = \"rate-limit-detail-list\";\n      [\n        [\"Reset\", resetAtMs],\n        [\"Resets in\", resetAtMs === null ? null : formatCompactDuration((resetAtMs - Date.now()) / 1e3)],\n        [\"Window\", windowSeconds === null ? null : formatCompactDuration(windowSeconds)],\n        [\"Policy\", entry.policy ?? entry.type ?? entry.scope]\n      ].filter(function([, value]) {\n        return value !== void 0 && value !== null && value !== \"\";\n      }).forEach(function([detailLabel, value]) {\n        details.append(createRateLimitChip(String(detailLabel), value));\n      });\n      const forecast = document.createElement(\"div\");\n      forecast.className = \"rate-limit-forecast\";\n      forecast.append(\n        createRateLimitMarker(\"Schedule\", projection.status, projection.status === \"deficit\" ? \"deficit\" : \"positive\"),\n        createRateLimitMarker(\"Balance\", projection.surplusLabel, projection.surplusLabel.startsWith(\"Deficit\") ? \"deficit\" : \"positive\"),\n        createRateLimitMarker(\"Projection\", projection.runwayLabel, projection.detail.includes(\"runs out\") ? \"deficit\" : \"\")\n      );\n      const caption = document.createElement(\"p\");\n      caption.className = \"rate-limit-caption\";\n      caption.textContent = projection.detail;\n      card.append(title, meter, meterLegend, metrics);\n      if (details.children.length) {\n        card.append(details);\n      }\n      card.append(forecast, caption);\n      return card;\n    });\n    elements.rateLimits.replaceChildren(summary, ...cards);\n  }\n  function createMetricCard(label, value) {\n    const card = document.createElement(\"article\");\n    card.className = \"kpi-card\";\n    const title = document.createElement(\"h3\");\n    title.textContent = label;\n    const number = document.createElement(\"p\");\n    number.textContent = value;\n    card.append(title, number);\n    return card;\n  }\n  function computeDisplayRuntimeSeconds(payload) {\n    if (!payload || !payload.codex_totals) {\n      return 0;\n    }\n    const base = Number(payload.codex_totals.seconds_running) || 0;\n    const generatedAtMs = Date.parse(payload.generated_at || \"\");\n    if (!Number.isFinite(generatedAtMs)) {\n      return base;\n    }\n    const elapsed = Math.max(0, Math.floor((Date.now() - generatedAtMs) / 1e3));\n    return base + elapsed;\n  }\n  function renderOverview(payload) {\n    const splitUnavailable = payload.codex_totals && payload.codex_totals.token_split_status === \"aggregate_only\";\n    const quiescence = payload.quiescence || { safe_to_shutdown: true, blocker_counts: {}, blockers: [] };\n    const drainBlockerCount = Object.values(quiescence.blocker_counts || {}).reduce(function(total, value) {\n      return total + (Number(value) || 0);\n    }, 0);\n    const warningCount = Array.isArray(quiescence.warnings) ? quiescence.warnings.reduce(function(total, warning) {\n      return total + (Number(warning && warning.count) || 0);\n    }, 0) : 0;\n    elements.kpiGrid.replaceChildren(\n      createMetricCard(\"Safe To Shutdown\", quiescence.safe_to_shutdown ? \"Yes\" : \"No\"),\n      createMetricCard(\"Shutdown Blockers\", formatNumber(drainBlockerCount)),\n      createMetricCard(\"Restart Warnings\", formatNumber(warningCount)),\n      createMetricCard(\"Running\", formatNumber(payload.counts.running)),\n      createMetricCard(\"Retrying\", formatNumber(payload.counts.retrying)),\n      createMetricCard(\"Blocked\", formatNumber(payload.counts.blocked)),\n      createMetricCard(\"Stopped\", formatNumber(payload.counts.stopped || 0)),\n      createMetricCard(\"Stalled Waiting\", formatNumber(payload.counts.running_stalled_waiting_count || 0)),\n      createMetricCard(\"Awaiting Input\", formatNumber(payload.counts.running_awaiting_input_count || 0)),\n      createMetricCard(\"Total Tokens\", formatOverviewTokenValue(payload, \"total_tokens\", splitUnavailable)),\n      createMetricCard(\"Input Tokens\", formatOverviewTokenValue(payload, \"input_tokens\", splitUnavailable)),\n      createMetricCard(\"Output Tokens\", formatOverviewTokenValue(payload, \"output_tokens\", splitUnavailable)),\n      createMetricCard(\"Cached Input Tokens\", formatOverviewTokenValue(payload, \"cached_input_tokens\", splitUnavailable)),\n      createMetricCard(\"Reasoning Output Tokens\", formatOverviewTokenValue(payload, \"reasoning_output_tokens\", splitUnavailable)),\n      createMetricCard(\"Max Context Window\", formatOverviewTokenValue(payload, \"model_context_window\", splitUnavailable)),\n      createMetricCard(\"Runtime Seconds\", formatNumber(computeDisplayRuntimeSeconds(payload)))\n    );\n    const failed = payload.health.dispatch_validation === \"failed\";\n    elements.healthMessage.className = failed ? \"health health-failed\" : \"health health-ok\";\n    const drainMode = payload.drain_mode || { active: false };\n    const drainStatus = drainMode.active ? \"Drain Mode: active, \" + (quiescence.safe_to_shutdown ? \"restart safe\" : \"restart blocked\") : \"Drain Mode: inactive\";\n    elements.healthMessage.textContent = \"Dispatch validation: \" + payload.health.dispatch_validation + \" \\u2022 \" + drainStatus;\n    const blockerDetail = quiescence.blockers && quiescence.blockers.length ? quiescence.blockers.map(function(blocker) {\n      return blocker.detail;\n    }).join(\" \\u2022 \") : \"\";\n    elements.lastError.textContent = [\n      payload.health.last_error ? \"Last error: \" + payload.health.last_error : \"\",\n      blockerDetail ? \"Shutdown blockers: \" + blockerDetail : \"\"\n    ].filter(Boolean).join(\" \\u2022 \");\n    renderRuntimeIdentityWarning(payload.runtime_identity || null);\n    renderRuntimeUpdate(payload.runtime_update || null, payload);\n    renderDrainModeWorkflow(payload);\n    renderRetryStatusSummary(payload);\n    const rateLimits = payload.rate_limits;\n    renderRateLimits(rateLimits);\n  }\n  function formatRuntimeUpdateLabel(value) {\n    return String(value || \"unknown\").replace(/_/g, \" \");\n  }\n  function isActionableRuntimeUpdate(readiness) {\n    return !!readiness && [\n      \"local_checkout_behind\",\n      \"remote_update_available\",\n      \"runtime_stale\",\n      \"source_changed_build_not_updated\"\n    ].includes(readiness.state) && isGithubRuntimeUpdateEligible(readiness.github_eligibility) && !(readiness.refusal_reasons && readiness.refusal_reasons.length > 0);\n  }\n  function isGithubRuntimeUpdateEligible(eligibility) {\n    return !!eligibility && [\n      \"github_verified\",\n      \"github_checks_absent_allowed\",\n      \"github_trusted_raw_git\"\n    ].includes(eligibility.state);\n  }\n  function updateRuntimeUpdateButtons(readiness, payload) {\n    const quiescence = payload && payload.quiescence ? payload.quiescence : { safe_to_shutdown: false };\n    const drainMode = payload && payload.drain_mode ? payload.drain_mode : { active: false };\n    const restart = payload && payload.runtime_restart ? payload.runtime_restart : null;\n    const restartPhase = restart && restart.phase;\n    const actionable = isActionableRuntimeUpdate(readiness);\n    const prepared = readiness && readiness.prepared === true;\n    const applyReady = readiness && readiness.apply_ready === true;\n    const prepareDisabled = !actionable || prepared;\n    const applyDisabled = !actionable || !applyReady || !drainMode.active || !quiescence.safe_to_shutdown || restartPhase === \"restarting\";\n    [\n      elements.runtimeUpdatePrepareButton,\n      elements.runtimeUpdatePreparePanelButton\n    ].filter(Boolean).forEach(function(button) {\n      button.disabled = prepareDisabled;\n    });\n    [\n      elements.runtimeUpdateApplyButton,\n      elements.runtimeUpdateApplyPanelButton\n    ].filter(Boolean).forEach(function(button) {\n      button.disabled = applyDisabled;\n    });\n  }\n  function shortRuntimeUpdateSha(value) {\n    const text = value ? String(value) : \"\";\n    return text ? text.slice(0, 12) : \"unknown\";\n  }\n  function describeRuntimeUpdateState(readiness) {\n    if (readiness && readiness.apply_ready === true) {\n      return \"Prepared update ready\";\n    }\n    if (readiness && readiness.prepared === true) {\n      return \"Update prepared\";\n    }\n    switch (readiness && readiness.state) {\n      case \"local_checkout_behind\":\n      case \"remote_update_available\":\n        return \"Update ready to prepare\";\n      case \"runtime_stale\":\n        return \"Restart required\";\n      case \"source_changed_build_not_updated\":\n        return \"New build available\";\n      case \"build_current\":\n        return \"Running the current build\";\n      default:\n        return readiness ? formatRuntimeUpdateLabel(readiness.state) : \"Runtime update unavailable\";\n    }\n  }\n  function formatRuntimeUpdateCommitCount(value) {\n    if (value === null || value === void 0) {\n      return \"an unknown number of commits\";\n    }\n    const count = Number(value);\n    if (!Number.isFinite(count)) {\n      return \"an unknown number of commits\";\n    }\n    return count + \" \" + (count === 1 ? \"commit\" : \"commits\");\n  }\n  function describeRuntimeUpdateAction(readiness, payload) {\n    if (!readiness) {\n      return \"The local runtime update detector is not configured.\";\n    }\n    const drainMode = payload && payload.drain_mode ? payload.drain_mode : { active: false };\n    const quiescence = payload && payload.quiescence ? payload.quiescence : { safe_to_shutdown: false };\n    const restart = payload && payload.runtime_restart ? payload.runtime_restart : null;\n    const restartCapability = restart && restart.capability ? restart.capability : null;\n    const github = readiness.github_eligibility || {};\n    const refusalReasons = Array.isArray(readiness.refusal_reasons) ? readiness.refusal_reasons : [];\n    if (!readiness.attention_required) {\n      return \"No operator action is needed. Symphony is already running from the expected checkout.\";\n    }\n    if (!isGithubRuntimeUpdateEligible(github)) {\n      return \"Wait for GitHub checks to finish before preparing this update.\";\n    }\n    if (refusalReasons.length) {\n      return \"Update is paused until this blocker clears: \" + refusalReasons.map(formatRuntimeUpdateLabel).join(\", \") + \".\";\n    }\n    if (readiness.state === \"runtime_stale\") {\n      return restartCapability && restartCapability.mode === \"supervisor_available\" ? \"Restart Symphony from this panel once Drain Mode is quiet.\" : \"Restart Symphony manually after Drain Mode is quiet.\";\n    }\n    if (readiness.apply_ready === true) {\n      if (!drainMode.active) {\n        return \"Enter Drain Mode before applying the prepared update.\";\n      }\n      if (!quiescence.safe_to_shutdown) {\n        return \"Wait for active work to drain before applying the prepared update.\";\n      }\n      return \"Apply the prepared update now. Symphony will keep restart guidance visible after the pull.\";\n    }\n    if (readiness.prepared === true) {\n      return \"Preparation is complete. Keep Drain Mode active until the system is quiet, then apply the update.\";\n    }\n    return \"Prepare the update first. Symphony will enter Drain Mode and pin the candidate before anything is applied.\";\n  }\n  function describeRuntimeUpdateBanner(readiness, payload) {\n    const local = readiness.local_checkout || {};\n    const remote = readiness.fetched_remote || {};\n    const counts = readiness.ahead_behind || {};\n    const branch = local.branch || remote.base_ref || \"current branch\";\n    const behind = counts.behind === null || counts.behind === void 0 ? \"unknown\" : counts.behind;\n    const target = shortRuntimeUpdateSha(remote.commit_sha);\n    const restart = payload && payload.runtime_restart ? payload.runtime_restart : null;\n    const restartCapability = restart && restart.capability ? restart.capability : null;\n    const restartCopy = restartCapability && restartCapability.mode === \"supervisor_available\" ? \"supervised restart available\" : restartCapability ? \"manual restart may be required\" : \"restart guidance pending\";\n    if (readiness.state === \"runtime_stale\") {\n      return \"The checkout has moved, but this dashboard is still running the older build. \" + restartCopy + \".\";\n    }\n    return \"Remote \" + (remote.remote || \"origin\") + \"/\" + (remote.base_ref || branch) + \" has \" + formatRuntimeUpdateCommitCount(behind) + \" ready for \" + branch + \". Target \" + target + \".\";\n  }\n  function describeGithubEligibility(eligibility) {\n    if (!eligibility) {\n      return \"GitHub checks: not reported\";\n    }\n    const summary = eligibility.check_summary || {};\n    const stateLabel = formatRuntimeUpdateLabel(eligibility.state || \"unknown\");\n    if (eligibility.state === \"github_verified\") {\n      return \"GitHub checks passed\" + (Number.isFinite(summary.succeeded) ? \" (\" + summary.succeeded + \" succeeded)\" : \"\");\n    }\n    if (eligibility.state === \"github_checks_pending\") {\n      return \"GitHub checks are still running\" + (Number.isFinite(summary.pending) ? \" (\" + summary.pending + \" pending)\" : \"\");\n    }\n    if (eligibility.state === \"github_checks_absent_allowed\") {\n      return \"No GitHub checks were reported; configuration allows the update to continue.\";\n    }\n    if (eligibility.state === \"github_trusted_raw_git\") {\n      return \"GitHub checks are bypassed by trusted raw-git mode.\";\n    }\n    return \"GitHub checks: \" + stateLabel;\n  }\n  function runtimeUpdateFact(label, detail) {\n    const item = document.createElement(\"div\");\n    item.className = \"runtime-update-fact\";\n    const title = document.createElement(\"strong\");\n    title.textContent = label;\n    const value = document.createElement(\"span\");\n    value.textContent = detail;\n    item.append(title, value);\n    return item;\n  }\n  function renderRuntimeUpdateFacts(readiness, payload) {\n    if (!elements.runtimeUpdateDetails) {\n      return;\n    }\n    if (!readiness) {\n      elements.runtimeUpdateDetails.textContent = \"Runtime update details unavailable.\";\n      return;\n    }\n    const local = readiness.local_checkout || {};\n    const remote = readiness.fetched_remote || {};\n    const counts = readiness.ahead_behind || {};\n    const fetch2 = readiness.last_fetch || {};\n    const restart = payload && payload.runtime_restart ? payload.runtime_restart : null;\n    const restartCapability = restart && restart.capability ? restart.capability : null;\n    const facts = [\n      runtimeUpdateFact(\"Current checkout\", (local.branch || \"unknown branch\") + \" @ \" + shortRuntimeUpdateSha(local.commit_sha)),\n      runtimeUpdateFact(\"Available build\", (remote.remote || \"origin\") + \"/\" + (remote.base_ref || \"main\") + \" @ \" + shortRuntimeUpdateSha(remote.commit_sha)),\n      runtimeUpdateFact(\"Distance\", (counts.behind === null || counts.behind === void 0 ? \"unknown\" : counts.behind) + \" behind, \" + (counts.ahead === null || counts.ahead === void 0 ? \"unknown\" : counts.ahead) + \" ahead\"),\n      runtimeUpdateFact(\"Checks\", describeGithubEligibility(readiness.github_eligibility)),\n      runtimeUpdateFact(\"Drain requirement\", readiness.drain_required ? \"Drain Mode required before apply\" : \"Drain Mode not required\"),\n      runtimeUpdateFact(\"Restart path\", restartCapability ? formatRuntimeUpdateLabel(restartCapability.mode) : \"Restart guidance pending\")\n    ];\n    if (fetch2.result) {\n      facts.push(runtimeUpdateFact(\"Last fetch\", formatRuntimeUpdateLabel(fetch2.result)));\n    }\n    if (readiness.prepared_update && readiness.prepared_update.candidate_sha) {\n      facts.push(runtimeUpdateFact(\"Prepared candidate\", shortRuntimeUpdateSha(readiness.prepared_update.candidate_sha)));\n    }\n    if (restart && restart.recommended_manual_recovery) {\n      facts.push(runtimeUpdateFact(\"Manual recovery\", restart.recommended_manual_recovery));\n    }\n    if (restart && restart.last_error && restart.last_error.message) {\n      facts.push(runtimeUpdateFact(\"Last restart error\", restart.last_error.message));\n    }\n    elements.runtimeUpdateDetails.replaceChildren(...facts);\n  }\n  function renderRuntimeUpdate(readiness, payload) {\n    if (!elements.runtimeUpdatePanel || !elements.runtimeUpdateState || !elements.runtimeUpdateDetails) {\n      return;\n    }\n    updateRuntimeUpdateButtons(readiness, payload);\n    if (!readiness) {\n      if (elements.runtimeUpdateBanner) {\n        elements.runtimeUpdateBanner.classList.add(\"hidden\");\n      }\n      elements.runtimeUpdateState.textContent = \"Runtime update readiness unavailable\";\n      elements.runtimeUpdateRecommendation.textContent = \"The local runtime update detector is not configured.\";\n      elements.runtimeUpdateDetails.textContent = \"Runtime update details unavailable.\";\n      return;\n    }\n    if (readiness.attention_required) {\n      elements.runtimeUpdateBanner.classList.remove(\"hidden\");\n      elements.runtimeUpdateTitle.textContent = readiness.state === \"runtime_stale\" ? \"Runtime restart required\" : \"Runtime update available\";\n      elements.runtimeUpdateSummary.textContent = describeRuntimeUpdateBanner(readiness, payload);\n    } else {\n      elements.runtimeUpdateBanner.classList.add(\"hidden\");\n      elements.runtimeUpdateSummary.textContent = \"\";\n    }\n    elements.runtimeUpdateState.textContent = describeRuntimeUpdateState(readiness);\n    elements.runtimeUpdateRecommendation.textContent = describeRuntimeUpdateAction(readiness, payload);\n    renderRuntimeUpdateFacts(readiness, payload);\n  }\n  function renderDrainModeWorkflow(payload) {\n    if (!elements.drainModeSummary || !elements.drainModeBoundary || !elements.drainBlockersList) {\n      return;\n    }\n    const drainMode = payload.drain_mode || { active: false };\n    const quiescence = payload.quiescence || { safe_to_shutdown: true, blocker_counts: {}, blockers: [] };\n    const active = !!drainMode.active;\n    const safeToShutdown = !!quiescence.safe_to_shutdown;\n    const blockers = Array.isArray(quiescence.blockers) ? quiescence.blockers : [];\n    const warnings = Array.isArray(quiescence.warnings) ? quiescence.warnings : [];\n    const restartGuidance = quiescence.restart_guidance || null;\n    const blockerDetails = blockers.map(function(blocker) {\n      return blocker && blocker.detail ? blocker.detail : \"\";\n    }).filter(Boolean);\n    const totalBlockers = DRAIN_BLOCKER_ORDER.reduce(function(total, category) {\n      return total + (Number(quiescence.blocker_counts && quiescence.blocker_counts[category]) || 0);\n    }, 0);\n    elements.drainModeSummary.textContent = active ? \"Drain Mode active\" + (drainMode.reason ? \": \" + drainMode.reason : \"\") : \"Drain Mode inactive\";\n    elements.drainModeBoundary.className = safeToShutdown ? \"drain-boundary drain-boundary-safe\" : \"drain-boundary drain-boundary-blocked\";\n    if (safeToShutdown) {\n      if (restartGuidance && restartGuidance.recommended_action === \"restart_runtime_to_current_build\") {\n        elements.drainModeBoundary.textContent = \"Shutdown/restart is safe: restart/update Symphony before dispatching pending normal work.\";\n      } else {\n        elements.drainModeBoundary.textContent = active ? \"Shutdown/restart is safe: no true shutdown blockers are present.\" : \"Restart safety is clear, but Drain Mode is not active.\";\n      }\n    } else {\n      elements.drainModeBoundary.textContent = \"Restart is not safe yet: \" + (blockerDetails.length ? blockerDetails.join(\" \\u2022 \") : totalBlockers + \" quiescence blockers remain.\");\n    }\n    const warning = payload.runtime_identity && payload.runtime_identity.health_warning;\n    const metaParts = [];\n    if (drainMode.entered_at) {\n      metaParts.push(\"Entered \" + formatDate(drainMode.entered_at));\n    }\n    if (drainMode.updated_at) {\n      metaParts.push(\"Updated \" + formatDate(drainMode.updated_at));\n    }\n    if (warning && warning.code === \"stale_runtime_build\") {\n      metaParts.push(\"Dispatch is unsafe because this runtime is stale; this is a restart warning, not an active worker count.\");\n    } else if (warning && warning.code === \"unknown_current_build_identity\") {\n      metaParts.push(\"Dispatch is unsafe because current build identity is unknown; this is a restart warning, not an active worker count.\");\n    } else {\n      metaParts.push(active ? \"New dispatch is stopped while Drain Mode is active.\" : \"Use Drain Mode before planned runtime restarts.\");\n    }\n    elements.drainModeMeta.textContent = metaParts.join(\" \\u2022 \");\n    const countByCategory = quiescence.blocker_counts || {};\n    const detailByCategory = blockers.reduce(function(acc, blocker) {\n      if (blocker && blocker.category && blocker.detail) {\n        acc[blocker.category] = blocker.detail;\n      }\n      return acc;\n    }, {});\n    const blockerNodes = DRAIN_BLOCKER_ORDER.map(function(category) {\n      const count = Number(countByCategory[category]) || 0;\n      const item = document.createElement(\"div\");\n      item.className = \"drain-blocker-item \" + (count > 0 ? \"drain-blocker-active\" : \"drain-blocker-clear\");\n      const label = document.createElement(\"strong\");\n      label.textContent = DRAIN_BLOCKER_LABELS[category] + \" \" + formatNumber(count);\n      const detail = document.createElement(\"span\");\n      detail.textContent = count > 0 ? detailByCategory[category] || \"Blocking safe restart\" : \"Clear\";\n      item.append(label, detail);\n      return item;\n    });\n    const warningNodes = warnings.map(function(warning2) {\n      const item = document.createElement(\"div\");\n      item.className = \"drain-blocker-item drain-blocker-warning\";\n      const label = document.createElement(\"strong\");\n      label.textContent = (DRAIN_WARNING_LABELS[warning2.category] || String(warning2.category || \"Warning\")) + \" \" + formatNumber(Number(warning2.count) || 0);\n      const detail = document.createElement(\"span\");\n      detail.textContent = [\n        warning2.detail || \"Warning present\",\n        warning2.recommended_action ? \"Action: \" + warning2.recommended_action : \"\"\n      ].filter(Boolean).join(\" \");\n      item.append(label, detail);\n      return item;\n    });\n    const pendingWork = restartGuidance && Array.isArray(restartGuidance.pending_work) ? restartGuidance.pending_work : [];\n    const pendingWorkNodes = pendingWork.map(function(entry) {\n      const item = document.createElement(\"div\");\n      item.className = \"drain-blocker-item drain-blocker-pending-work\";\n      const label = document.createElement(\"strong\");\n      label.textContent = \"Pending work \" + formatNumber(Number(entry && entry.count) || 0);\n      const detail = document.createElement(\"span\");\n      detail.textContent = formatPendingWorkDetail(entry);\n      item.append(label, detail);\n      return item;\n    });\n    const nodes = [...blockerNodes, ...warningNodes, ...pendingWorkNodes];\n    elements.drainBlockersList.replaceChildren(...nodes);\n    if (elements.drainEnterButton) {\n      elements.drainEnterButton.disabled = active;\n    }\n    if (elements.drainExitButton) {\n      elements.drainExitButton.disabled = !active || !safeToShutdown;\n    }\n    if (elements.drainWaitButton) {\n      elements.drainWaitButton.disabled = !active || safeToShutdown;\n    }\n    if (elements.drainShutdownButton) {\n      elements.drainShutdownButton.disabled = !active || !safeToShutdown;\n    }\n  }\n  function renderRuntimeIdentityWarning(runtimeIdentity) {\n    if (!elements.runtimeStaleBanner || !elements.runtimeStaleTitle || !elements.runtimeStaleSummary) {\n      return;\n    }\n    const warning = runtimeIdentity && runtimeIdentity.health_warning;\n    if (!warning) {\n      elements.runtimeStaleBanner.classList.add(\"hidden\");\n      elements.runtimeStaleTitle.textContent = \"Runtime Build Warning\";\n      elements.runtimeStaleSummary.textContent = \"\";\n      return;\n    }\n    elements.runtimeStaleBanner.classList.remove(\"hidden\");\n    elements.runtimeStaleTitle.textContent = warning.code === \"unknown_current_build_identity\" ? \"Runtime build identity unknown\" : \"Runtime build is stale\";\n    const running = runtimeIdentity.running_build || {};\n    const current = runtimeIdentity.current_build || {};\n    const processStarted = runtimeIdentity.process_started_at || runtimeIdentity.process_started_at_ms || null;\n    const currentLabel = current.identity || current.commit_sha || \"unknown\";\n    elements.runtimeStaleSummary.textContent = [\n      warning.message,\n      \"Running build \" + (running.identity || running.commit_sha || \"unknown\"),\n      currentLabel === \"unknown\" ? \"Current build unknown\" : \"Current build \" + currentLabel,\n      \"Process started \" + formatDate(processStarted),\n      warning.recommended_action\n    ].join(\" \\u2022 \");\n  }\n  function renderRetryStatusSummary(payload) {\n    const entries = payload.retry_status && Array.isArray(payload.retry_status.entries) ? payload.retry_status.entries : Array.isArray(payload.retrying) ? payload.retrying.map(function(entry) {\n      const cause = entry.retry_cause || {};\n      return {\n        issue_identifier: entry.issue_identifier,\n        attempt: entry.attempt,\n        due_at: entry.due_at,\n        due_state: entry.due_state || \"pending\",\n        overdue_ms: entry.overdue_ms || null,\n        retry_wait_ms: entry.retry_wait_ms || null,\n        reason_code: cause.reason_code || entry.stop_reason_code || null,\n        detail: cause.detail || entry.stop_reason_detail || entry.error || null,\n        operator_detail: cause.operator_detail || null,\n        headline: cause.headline || entry.operator_explainer_hint && entry.operator_explainer_hint.headline || \"Run is waiting to retry\",\n        expected_transition: cause.expected_transition || null,\n        last_phase: cause.last_phase || entry.last_phase || null\n      };\n    }) : [];\n    if (!entries.length) {\n      elements.retryStatusSummary.classList.add(\"hidden\");\n      elements.retryStatusSummary.replaceChildren();\n      return;\n    }\n    const header = document.createElement(\"div\");\n    header.className = \"retry-status-header\";\n    const overdueCount = entries.filter(function(entry) {\n      return entry.due_state === \"overdue\";\n    }).length;\n    header.textContent = overdueCount > 0 ? overdueCount + \" overdue \" + (overdueCount === 1 ? \"retry needs\" : \"retries need\") + \" attention\" : entries.length + \" retry\" + (entries.length === 1 ? \"\" : \"ies\") + \" scheduled\";\n    const list = document.createElement(\"div\");\n    list.className = \"retry-status-list\";\n    entries.slice(0, 4).forEach(function(entry) {\n      const item = document.createElement(\"div\");\n      item.className = \"retry-status-item \" + (entry.due_state === \"overdue\" ? \"overdue\" : \"pending\");\n      const title = document.createElement(\"div\");\n      title.className = \"retry-status-title\";\n      const issue = document.createElement(\"strong\");\n      issue.textContent = entry.issue_identifier || \"unknown issue\";\n      const statePill = document.createElement(\"span\");\n      statePill.className = \"status-pill \" + (entry.due_state === \"overdue\" ? \"failed\" : \"pending\");\n      statePill.textContent = entry.due_state === \"overdue\" ? \"Overdue \" + formatElapsedMs(entry.overdue_ms || 0) : \"Retry due \" + formatDate(entry.due_at);\n      title.append(issue, statePill);\n      const reason = document.createElement(\"div\");\n      reason.className = \"retry-status-reason\";\n      reason.textContent = (entry.reason_code || \"unknown_reason\") + \" - \" + (entry.operator_detail || entry.detail || entry.headline || \"No retry detail available\");\n      const meta = document.createElement(\"div\");\n      meta.className = \"muted\";\n      const parts = [];\n      if (entry.last_phase) {\n        parts.push(\"Last phase: \" + entry.last_phase);\n      }\n      if (entry.detail && entry.detail !== entry.operator_detail) {\n        parts.push(entry.detail);\n      }\n      if (entry.expected_transition) {\n        parts.push(entry.expected_transition);\n      }\n      meta.textContent = parts.join(\" \\u2022 \");\n      item.append(title, reason, meta);\n      list.append(item);\n    });\n    elements.retryStatusSummary.classList.remove(\"hidden\");\n    elements.retryStatusSummary.replaceChildren(header, list);\n  }\n  function renderActionRequiredBanner(payload) {\n    const blockedEntries = Array.isArray(payload && payload.blocked) ? payload.blocked : [];\n    const grouped = blockedEntries.reduce(function(acc, entry) {\n      if (!isActionRequiredCode(entry.stop_reason_code)) {\n        return acc;\n      }\n      acc[entry.stop_reason_code] = (acc[entry.stop_reason_code] || 0) + 1;\n      return acc;\n    }, {});\n    const groupedEntries = Object.entries(grouped);\n    if (!groupedEntries.length) {\n      elements.actionRequiredBanner.classList.add(\"hidden\");\n      elements.actionRequiredSummary.textContent = \"\";\n      elements.actionRequiredGroups.replaceChildren();\n      return;\n    }\n    const total = groupedEntries.reduce(function(sum, entry) {\n      const count = entry[1];\n      return sum + count;\n    }, 0);\n    elements.actionRequiredBanner.classList.remove(\"hidden\");\n    elements.actionRequiredSummary.textContent = total + \" blocked run\" + (total === 1 ? \"\" : \"s\") + \" need operator action.\";\n    const groupNodes = groupedEntries.map(function(entry) {\n      const code = entry[0];\n      const count = entry[1];\n      const button = document.createElement(\"button\");\n      button.type = \"button\";\n      button.className = \"ghost-button reason-chip\";\n      button.textContent = getActionRequiredLabel(code) + \" (\" + count + \")\";\n      button.title = \"Filter blocked rows for \" + getActionRequiredLabel(code);\n      button.addEventListener(\"click\", function() {\n        state.filter.status = \"blocked\";\n        state.filter.blockedReason = code;\n        elements.statusFilter.value = \"blocked\";\n        if (state.payload) {\n          renderRunning(state.payload);\n          renderRetry(state.payload);\n          renderBlocked(state.payload);\n        }\n      });\n      return button;\n    });\n    elements.actionRequiredGroups.replaceChildren(...groupNodes);\n  }\n  function renderApiDegradedBanner(payload) {\n    if (!payload || !payload.api_degraded_mode) {\n      elements.apiDegradedBanner.classList.add(\"hidden\");\n      elements.apiDegradedSummary.textContent = \"\";\n      return;\n    }\n    const routes = Array.isArray(payload.api_degraded_routes) ? payload.api_degraded_routes.join(\", \") : \"n/a\";\n    elements.apiDegradedBanner.classList.remove(\"hidden\");\n    elements.apiDegradedSummary.textContent = (payload.api_degraded_reason_code || \"unknown\") + \" \\u2022 fallback routes: \" + routes;\n  }\n  function describeTransition(transition) {\n    switch (transition) {\n      case \"completion_gate_blocked\":\n        return { label: \"Completion Gate Blocked\", result: \"failure\", detail: \"No progress signal detected in redispatch window.\" };\n      case \"circuit_breaker_opened\":\n        return { label: \"Circuit Breaker Opened\", result: \"failure\", detail: \"Respawn threshold reached; operator intervention required.\" };\n      case \"resume_accepted\":\n        return { label: \"Resume Accepted\", result: \"success\", detail: \"Resume request accepted and redispatch restarted.\" };\n      case \"resume_rejected\":\n        return { label: \"Resume Rejected\", result: \"failure\", detail: \"Resume request rejected; resolve blocking condition first.\" };\n      case \"cancel_accepted\":\n        return { label: \"Cancel Accepted\", result: \"success\", detail: \"Issue returned to backlog.\" };\n      case \"cancel_rejected\":\n        return { label: \"Cancel Rejected\", result: \"failure\", detail: \"Cancel request rejected; tracker state unchanged.\" };\n      default:\n        return null;\n    }\n  }\n  function deriveOperatorTransitionRows(issueId, payload) {\n    const rows = [];\n    const seen = /* @__PURE__ */ new Set();\n    function addRow(at, transition, detail) {\n      const key = transition + \":\" + String(at || \"n/a\") + \":\" + String(detail || \"\");\n      if (seen.has(key)) {\n        return;\n      }\n      seen.add(key);\n      const descriptor = describeTransition(transition);\n      if (!descriptor) {\n        return;\n      }\n      rows.push({\n        at: at || \"n/a\",\n        issue_identifier: issueId,\n        label: descriptor.label,\n        result: descriptor.result,\n        detail: detail && detail.trim ? detail.trim() ? detail : descriptor.detail : descriptor.detail\n      });\n    }\n    const timeline = Array.isArray(payload.phase_timeline) ? payload.phase_timeline : [];\n    for (const marker of timeline) {\n      const normalized = String(marker && marker.detail ? marker.detail : \"\").trim().toLowerCase();\n      const transition = OPERATOR_TRANSITION_RULES.detailMap[normalized];\n      if (transition) {\n        addRow(marker.at, transition, marker.detail || null);\n      }\n    }\n    const events = Array.isArray(payload.recent_events) ? payload.recent_events : [];\n    for (const entry of events) {\n      const transitionByEvent = OPERATOR_TRANSITION_RULES.eventMap[String(entry && entry.event ? entry.event : \"\")];\n      if (transitionByEvent) {\n        addRow(entry.at, transitionByEvent, entry.message || null);\n      }\n      const normalizedMessage = String(entry && entry.message ? entry.message : \"\").trim().toLowerCase();\n      const transitionByMessage = OPERATOR_TRANSITION_RULES.detailMap[normalizedMessage];\n      if (transitionByMessage) {\n        addRow(entry.at, transitionByMessage, entry.message || null);\n      }\n    }\n    if (payload.blocked && (payload.blocked.stop_reason_code === \"${REASON_CODES.operatorNoProgressRedispatchBlocked}\" || payload.blocked.stop_reason_code === \"${REASON_CODES.awaitingHumanReviewScopeIncomplete}\")) {\n      addRow(\"n/a\", \"completion_gate_blocked\", payload.blocked.stop_reason_detail || null);\n    }\n    return rows.sort(function(a, b) {\n      const atA = Date.parse(a.at);\n      const atB = Date.parse(b.at);\n      if (Number.isFinite(atA) && Number.isFinite(atB)) {\n        return atA - atB;\n      }\n      if (Number.isFinite(atA)) {\n        return -1;\n      }\n      if (Number.isFinite(atB)) {\n        return 1;\n      }\n      return a.label.localeCompare(b.label);\n    });\n  }\n\n  // src/api/dashboard-client/stopped-runs.ts\n  function recoveryStatusLabel(status) {\n    switch (status) {\n      case \"resume_available\":\n        return \"Resume available\";\n      case \"active_issue_present\":\n        return \"Active issue present\";\n      case \"capability_mismatch\":\n        return \"Capability mismatch\";\n      case \"resume_unavailable\":\n        return \"Resume unavailable\";\n      case \"inspect_forensics\":\n      default:\n        return \"Inspect forensics\";\n    }\n  }\n  function renderStoppedRunRecovery(payload) {\n    if (!state.stoppedRunRecoveryLoaded) {\n      const empty = document.createElement(\"p\");\n      empty.className = \"muted\";\n      empty.textContent = \"Stopped-run recovery detail loads on demand.\";\n      elements.stoppedRunRecoveryList.replaceChildren(empty);\n      return;\n    }\n    const acknowledged = new Set(JSON.parse(window.localStorage.getItem(\"symphony.stoppedRunAcknowledged\") || \"[]\"));\n    const entries = (payload.stopped_runs || []).filter(function(entry) {\n      return !acknowledged.has(entry.run_id);\n    });\n    if (!entries.length) {\n      const empty = document.createElement(\"p\");\n      empty.className = \"muted\";\n      empty.textContent = \"No recent stopped runs need recovery.\";\n      elements.stoppedRunRecoveryList.replaceChildren(empty);\n      return;\n    }\n    const cards = entries.map((entry) => {\n      const card = document.createElement(\"article\");\n      card.className = \"recovery-card\" + (entry.capability_mismatch ? \" recovery-card-warning\" : \"\");\n      const header = document.createElement(\"div\");\n      header.className = \"recovery-card-head\";\n      const title = document.createElement(\"h3\");\n      title.textContent = entry.issue_identifier;\n      const status = document.createElement(\"span\");\n      status.className = \"status-pill \" + (entry.resume_valid ? \"pending\" : entry.capability_mismatch ? \"failed\" : \"actionability-recommended\");\n      status.textContent = recoveryStatusLabel(entry.recovery_status);\n      header.append(title, status);\n      const meta = document.createElement(\"dl\");\n      meta.className = \"recovery-grid\";\n      const addMeta = function(label, value) {\n        const group = document.createElement(\"div\");\n        const term = document.createElement(\"dt\");\n        term.textContent = label;\n        const description = document.createElement(\"dd\");\n        description.textContent = value || \"n/a\";\n        group.append(term, description);\n        meta.append(group);\n      };\n      addMeta(\"Run\", entry.run_id);\n      addMeta(\"Thread\", entry.thread_id);\n      addMeta(\"Session\", entry.session_id);\n      addMeta(\"Turn\", entry.turn_id);\n      addMeta(\"Last relevant\", formatDate(entry.last_relevant_at));\n      addMeta(\"Terminal\", entry.terminal_status + (entry.terminal_reason_code ? \" / \" + entry.terminal_reason_code : \"\"));\n      addMeta(\"Root cause\", (entry.root_cause_status || \"unknown\") + (entry.root_cause_reason_code ? \" / \" + getActionRequiredLabel(entry.root_cause_reason_code) : \"\"));\n      addMeta(\"Current recovery\", recoveryStatusLabel(entry.recovery_status));\n      const detail = document.createElement(\"p\");\n      detail.className = \"muted\";\n      detail.textContent = \"Terminal detail: \" + (entry.terminal_reason_detail || \"n/a\") + \" | Root-cause detail: \" + (entry.root_cause_reason_detail || \"n/a\");\n      const capability = document.createElement(\"p\");\n      capability.className = entry.capability_mismatch ? \"capability-warning-text\" : \"muted\";\n      capability.textContent = entry.capability_mismatch && entry.capability_warning ? \"Resume disabled: \" + entry.capability_warning.unsupported_capability_message + \" Recovery: \" + entry.capability_warning.recommended_recovery_action : entry.resume_disabled_reason || \"Resume is available from API recovery metadata.\";\n      const actions = document.createElement(\"div\");\n      actions.className = \"action-cell recovery-actions\";\n      const inspect = createActionButton(\"Inspect Forensics\", \"ghost-button\", function() {\n        window.open(entry.actions.inspect_forensics_url, \"_blank\", \"noopener\");\n      });\n      const inspectThread = createActionButton(\"Inspect Thread\", \"ghost-button\", function() {\n        if (entry.actions.inspect_thread_url) {\n          window.open(entry.actions.inspect_thread_url, \"_blank\", \"noopener\");\n        }\n      });\n      inspectThread.disabled = !entry.actions.inspect_thread_url;\n      const copyThread = createActionButton(\"Copy Thread\", \"ghost-button\", function() {\n        copyText(entry.thread_id || \"\");\n      });\n      copyThread.disabled = !entry.actions.copy_thread_id_supported;\n      const copySession = createActionButton(\"Copy Session\", \"ghost-button\", function() {\n        copyText(entry.session_id || \"\");\n      });\n      copySession.disabled = !entry.actions.copy_session_id_supported;\n      const resume = createActionButton(\"Resume\", \"ghost-button\", function() {\n        if (entry.resume_valid) {\n          void resumeBlockedIssue(entry.issue_identifier);\n        }\n      });\n      resume.disabled = !entry.resume_valid;\n      const acknowledge = createActionButton(\"Acknowledge Cancellation\", \"ghost-button\", function() {\n        acknowledged.add(entry.run_id);\n        window.localStorage.setItem(\"symphony.stoppedRunAcknowledged\", JSON.stringify(Array.from(acknowledged)));\n        renderStoppedRunRecovery(payload);\n      });\n      actions.append(inspect, inspectThread, copyThread, copySession, resume, acknowledge);\n      card.append(header, meta, detail, capability, actions);\n      return card;\n    });\n    elements.stoppedRunRecoveryList.replaceChildren(...cards);\n  }\n  function normalizeStoppedRunRecoveryPayload(recovery) {\n    return {\n      stopped_runs: recovery && Array.isArray(recovery.stopped_runs) ? recovery.stopped_runs : [],\n      counts: {\n        stopped: recovery && recovery.counts && typeof recovery.counts.stopped === \"number\" ? recovery.counts.stopped : 0\n      }\n    };\n  }\n  function mergeStoppedRunRecoveryPayload(payload, recoveryPayload) {\n    return {\n      ...payload,\n      stopped_runs: recoveryPayload.stopped_runs || [],\n      counts: {\n        ...payload.counts,\n        stopped: recoveryPayload.counts && typeof recoveryPayload.counts.stopped === \"number\" ? recoveryPayload.counts.stopped : 0\n      }\n    };\n  }\n  async function loadStoppedRunRecovery() {\n    if (state.stoppedRunRecoveryLoading) {\n      return;\n    }\n    state.stoppedRunRecoveryLoading = true;\n    if (elements.stoppedRunRecoveryLoad) {\n      elements.stoppedRunRecoveryLoad.disabled = true;\n    }\n    try {\n      const recovery = await fetchJson(\"/api/v1/stopped-runs/recovery\");\n      state.stoppedRunRecoveryLoaded = true;\n      state.stoppedRunRecoveryPayload = normalizeStoppedRunRecoveryPayload(recovery);\n      if (state.payload) {\n        state.payload = mergeStoppedRunRecoveryPayload(state.payload, state.stoppedRunRecoveryPayload);\n        state.lastGoodPayload = state.payload;\n        renderOverview(state.payload);\n        renderStoppedRunRecovery(state.payload);\n      } else {\n        renderStoppedRunRecovery(state.stoppedRunRecoveryPayload);\n      }\n      setRefreshStatus(\"Stopped-run recovery loaded\", false);\n    } catch (error) {\n      setRefreshStatus(\"Stopped-run recovery load failed: \" + String(error), true);\n    } finally {\n      state.stoppedRunRecoveryLoading = false;\n      if (elements.stoppedRunRecoveryLoad) {\n        elements.stoppedRunRecoveryLoad.disabled = false;\n      }\n    }\n  }\n\n  // src/api/dashboard-client/runtime.ts\n  function renderThroughput(payload) {\n    if (!payload || !payload.throughput) {\n      elements.throughputOutput.textContent = \"No throughput samples yet.\";\n      return;\n    }\n    const throughput = payload.throughput;\n    const sparkline = Array.isArray(throughput.sparkline_10m) ? throughput.sparkline_10m : [];\n    elements.throughputOutput.textContent = JSON.stringify(\n      {\n        current_tps: throughput.current_tps,\n        avg_tps_60s: throughput.avg_tps_60s,\n        sample_count: throughput.sample_count,\n        window_seconds: throughput.window_seconds,\n        sparkline_10m: sparkline\n      },\n      null,\n      2\n    );\n  }\n  function renderRuntimeResolution(runtimeResolution) {\n    if (!runtimeResolution) {\n      elements.runtimeResolutionOutput.textContent = \"Runtime resolution unavailable.\";\n      return;\n    }\n    elements.runtimeResolutionOutput.textContent = formatCanonicalJsonBlock(\"Runtime Resolution JSON\", runtimeResolution);\n  }\n  function renderRuntimeEvents(payload) {\n    const events = Array.isArray(payload && payload.recent_runtime_events) ? payload.recent_runtime_events : [];\n    const filtered = events.filter(function(entry) {\n      if (state.filter.eventFeedSeverity === \"all\") {\n        return true;\n      }\n      return entry && entry.severity === state.filter.eventFeedSeverity;\n    });\n    if (!filtered.length) {\n      const empty = document.createElement(\"li\");\n      empty.className = \"muted\";\n      empty.textContent = \"No runtime events.\";\n      elements.runtimeEventsList.replaceChildren(empty);\n      return;\n    }\n    const nodes = filtered.map(function(entry) {\n      const item = document.createElement(\"li\");\n      const title = document.createElement(\"strong\");\n      title.textContent = \"[\" + (entry.severity || \"info\") + \"] \" + (entry.event || \"unknown\");\n      const meta = document.createElement(\"span\");\n      const issue = entry.issue_identifier ? \" \\u2022 issue \" + entry.issue_identifier : \"\";\n      const session = entry.session_id ? \" \\u2022 session \" + entry.session_id : \"\";\n      const detail = entry.detail ? \" \\u2022 \" + entry.detail : \"\";\n      meta.textContent = formatDate(entry.at) + issue + session + detail;\n      item.append(title, meta);\n      return item;\n    });\n    elements.runtimeEventsList.replaceChildren(...nodes);\n  }\n  function renderSnapshotError(errorPayload) {\n    elements.snapshotErrorPanel.classList.remove(\"hidden\");\n    elements.snapshotErrorMessage.textContent = (errorPayload && errorPayload.code ? String(errorPayload.code) + \": \" : \"\") + (errorPayload && errorPayload.message ? String(errorPayload.message) : \"Snapshot unavailable.\");\n    if (!state.lastGoodPayload) {\n      elements.kpiGrid.replaceChildren();\n      elements.rateLimits.textContent = \"n/a\";\n      const emptyRunningRow = document.createElement(\"tr\");\n      const runningCell = document.createElement(\"td\");\n      runningCell.colSpan = 10;\n      runningCell.className = \"muted\";\n      runningCell.textContent = \"No running issues while snapshot is unavailable.\";\n      emptyRunningRow.appendChild(runningCell);\n      elements.runningRows.replaceChildren(emptyRunningRow);\n      const emptyRetryRow = document.createElement(\"tr\");\n      const retryCell = document.createElement(\"td\");\n      retryCell.colSpan = 10;\n      retryCell.className = \"muted\";\n      retryCell.textContent = \"No retry data while snapshot is unavailable.\";\n      emptyRetryRow.appendChild(retryCell);\n      elements.retryRows.replaceChildren(emptyRetryRow);\n      const emptyBlockedRow = document.createElement(\"tr\");\n      const blockedCell = document.createElement(\"td\");\n      blockedCell.colSpan = 8;\n      blockedCell.className = \"muted\";\n      blockedCell.textContent = \"No blocked-input data while snapshot is unavailable.\";\n      emptyBlockedRow.appendChild(blockedCell);\n      elements.blockedRows.replaceChildren(emptyBlockedRow);\n      const emptyStopped = document.createElement(\"p\");\n      emptyStopped.className = \"muted\";\n      emptyStopped.textContent = \"No stopped-run recovery data while snapshot is unavailable.\";\n      elements.stoppedRunRecoveryList.replaceChildren(emptyStopped);\n      return;\n    }\n    renderOverview(state.lastGoodPayload);\n    renderThroughput(state.lastGoodPayload);\n    renderRunning(state.lastGoodPayload);\n    renderRetry(state.lastGoodPayload);\n    renderBlocked(state.lastGoodPayload);\n    renderStoppedRunRecovery(state.lastGoodPayload);\n    renderRuntimeEvents(state.lastGoodPayload);\n  }\n  function clearSnapshotError() {\n    elements.snapshotErrorPanel.classList.add(\"hidden\");\n    elements.snapshotErrorMessage.textContent = \"Snapshot unavailable.\";\n  }\n\n  // src/api/dashboard-client/project-history.ts\n  function factLabel(value) {\n    return String(value || \"unknown\").replace(/_/g, \" \");\n  }\n  function factClass(status) {\n    switch (status) {\n      case \"present\":\n        return \"mini-badge mini-badge-good\";\n      case \"lifecycle_pending\":\n      case \"optional_unavailable\":\n      case \"missing\":\n        return \"mini-badge mini-badge-missing\";\n      case \"redacted\":\n      case \"truncated\":\n        return \"mini-badge mini-badge-warning\";\n      case \"degraded\":\n      case \"unavailable\":\n      default:\n        return \"mini-badge mini-badge-bad\";\n    }\n  }\n  function createFactBadge(fact) {\n    const badge = document.createElement(\"span\");\n    badge.className = factClass(fact && fact.status);\n    badge.textContent = factLabel(fact && fact.status) + \": \" + factLabel(fact && fact.fact);\n    if (fact && (fact.reason_code || fact.detail)) {\n      badge.title = [fact.reason_code, fact.detail].filter(Boolean).join(\" | \");\n    }\n    return badge;\n  }\n  function createFactBadges(facts) {\n    if (!Array.isArray(facts)) {\n      return [];\n    }\n    const badgesByLabel = /* @__PURE__ */ new Map();\n    for (const fact of facts) {\n      const badge = createFactBadge(fact);\n      const label = badge.textContent || \"\";\n      const existing = badgesByLabel.get(label);\n      if (!existing) {\n        badgesByLabel.set(label, badge);\n        continue;\n      }\n      if (badge.title && existing.title !== badge.title) {\n        existing.title = [existing.title, badge.title].filter(Boolean).join(\" | \");\n      }\n    }\n    return Array.from(badgesByLabel.values());\n  }\n  function createProjectHistoryEmptyRow(message) {\n    const row = document.createElement(\"tr\");\n    const cell = document.createElement(\"td\");\n    cell.colSpan = 9;\n    cell.className = \"muted\";\n    cell.textContent = message;\n    row.append(cell);\n    return row;\n  }\n  function projectKeyFromHistoryPayload(historyPayload) {\n    const runs = historyPayload && Array.isArray(historyPayload.runs) ? historyPayload.runs : [];\n    for (const run of runs) {\n      if (run && run.identity && run.identity.project && run.identity.project.key) {\n        return run.identity.project.key;\n      }\n      if (run && run.identity_projection && run.identity_projection.project_key) {\n        return run.identity_projection.project_key;\n      }\n    }\n    return \"\";\n  }\n  function summarizeProjectHistoryReferences(row) {\n    const summary = row && row.summary ? row.summary : {};\n    return [\n      \"evidence \" + formatNumber(summary.evidence_reference_count || 0),\n      \"tracker \" + formatNumber(summary.tracker_snapshot_count || 0),\n      \"PR/ref \" + formatNumber(summary.ticket_reference_count || 0),\n      \"operator \" + formatNumber(summary.operator_action_count || 0)\n    ].join(\" \\u2022 \");\n  }\n  function summarizeProjectHistoryMetrics(row) {\n    const summary = row && row.summary ? row.summary : {};\n    return [\n      \"attempts \" + formatNumber(summary.attempt_count || 0),\n      \"phases \" + formatNumber(summary.phase_count || 0),\n      \"threads \" + formatNumber(summary.thread_count || 0),\n      \"turns \" + formatNumber(summary.turn_count || 0),\n      \"tokens \" + (summary.total_tokens === null || summary.total_tokens === void 0 ? \"n/a\" : formatNumber(summary.total_tokens))\n    ].join(\" \\u2022 \");\n  }\n  function summarizeProjectHistoryHealth(health) {\n    if (!health || typeof health !== \"object\") {\n      return \"Health: unavailable\";\n    }\n    const storage = health.storage || {};\n    const schema = health.schema || {};\n    const counts = health.counts || {};\n    const retention = health.retention || {};\n    const prune = retention.last_prune || {};\n    const writes = health.writes || {};\n    const projections = health.projections || {};\n    const appServerLite = health.app_server_lite || {};\n    const policyParts = [];\n    if (appServerLite.redacted_event_count || appServerLite.truncated_event_count || appServerLite.summary_only_event_count) {\n      policyParts.push(\n        \"payload policy redacted \" + formatNumber(appServerLite.redacted_event_count || 0) + \" / truncated \" + formatNumber(appServerLite.truncated_event_count || 0) + \" / summary-only \" + formatNumber(appServerLite.summary_only_event_count || 0)\n      );\n    }\n    if (appServerLite.unavailable_event_count) {\n      const reasons = Array.isArray(appServerLite.unavailable_reasons) ? appServerLite.unavailable_reasons.map(function(reason) {\n        return (reason.reason_code || \"unknown\") + \" \" + formatNumber(reason.count || 0) + \" \" + (reason.classification || \"unknown\");\n      }).join(\", \") : \"reason unknown\";\n      policyParts.push(\"payload unavailable \" + formatNumber(appServerLite.unavailable_event_count || 0) + \" (\" + reasons + \")\");\n    }\n    const diagnostics = Array.isArray(health.diagnostics) ? health.diagnostics : [];\n    const lifecyclePendingCount = diagnostics.filter(function(fact) {\n      return fact && fact.status === \"lifecycle_pending\";\n    }).length;\n    const optionalUnavailableCount = diagnostics.filter(function(fact) {\n      return fact && fact.status === \"optional_unavailable\";\n    }).length;\n    const degradedFactCount = diagnostics.filter(function(fact) {\n      return fact && (fact.status === \"missing\" || fact.status === \"degraded\" || fact.status === \"unavailable\");\n    }).length;\n    if (lifecyclePendingCount || optionalUnavailableCount || degradedFactCount) {\n      policyParts.push(\n        \"facts pending \" + formatNumber(lifecyclePendingCount) + \" / optional unavailable \" + formatNumber(optionalUnavailableCount) + \" / degraded \" + formatNumber(degradedFactCount)\n      );\n    }\n    return [\n      \"Health: \" + (health.status || \"unknown\"),\n      \"enabled \" + (health.enabled ? \"yes\" : \"no\"),\n      \"storage \" + (storage.target || storage.type || \"n/a\"),\n      \"schema \" + (schema.status || \"unknown\") + \" / integrity \" + (schema.integrity_ok ? \"ok\" : \"degraded\"),\n      \"runs \" + formatNumber(counts.runs || 0),\n      \"tickets \" + (counts.tickets === null || counts.tickets === void 0 ? \"n/a\" : formatNumber(counts.tickets)),\n      \"retention \" + (retention.retention_days === null || retention.retention_days === void 0 ? \"n/a\" : formatNumber(retention.retention_days) + \"d\"),\n      \"prune \" + (prune.status || \"unknown\"),\n      \"writes \" + (writes.status || \"unknown\"),\n      \"projection \" + (projections.status || \"unknown\"),\n      \"app-server-lite \" + (appServerLite.status || \"unknown\")\n    ].concat(policyParts).join(\" \\u2022 \");\n  }\n  function renderProjectHistory() {\n    const projectKey = state.projectHistory.projectKey || \"\";\n    elements.projectHistoryProjectKey.value = projectKey;\n    elements.projectHistoryLoad.disabled = !!state.projectHistory.loading;\n    if (state.projectHistory.loading) {\n      elements.projectHistoryStatus.textContent = \"Loading bounded ticket history for project \" + projectKey + \".\";\n      elements.projectHistoryRows.replaceChildren(createProjectHistoryEmptyRow(\"Loading project history...\"));\n      return;\n    }\n    if (state.projectHistory.error) {\n      elements.projectHistoryStatus.textContent = \"Project history unavailable: \" + state.projectHistory.error + \" \\u2022 \" + summarizeProjectHistoryHealth(state.projectHistory.healthPayload);\n      const healthFacts = state.projectHistory.healthPayload && Array.isArray(state.projectHistory.healthPayload.diagnostics) ? state.projectHistory.healthPayload.diagnostics : [];\n      elements.projectHistoryFacts.replaceChildren(...createFactBadges(healthFacts));\n      elements.projectHistoryRows.replaceChildren(createProjectHistoryEmptyRow(\"Project history unavailable.\"));\n      elements.projectHistoryDetail.classList.add(\"hidden\");\n      elements.projectHistoryDetail.replaceChildren();\n      return;\n    }\n    const payload = state.projectHistory.listPayload;\n    if (!payload) {\n      elements.projectHistoryStatus.textContent = projectKey ? \"Project history is ready to load for project \" + projectKey + \".\" : \"No project key discovered from bounded run history yet.\";\n      elements.projectHistoryFacts.replaceChildren();\n      elements.projectHistoryRows.replaceChildren(createProjectHistoryEmptyRow(\"Project history uses bounded ticket rows and loads detail on demand.\"));\n      elements.projectHistoryDetail.classList.add(\"hidden\");\n      elements.projectHistoryDetail.replaceChildren();\n      return;\n    }\n    const tickets = Array.isArray(payload.tickets) ? payload.tickets : [];\n    const page = payload.page || {};\n    elements.projectHistoryStatus.textContent = \"Showing \" + formatNumber(tickets.length) + \" of \" + (page.total === null || page.total === void 0 ? \"unknown\" : formatNumber(page.total)) + \" ticket rows\" + (page.has_more ? \" (bounded page; more rows available).\" : \".\") + \" \\u2022 \" + summarizeProjectHistoryHealth(payload.health || state.projectHistory.healthPayload);\n    elements.projectHistoryFacts.replaceChildren(...createFactBadges(payload.facts));\n    if (!tickets.length) {\n      elements.projectHistoryRows.replaceChildren(createProjectHistoryEmptyRow(\"No project ticket history available for this project.\"));\n      return;\n    }\n    const rows = tickets.map((entry) => {\n      const row = document.createElement(\"tr\");\n      row.dataset.ticketKey = entry.ticket_identity && entry.ticket_identity.key ? entry.ticket_identity.key : \"\";\n      const ticketCell = document.createElement(\"td\");\n      const ticketTitle = document.createElement(\"strong\");\n      ticketTitle.textContent = entry.ticket_identity && entry.ticket_identity.human_issue_identifier || row.dataset.ticketKey || \"unknown\";\n      const ticketMeta = document.createElement(\"div\");\n      ticketMeta.className = \"muted\";\n      ticketMeta.textContent = row.dataset.ticketKey || \"ticket key unavailable\";\n      ticketCell.append(ticketTitle, ticketMeta);\n      const stateCell = document.createElement(\"td\");\n      stateCell.append(createStateBadge(entry.state || \"unknown\"));\n      const statusMeta = document.createElement(\"div\");\n      statusMeta.className = \"muted\";\n      statusMeta.textContent = entry.current_status || entry.last_known_status || \"unknown\";\n      stateCell.append(statusMeta);\n      const attemptCell = document.createElement(\"td\");\n      const latestAttempt = entry.latest_attempt || {};\n      attemptCell.textContent = latestAttempt.attempt_number === null || latestAttempt.attempt_number === void 0 ? \"No attempt recorded\" : \"Attempt \" + latestAttempt.attempt_number + \" \\u2022 \" + (latestAttempt.status || \"unknown\");\n      const attemptMeta = document.createElement(\"div\");\n      attemptMeta.className = \"muted\";\n      attemptMeta.textContent = formatDate(latestAttempt.started_at) + (latestAttempt.ended_at ? \" - \" + formatDate(latestAttempt.ended_at) : \"\");\n      attemptCell.append(attemptMeta);\n      const outcomeCell = document.createElement(\"td\");\n      outcomeCell.textContent = latestAttempt.outcome || \"No terminal outcome\";\n      const outcomeMeta = document.createElement(\"div\");\n      outcomeMeta.className = \"muted\";\n      outcomeMeta.textContent = latestAttempt.outcome_reason_code || \"outcome fact missing or unavailable\";\n      outcomeCell.append(outcomeMeta);\n      const refsCell = document.createElement(\"td\");\n      refsCell.textContent = summarizeProjectHistoryReferences(entry);\n      const summaryCell = document.createElement(\"td\");\n      summaryCell.textContent = summarizeProjectHistoryMetrics(entry);\n      const factsCell = document.createElement(\"td\");\n      factsCell.className = \"project-history-facts-cell\";\n      const factBadges = createFactBadges(entry.facts);\n      factsCell.replaceChildren(...factBadges);\n      const observedCell = document.createElement(\"td\");\n      observedCell.textContent = formatDate(entry.latest_observed_at);\n      const actionsCell = document.createElement(\"td\");\n      const detailButton = createActionButton(\n        state.projectHistory.detailLoadingTicketKey === row.dataset.ticketKey ? \"Loading...\" : \"View Timeline\",\n        \"ghost-button\",\n        function() {\n          void loadProjectHistoryDetail(row.dataset.ticketKey || \"\");\n        }\n      );\n      detailButton.disabled = !row.dataset.ticketKey || state.projectHistory.detailLoadingTicketKey === row.dataset.ticketKey;\n      actionsCell.append(detailButton);\n      row.append(ticketCell, stateCell, attemptCell, outcomeCell, refsCell, summaryCell, factsCell, observedCell, actionsCell);\n      return row;\n    });\n    elements.projectHistoryRows.replaceChildren(...rows);\n    renderProjectHistoryDetail();\n  }\n  function createProjectHistorySection(title, items, renderItem) {\n    const section = document.createElement(\"section\");\n    section.className = \"thread-detail-section\";\n    const heading = document.createElement(\"h3\");\n    heading.textContent = title;\n    section.append(heading);\n    if (!Array.isArray(items) || !items.length) {\n      const empty = document.createElement(\"p\");\n      empty.className = \"muted\";\n      empty.textContent = \"No \" + title.toLowerCase() + \" facts recorded.\";\n      section.append(empty);\n      return section;\n    }\n    const list = document.createElement(\"ul\");\n    list.className = \"project-history-timeline-list\";\n    for (const item of items) {\n      const node = document.createElement(\"li\");\n      node.textContent = renderItem(item);\n      list.append(node);\n    }\n    section.append(list);\n    return section;\n  }\n  function renderProjectHistoryDetail() {\n    const detail = state.projectHistory.detailPayload;\n    elements.projectHistoryDetail.replaceChildren();\n    if (state.projectHistory.detailError) {\n      elements.projectHistoryDetail.classList.remove(\"hidden\");\n      const error = document.createElement(\"p\");\n      error.className = \"status-error\";\n      error.textContent = \"Ticket timeline unavailable: \" + state.projectHistory.detailError;\n      elements.projectHistoryDetail.append(error);\n      return;\n    }\n    if (!detail) {\n      elements.projectHistoryDetail.classList.add(\"hidden\");\n      return;\n    }\n    elements.projectHistoryDetail.classList.remove(\"hidden\");\n    const heading = document.createElement(\"h3\");\n    heading.textContent = \"Ticket Timeline: \" + (detail.ticket_identity && detail.ticket_identity.human_issue_identifier || detail.ticket_identity.key);\n    const facts = document.createElement(\"div\");\n    facts.className = \"inline-badges\";\n    facts.replaceChildren(...createFactBadges(detail.facts));\n    const grid = document.createElement(\"div\");\n    grid.className = \"project-history-detail-grid\";\n    const drainAuditEvents = Array.isArray(state.projectHistory.listPayload && state.projectHistory.listPayload.drain_audit_events) ? state.projectHistory.listPayload.drain_audit_events : [];\n    grid.append(\n      createProjectHistorySection(\"Drain Audit Events\", drainAuditEvents, function(item) {\n        return item.event_type + \" \\u2022 \" + item.result_code + \" \\u2022 \" + (item.actor || item.source || \"unknown\") + \" \\u2022 \" + formatDate(item.occurred_at);\n      }),\n      createProjectHistorySection(\"Attempts\", detail.attempts, function(item) {\n        return \"Attempt \" + item.attempt_number + \" \\u2022 \" + item.status + \" \\u2022 \" + formatDate(item.started_at) + \" - \" + formatDate(item.ended_at);\n      }),\n      createProjectHistorySection(\"Phases\", detail.phases, function(item) {\n        return item.phase + \" \\u2022 \" + item.status + \" \\u2022 \" + formatDate(item.started_at) + \" - \" + formatDate(item.ended_at) + (item.reason_code ? \" \\u2022 \" + item.reason_code : \"\");\n      }),\n      createProjectHistorySection(\"State Transitions\", detail.state_transitions, function(item) {\n        return item.from_status + \" -> \" + item.to_status + \" \\u2022 \" + formatDate(item.transitioned_at) + (item.reason_code ? \" \\u2022 \" + item.reason_code : \"\");\n      }),\n      createProjectHistorySection(\"Thread References\", detail.thread_references, function(item) {\n        return item.thread_id + \" \\u2022 attempt \" + item.attempt_id + \" \\u2022 \" + item.status + \" \\u2022 \" + formatDate(item.started_at);\n      }),\n      createProjectHistorySection(\"Turn References\", detail.turn_references, function(item) {\n        return item.turn_id + \" \\u2022 thread \" + item.thread_id + \" \\u2022 turn \" + item.turn_index + \" \\u2022 \" + item.status;\n      }),\n      createProjectHistorySection(\"Outcomes\", detail.outcomes, function(item) {\n        return item.outcome + \" \\u2022 \" + (item.reason_code || \"no reason\") + \" \\u2022 \" + formatDate(item.recorded_at);\n      }),\n      createProjectHistorySection(\"Blockers\", detail.blockers, function(item) {\n        return item.status + \" \\u2022 \" + item.blocker_type + \" \\u2022 \" + (item.reason_code || \"no reason\") + \" \\u2022 \" + (item.reason_detail || \"no detail\");\n      }),\n      createProjectHistorySection(\"Evidence\", detail.evidence_references, function(item) {\n        return item.evidence_kind + \" \\u2022 \" + (item.title || item.uri || \"untitled\") + \" \\u2022 \" + formatDate(item.recorded_at);\n      }),\n      createProjectHistorySection(\"Tracker And PR Facts\", [].concat(detail.tracker_facts || [], detail.pr_and_reference_facts || [], detail.operator_facts || []), function(item) {\n        return (item.tracker_status || item.reference_kind || item.action || \"fact\") + \" \\u2022 \" + (item.label || item.result || item.state || item.tracker_status || \"n/a\") + \" \\u2022 \" + formatDate(item.last_observed_at || item.observed_at || item.requested_at);\n      }),\n      createProjectHistorySection(\"Ticket Drain Audit Events\", detail.drain_audit_events, function(item) {\n        const blockers = Array.isArray(item.blocker_summaries) && item.blocker_summaries.length ? \" \\u2022 blockers \" + item.blocker_summaries.map(function(blocker) {\n          return blocker.category + \":\" + blocker.count;\n        }).join(\", \") : \"\";\n        return item.event_type + \" \\u2022 \" + item.result + \" \\u2022 \" + item.result_code + \" \\u2022 \" + formatDate(item.occurred_at) + blockers;\n      }),\n      createProjectHistorySection(\"App Server Lite Excerpts\", detail.app_server_lite_summaries, function(item) {\n        return item.source_event_name + \" \\u2022 \" + item.detail_status + \" \\u2022 \" + (item.summary || item.redacted_excerpt || item.unavailable_reason_code || \"no excerpt\");\n      }),\n      createProjectHistorySection(\"Token And Model Facts\", detail.token_model_summaries, function(item) {\n        return (item.effective_model || item.requested_model || \"model unknown\") + \" \\u2022 tokens \" + (item.total_tokens === null || item.total_tokens === void 0 ? \"n/a\" : formatNumber(item.total_tokens)) + \" \\u2022 \" + (item.telemetry_confidence || \"confidence unknown\");\n      }),\n      createProjectHistorySection(\"Blocked Input Events\", detail.blocked_input_events, function(item) {\n        return (item.request_id || \"request\") + \" \\u2022 \" + (item.status || \"status unknown\") + \" \\u2022 \" + (item.reason_code || \"no reason\");\n      })\n    );\n    elements.projectHistoryDetail.append(heading, facts, grid);\n  }\n  async function loadProjectHistory(projectKey) {\n    const requestedProjectKey = String(projectKey || elements.projectHistoryProjectKey.value || \"\").trim();\n    if (!requestedProjectKey || state.projectHistory.loading) {\n      renderProjectHistory();\n      return;\n    }\n    state.projectHistory.projectKey = requestedProjectKey;\n    state.projectHistory.loading = true;\n    state.projectHistory.error = null;\n    renderProjectHistory();\n    try {\n      state.projectHistory.listPayload = await fetchJson(\"/api/v1/projects/\" + encodeURIComponent(requestedProjectKey) + \"/history/tickets?limit=50\");\n      state.projectHistory.healthPayload = state.projectHistory.listPayload.health || null;\n      state.projectHistory.detailPayload = null;\n      state.projectHistory.detailError = null;\n    } catch (error) {\n      state.projectHistory.listPayload = null;\n      state.projectHistory.error = String(error);\n      try {\n        state.projectHistory.healthPayload = await fetchJson(\"/api/v1/projects/\" + encodeURIComponent(requestedProjectKey) + \"/history/health\");\n      } catch {\n        state.projectHistory.healthPayload = null;\n      }\n    } finally {\n      state.projectHistory.loading = false;\n      renderProjectHistory();\n    }\n  }\n  async function loadProjectHistoryDetail(ticketKey) {\n    const projectKey = state.projectHistory.projectKey;\n    if (!projectKey || !ticketKey) {\n      return;\n    }\n    state.projectHistory.detailLoadingTicketKey = ticketKey;\n    state.projectHistory.detailError = null;\n    renderProjectHistory();\n    try {\n      state.projectHistory.detailPayload = await fetchJson(\n        \"/api/v1/projects/\" + encodeURIComponent(projectKey) + \"/history/tickets/\" + encodeURIComponent(ticketKey)\n      );\n    } catch (error) {\n      state.projectHistory.detailPayload = null;\n      state.projectHistory.detailError = String(error);\n    } finally {\n      state.projectHistory.detailLoadingTicketKey = null;\n      renderProjectHistory();\n    }\n  }\n\n  // src/api/dashboard-client/apple-constellation-core.ts\n  var FALLBACK_LENS = {\n    identifier: \"NIE-300\",\n    title: \"Chatty\",\n    runLabel: \"Run Attempt #2\",\n    latestRole: \"assistant\",\n    latestMessage: \"Applying bounded payload strategy to snapshot API. Adding windowed transcript pagination and cursor semantics...\",\n    latestAt: \"12:41:28 - now\",\n    threadId: \"thread_01JX7...\",\n    sessionId: \"sessions/01JX7/rollout.json\",\n    confidence: 0.92,\n    confidenceLabel: \"high\",\n    roleStream: [\n      { role: \"assistant\", count: 6 },\n      { role: \"tool\", count: 3 },\n      { role: \"user\", count: 1 },\n      { role: \"system\", count: 2 }\n    ],\n    events: [\n      { label: \"terminal\", detail: \"command stream\", at: \"12:41:12\", tone: \"blue\" },\n      { label: \"git\", detail: \"diff + branch\", at: \"12:41:05\", tone: \"orange\" },\n      { label: \"Linear\", detail: \"ticket sync\", at: \"12:40:58\", tone: \"violet\" },\n      { label: \"build\", detail: \"asset bundle\", at: \"12:40:41\", tone: \"green\" },\n      { label: \"tests\", detail: \"targeted suite\", at: \"12:40:29\", tone: \"cyan\" }\n    ],\n    contextVisible: 120,\n    contextClipped: 80,\n    contextPercent: 60\n  };\n  function cleanText(value) {\n    if (value === null || value === void 0) {\n      return \"\";\n    }\n    return String(value).replace(/\\s+/g, \" \").trim();\n  }\n  function truncate(value, maxLength) {\n    const text = cleanText(value);\n    if (text.length <= maxLength) {\n      return text;\n    }\n    return text.slice(0, Math.max(0, maxLength - 3)).trim() + \"...\";\n  }\n  function createElement(tag, className, text) {\n    const element = document.createElement(tag);\n    element.className = className;\n    if (text !== void 0) {\n      element.textContent = text;\n    }\n    return element;\n  }\n  function setStyleProperty(element, name, value) {\n    if (element.style && typeof element.style.setProperty === \"function\") {\n      element.style.setProperty(name, value);\n      return;\n    }\n    element.style[name] = value;\n  }\n  function identifierFromEntry(entry) {\n    return cleanText(entry && (entry.issue_identifier || entry.identifier || entry.issue_id));\n  }\n  function chooseFocus(model) {\n    const running = Array.isArray(model?.running) ? model.running : [];\n    const blocked = Array.isArray(model?.blocked) ? model.blocked : [];\n    const retry = Array.isArray(model?.retry) ? model.retry : [];\n    const active = [].concat(running, blocked, retry);\n    return active.find((entry) => identifierFromEntry(entry) === \"NIE-300\") || model?.focus || running[0] || blocked[0] || retry[0] || null;\n  }\n  function titleFromEntry(entry, identifier) {\n    const candidates = [\n      entry?.issue_title,\n      entry?.title,\n      entry?.headline,\n      entry?.operator_explainer_hint?.headline,\n      entry?.conversation_latest?.summary,\n      entry?.last_event_summary,\n      entry?.last_message\n    ];\n    for (const candidate of candidates) {\n      const text = truncate(cleanText(candidate), 42);\n      if (text && text !== identifier) {\n        return identifier === \"NIE-300\" && text.length > 28 ? \"Chatty\" : text;\n      }\n    }\n    return identifier === \"NIE-300\" ? \"Chatty\" : \"Focused Run\";\n  }\n  function roleFromEvent(event) {\n    const normalized = cleanText(`${event?.event || \"\"} ${event?.request_category || \"\"} ${event?.request_method || \"\"}`).toLowerCase();\n    if (event?.tool_name || event?.tool_call_id || normalized.includes(\"tool\")) {\n      return \"tool\";\n    }\n    if (normalized.includes(\"assistant\") || normalized.includes(\"turn\")) {\n      return \"assistant\";\n    }\n    if (normalized.includes(\"input\") || normalized.includes(\"user\")) {\n      return \"user\";\n    }\n    if (normalized.includes(\"system\") || normalized.includes(\"protocol\")) {\n      return \"system\";\n    }\n    return \"runtime\";\n  }\n  function roleCountsFromEntry(entry) {\n    const metadataCounts = entry?.conversation?.metadata?.role_counts;\n    const counts = {\n      system: Number(metadataCounts?.system || 0),\n      user: Number(metadataCounts?.user || 0),\n      assistant: Number(metadataCounts?.assistant || 0),\n      tool: Number(metadataCounts?.tool || 0),\n      runtime: Number(metadataCounts?.runtime || 0)\n    };\n    const messages = Array.isArray(entry?.conversation?.messages) ? entry.conversation.messages : [];\n    for (const message of messages) {\n      const role = cleanText(message?.role);\n      if (role in counts && !metadataCounts) {\n        counts[role] += 1;\n      }\n    }\n    const recentEvents = [].concat(\n      Array.isArray(entry?.recent_events) ? entry.recent_events : [],\n      Array.isArray(entry?.session_console) ? entry.session_console : []\n    );\n    for (const event of recentEvents) {\n      counts[roleFromEvent(event)] += 1;\n    }\n    const latestRole = cleanText(entry?.conversation_latest?.role);\n    if (latestRole in counts && counts[latestRole] === 0) {\n      counts[latestRole] += 1;\n    }\n    return counts;\n  }\n  function roleStreamFromEntry(entry) {\n    if (!entry) {\n      return FALLBACK_LENS.roleStream;\n    }\n    const counts = roleCountsFromEntry(entry);\n    const stream = [\"assistant\", \"tool\", \"user\", \"system\", \"runtime\"].map((role) => ({ role, count: counts[role] || 0 })).filter((item) => item.count > 0).slice(0, 4);\n    return stream.length ? stream : FALLBACK_LENS.roleStream;\n  }\n  function eventLabel(event, index) {\n    const tool = cleanText(event?.tool_name);\n    const normalizedTool = tool.toLowerCase();\n    if (tool) {\n      if (normalizedTool.includes(\"linear\")) {\n        return \"Linear\";\n      }\n      if (normalizedTool.includes(\"exec\") || normalizedTool.includes(\"shell\") || normalizedTool.includes(\"terminal\")) {\n        return \"terminal\";\n      }\n      return truncate(tool, 14);\n    }\n    const eventName = cleanText(event?.event);\n    if (/git|commit|branch/i.test(eventName)) {\n      return \"git\";\n    }\n    if (/linear|issue|tracker/i.test(eventName)) {\n      return \"Linear\";\n    }\n    if (/build|bundle|asset/i.test(eventName)) {\n      return \"build\";\n    }\n    if (/test|vitest|check/i.test(eventName)) {\n      return \"tests\";\n    }\n    if (/tool|command|exec|terminal/i.test(eventName)) {\n      return \"terminal\";\n    }\n    return [\"terminal\", \"git\", \"Linear\", \"build\", \"tests\"][index % 5];\n  }\n  function eventTone(label) {\n    const normalized = label.toLowerCase();\n    if (normalized === \"git\") {\n      return \"orange\";\n    }\n    if (normalized === \"linear\") {\n      return \"violet\";\n    }\n    if (normalized === \"build\") {\n      return \"green\";\n    }\n    if (normalized === \"tests\") {\n      return \"cyan\";\n    }\n    return \"blue\";\n  }\n  function formatEventTime(event) {\n    const raw = event?.at || event?.observed_at || event?.at_ms;\n    const parsed = typeof raw === \"number\" ? raw : Date.parse(cleanText(raw));\n    if (!Number.isFinite(parsed)) {\n      return \"--:--:--\";\n    }\n    return new Date(parsed).toLocaleTimeString([], { hour: \"2-digit\", minute: \"2-digit\", second: \"2-digit\", hour12: false });\n  }\n  function eventsFromEntry(entry) {\n    if (!entry) {\n      return FALLBACK_LENS.events;\n    }\n    const source = [].concat(\n      Array.isArray(entry?.recent_events) ? entry.recent_events : [],\n      Array.isArray(entry?.conversation?.messages) ? entry.conversation.messages : [],\n      Array.isArray(entry?.session_console) ? entry.session_console : []\n    );\n    const events = source.slice(-8).reverse().map((event, index) => {\n      const label = eventLabel(event, index);\n      return {\n        label,\n        detail: truncate(cleanText(event?.message || event?.content || event?.event || event?.source || \"activity\"), 30),\n        at: formatEventTime(event),\n        tone: eventTone(label)\n      };\n    }).filter((event) => event.detail || event.label).slice(0, 5);\n    return events.length ? events : FALLBACK_LENS.events;\n  }\n  function latestMessageFromEntry(entry) {\n    if (!entry) {\n      return {\n        role: FALLBACK_LENS.latestRole,\n        message: FALLBACK_LENS.latestMessage,\n        at: FALLBACK_LENS.latestAt\n      };\n    }\n    const latest = entry.conversation_latest || entry.conversation?.latest || {};\n    const role = cleanText(latest.role) || (entry.pending_input_preview ? \"user\" : \"assistant\");\n    const message = cleanText(latest.summary) || cleanText(entry.last_message) || cleanText(entry.pending_input_preview?.prompt_preview) || cleanText(entry.last_event_summary) || \"No agent message has been observed yet.\";\n    const at = cleanText(latest.at) || cleanText(entry.last_event_at) || cleanText(entry.started_at);\n    return {\n      role,\n      message: truncate(message, 170),\n      at: at ? formatEventTime({ at }) + \" - now\" : \"--:--:--\"\n    };\n  }\n  function contextFromEntry(entry) {\n    const total = Number(entry?.tokens?.total_tokens || 0);\n    const windowSize = Number(entry?.tokens?.model_context_window || 0);\n    if (Number.isFinite(total) && total > 0 && Number.isFinite(windowSize) && windowSize > 0) {\n      const visible = Math.max(1, Math.round(total / 1e3));\n      const capacity = Math.max(visible, Math.round(windowSize / 1e3));\n      const clipped = Math.max(0, capacity - visible);\n      return {\n        visible,\n        clipped,\n        percent: Math.max(8, Math.min(100, Math.round(visible / capacity * 100)))\n      };\n    }\n    return {\n      visible: FALLBACK_LENS.contextVisible,\n      clipped: FALLBACK_LENS.contextClipped,\n      percent: FALLBACK_LENS.contextPercent\n    };\n  }\n  function confidenceFromEntry(entry) {\n    const summary = entry?.transcript_tool_call_diagnostic_summary;\n    if (!entry) {\n      return { confidence: FALLBACK_LENS.confidence, label: FALLBACK_LENS.confidenceLabel };\n    }\n    if (summary?.active_missing_tool_output?.active) {\n      return { confidence: 0.61, label: \"missing output\" };\n    }\n    if (summary?.detailed_diagnostics_available || summary?.total_count > 0 || entry?.conversation_latest?.summary) {\n      return { confidence: 0.92, label: \"high\" };\n    }\n    if (entry?.last_message || entry?.last_event_summary) {\n      return { confidence: 0.78, label: \"partial\" };\n    }\n    return { confidence: 0.54, label: \"thin\" };\n  }\n  function buildLensModel(model) {\n    const focus = chooseFocus(model || {});\n    if (!focus) {\n      return FALLBACK_LENS;\n    }\n    const identifier = identifierFromEntry(focus) || FALLBACK_LENS.identifier;\n    const latest = latestMessageFromEntry(focus);\n    const context = contextFromEntry(focus);\n    const confidence = confidenceFromEntry(focus);\n    return {\n      identifier,\n      title: titleFromEntry(focus, identifier),\n      runLabel: \"Run Attempt #\" + String(focus.retry_attempt ?? focus.attempt ?? focus.attempt_number ?? 1),\n      latestRole: latest.role,\n      latestMessage: latest.message,\n      latestAt: latest.at,\n      threadId: cleanText(focus.thread_id || focus.previous_thread_id || focus.persisted_thread_id) || FALLBACK_LENS.threadId,\n      sessionId: cleanText(focus.session_id || focus.previous_session_id) || FALLBACK_LENS.sessionId,\n      confidence: confidence.confidence,\n      confidenceLabel: confidence.label,\n      roleStream: roleStreamFromEntry(focus),\n      events: eventsFromEntry(focus),\n      contextVisible: context.visible,\n      contextClipped: context.clipped,\n      contextPercent: context.percent\n    };\n  }\n  function renderFocusPill(lens) {\n    const pill = createElement(\"section\", \"lens-focus-pill\");\n    const identifier = createElement(\"div\", \"lens-focus-id\", lens.identifier);\n    const title = createElement(\"div\", \"lens-focus-title\", lens.title);\n    const run = createElement(\"div\", \"lens-focus-run\", lens.runLabel);\n    pill.append(identifier, title, run);\n    return pill;\n  }\n  function renderCurrentMessage(lens) {\n    const card = createElement(\"section\", \"lens-current-message\");\n    card.append(\n      createElement(\"div\", \"lens-label\", \"Latest Message\"),\n      createElement(\"div\", \"lens-message-role\", \"role: \" + lens.latestRole),\n      createElement(\"p\", \"lens-message-body\", lens.latestMessage),\n      createElement(\"div\", \"lens-message-time\", lens.latestAt)\n    );\n    return card;\n  }\n  function renderRoleStream(lens) {\n    const panel = createElement(\"section\", \"lens-role-stream\");\n    panel.append(createElement(\"div\", \"lens-label\", \"Recent Roles\"));\n    const lanes = createElement(\"div\", \"lens-stream-lanes\");\n    lens.roleStream.forEach((entry, index) => {\n      const lane = createElement(\"div\", \"lens-stream-lane lens-stream-\" + entry.role);\n      setStyleProperty(lane, \"--stream-index\", String(index));\n      setStyleProperty(lane, \"--stream-count\", String(Math.max(1, Math.min(8, entry.count))));\n      lane.append(createElement(\"span\", \"lens-stream-role\", entry.role), createElement(\"strong\", \"lens-stream-count\", String(entry.count)));\n      lanes.appendChild(lane);\n    });\n    panel.appendChild(lanes);\n    return panel;\n  }\n  function renderEventOrbit(lens) {\n    const orbit = createElement(\"div\", \"lens-event-orbit\");\n    lens.events.forEach((event, index) => {\n      const node = createElement(\"article\", \"lens-event-node lens-event-\" + event.tone);\n      const path = createElement(\"span\", \"lens-event-path\");\n      const body = createElement(\"span\", \"lens-event-body\");\n      const angle = lens.events.length > 1 ? -130 + index * (260 / (lens.events.length - 1)) : -28;\n      const radius = 152 + index % 3 * 48;\n      const speed = 22 + index * 4;\n      const scale = index % 3 === 0 ? 0.88 : index % 3 === 1 ? 1.04 : 0.96;\n      setStyleProperty(node, \"--orbit-angle\", String(angle) + \"deg\");\n      setStyleProperty(node, \"--orbit-counter-angle\", String(-angle) + \"deg\");\n      setStyleProperty(node, \"--orbit-radius\", String(radius) + \"px\");\n      setStyleProperty(node, \"--orbit-speed\", String(speed) + \"s\");\n      setStyleProperty(node, \"--orbit-delay\", String(-index * 3) + \"s\");\n      setStyleProperty(node, \"--orbit-scale\", String(scale));\n      const bead = createElement(\"span\", \"lens-event-bead\", event.label.slice(0, 1).toUpperCase());\n      const copy = createElement(\"span\", \"lens-event-copy\");\n      copy.append(createElement(\"strong\", \"lens-event-label\", event.label), createElement(\"span\", \"lens-event-time\", event.at));\n      body.append(bead, copy);\n      path.appendChild(body);\n      node.appendChild(path);\n      orbit.appendChild(node);\n    });\n    return orbit;\n  }\n  function renderConfidence(lens) {\n    const panel = createElement(\"aside\", \"lens-confidence\");\n    panel.append(\n      createElement(\"span\", \"lens-label\", \"Transcript Coverage\"),\n      createElement(\"strong\", \"lens-confidence-score\", lens.confidence.toFixed(2)),\n      createElement(\"span\", \"lens-confidence-label\", lens.confidenceLabel)\n    );\n    return panel;\n  }\n  function renderContextMeter(lens) {\n    const panel = createElement(\"section\", \"lens-context-meter\");\n    const ticks = createElement(\"div\", \"lens-context-ticks\");\n    setStyleProperty(ticks, \"--context-fill\", String(lens.contextPercent) + \"%\");\n    const filledTicks = Math.max(1, Math.min(20, Math.round(lens.contextPercent / 5)));\n    for (let index = 0; index < 20; index += 1) {\n      ticks.appendChild(createElement(\"span\", index < filledTicks ? \"lens-context-tick lens-context-tick-filled\" : \"lens-context-tick\"));\n    }\n    panel.append(\n      createElement(\"div\", \"lens-label\", \"Visible Context\"),\n      createElement(\"strong\", \"lens-context-visible\", String(lens.contextVisible) + \" messages\"),\n      ticks,\n      createElement(\"div\", \"lens-context-clip\", String(lens.contextVisible) + \" visible / \" + String(lens.contextClipped) + \" clipped\")\n    );\n    return panel;\n  }\n  function renderEvidenceDock(lens) {\n    const dock = createElement(\"section\", \"lens-evidence-dock\");\n    const thread = createElement(\"span\", \"lens-evidence-node\", \"thread \" + lens.threadId);\n    const session = createElement(\"span\", \"lens-evidence-node\", \"session \" + lens.sessionId);\n    const transcript = createElement(\"span\", \"lens-evidence-node\", \"transcript\");\n    const snapshot = createElement(\"span\", \"lens-evidence-node\", \"state snapshot\");\n    const audit = createElement(\"span\", \"lens-evidence-node\", \"audit receipt\");\n    for (const node of [thread, session, transcript, snapshot, audit]) {\n      node.title = node.textContent || \"\";\n    }\n    dock.append(thread, session, transcript, snapshot, audit);\n    return dock;\n  }\n  function renderCoreStar() {\n    const core = createElement(\"div\", \"lens-core-star\");\n    core.append(\n      createElement(\"span\", \"lens-star-pulse\"),\n      createElement(\"span\", \"lens-star-point\"),\n      createElement(\"span\", \"lens-star-grid\")\n    );\n    return core;\n  }\n  function renderDepthField() {\n    const field = createElement(\"div\", \"lens-depth-field\");\n    field.append(\n      createElement(\"span\", \"lens-depth-plane lens-depth-plane-back\"),\n      createElement(\"span\", \"lens-depth-plane lens-depth-plane-mid\"),\n      createElement(\"span\", \"lens-depth-plane lens-depth-plane-front\")\n    );\n    return field;\n  }\n  function renderOrbitTracks() {\n    const tracks = createElement(\"div\", \"lens-orbit-tracks\");\n    tracks.append(\n      createElement(\"span\", \"lens-orbit-track lens-orbit-track-alpha\"),\n      createElement(\"span\", \"lens-orbit-track lens-orbit-track-beta\"),\n      createElement(\"span\", \"lens-orbit-track lens-orbit-track-gamma\"),\n      createElement(\"span\", \"lens-orbit-track lens-orbit-track-delta\")\n    );\n    return tracks;\n  }\n  function renderConstellationCore(model) {\n    if (!elements.constellationCore) {\n      return;\n    }\n    const lens = buildLensModel(model || {});\n    const root = createElement(\"div\", \"lens-system\");\n    root.append(\n      renderDepthField(),\n      renderOrbitTracks(),\n      createElement(\"span\", \"lens-ring lens-ring-outer\"),\n      createElement(\"span\", \"lens-ring lens-ring-middle\"),\n      createElement(\"span\", \"lens-ring lens-ring-inner\"),\n      renderFocusPill(lens),\n      renderCurrentMessage(lens),\n      renderRoleStream(lens),\n      renderEventOrbit(lens),\n      renderCoreStar(),\n      renderConfidence(lens),\n      renderContextMeter(lens),\n      renderEvidenceDock(lens)\n    );\n    elements.constellationCore.replaceChildren(root);\n  }\n\n  // src/api/dashboard-client/apple-constellation-gravity.ts\n  var FALLBACK_ITEMS = [\n    {\n      identifier: \"NIE-312\",\n      title: \"Auth Flow Polish\",\n      detail: \"needs review\",\n      gravity: 0.72,\n      tone: \"warning\",\n      source: \"retry\"\n    },\n    {\n      identifier: \"NIE-287\",\n      title: \"Telemetry Ingestion\",\n      detail: \"warning\",\n      gravity: 0.37,\n      tone: \"warning\",\n      source: \"retry\"\n    },\n    {\n      identifier: \"NIE-301\",\n      title: \"Snapshot Diffing\",\n      detail: \"running\",\n      gravity: 0.24,\n      tone: \"running\",\n      source: \"running\"\n    },\n    {\n      identifier: \"NIE-300\",\n      title: \"Chatty\",\n      detail: \"focus\",\n      gravity: 0.91,\n      tone: \"focus\",\n      source: \"running\"\n    },\n    {\n      identifier: \"NIE-298\",\n      title: \"Linear Webhooks\",\n      detail: \"running\",\n      gravity: 0.18,\n      tone: \"running\",\n      source: \"running\"\n    },\n    {\n      identifier: \"NIE-276\",\n      title: \"UI Refactor\",\n      detail: \"blocked\",\n      gravity: 0.63,\n      tone: \"blocked\",\n      source: \"blocked\"\n    }\n  ];\n  function cleanText2(value) {\n    if (value === null || value === void 0) {\n      return \"\";\n    }\n    return String(value).replace(/\\s+/g, \" \").trim();\n  }\n  function titleFromEntry2(entry, identifier) {\n    const candidates = [\n      entry.issue_title,\n      entry.title,\n      entry.headline,\n      entry.operator_explainer_hint && entry.operator_explainer_hint.headline,\n      entry.last_event_summary,\n      entry.last_message,\n      entry.conversation_latest && entry.conversation_latest.summary\n    ];\n    for (const candidate of candidates) {\n      const text = cleanText2(candidate);\n      if (text && text !== identifier) {\n        return text.length > 34 ? text.slice(0, 31) + \"...\" : text;\n      }\n    }\n    if (identifier === \"NIE-300\") {\n      return \"Chatty\";\n    }\n    return \"Runtime Work\";\n  }\n  function detailFromEntry(entry, source, tone) {\n    if (tone === \"focus\") {\n      return \"focus\";\n    }\n    if (source === \"blocked\") {\n      if (entry.awaiting_operator_reason_code) {\n        return \"blocked: \" + cleanText2(entry.awaiting_operator_reason_code).replace(/_/g, \" \");\n      }\n      if (entry.stop_reason_code) {\n        return \"blocked: \" + cleanText2(entry.stop_reason_code).replace(/_/g, \" \");\n      }\n      return \"blocked\";\n    }\n    if (source === \"retry\") {\n      if (entry.due_state === \"overdue\") {\n        return \"retry overdue\";\n      }\n      if (entry.stop_reason_code) {\n        return \"retry: \" + cleanText2(entry.stop_reason_code).replace(/_/g, \" \");\n      }\n      return \"retry scheduled\";\n    }\n    if (entry.awaiting_input) {\n      return \"awaiting input\";\n    }\n    if (entry.stalled_waiting) {\n      return \"waiting\";\n    }\n    if (entry.current_phase) {\n      return cleanText2(entry.current_phase).replace(/_/g, \" \");\n    }\n    if (entry.progress_signal_state) {\n      return cleanText2(entry.progress_signal_state).replace(/_/g, \" \");\n    }\n    return \"running\";\n  }\n  function sourceTone(entry, source, identifier, focusIdentifier) {\n    if (identifier === focusIdentifier) {\n      return \"focus\";\n    }\n    if (source === \"blocked\") {\n      return \"blocked\";\n    }\n    if (source === \"retry\" || entry.due_state === \"overdue\") {\n      return \"warning\";\n    }\n    return \"running\";\n  }\n  function gravityFromEntry(entry, source, tone, index) {\n    if (typeof entry.gravity === \"number\" && Number.isFinite(entry.gravity)) {\n      return Math.max(0.01, Math.min(0.99, entry.gravity));\n    }\n    if (tone === \"focus\") {\n      return 0.91;\n    }\n    if (source === \"blocked\") {\n      return entry.awaiting_operator ? 0.72 : 0.63;\n    }\n    if (source === \"retry\") {\n      return entry.due_state === \"overdue\" ? 0.72 : 0.37;\n    }\n    if (entry.awaiting_input) {\n      return 0.63;\n    }\n    if (entry.stalled_waiting) {\n      return 0.37;\n    }\n    return Math.max(0.18, 0.31 - index * 0.04);\n  }\n  function normalizeEntry(entry, source, focusIdentifier, index) {\n    const identifier = cleanText2(entry.issue_identifier || entry.identifier || entry.issue_id);\n    if (!identifier) {\n      return null;\n    }\n    const tone = sourceTone(entry, source, identifier, focusIdentifier);\n    return {\n      identifier,\n      title: titleFromEntry2(entry, identifier),\n      detail: detailFromEntry(entry, source, tone),\n      gravity: gravityFromEntry(entry, source, tone, index),\n      tone,\n      source\n    };\n  }\n  function chooseFocusIdentifier(entries, focus) {\n    const chatty = entries.find((entry) => cleanText2(entry.issue_identifier || entry.identifier || entry.issue_id) === \"NIE-300\");\n    if (chatty) {\n      return \"NIE-300\";\n    }\n    return cleanText2(focus && (focus.issue_identifier || focus.identifier || focus.issue_id));\n  }\n  function collectItems(model) {\n    const running = Array.isArray(model.running) ? model.running : [];\n    const blocked = Array.isArray(model.blocked) ? model.blocked : [];\n    const retry = Array.isArray(model.retry) ? model.retry : [];\n    const entries = [].concat(\n      retry.map((entry) => ({ entry, source: \"retry\" })),\n      running.map((entry) => ({ entry, source: \"running\" })),\n      blocked.map((entry) => ({ entry, source: \"blocked\" }))\n    );\n    if (!entries.length) {\n      return FALLBACK_ITEMS;\n    }\n    const focusIdentifier = chooseFocusIdentifier(\n      entries.map((item) => item.entry),\n      model.focus\n    );\n    const seen = /* @__PURE__ */ new Set();\n    return entries.map((item, index) => normalizeEntry(item.entry, item.source, focusIdentifier, index)).filter((item) => Boolean(item)).filter((item) => {\n      if (seen.has(item.identifier)) {\n        return false;\n      }\n      seen.add(item.identifier);\n      return true;\n    }).sort((left, right) => right.gravity - left.gravity).slice(0, 6);\n  }\n  function glyphForTone(tone) {\n    if (tone === \"focus\") {\n      return \"*\";\n    }\n    if (tone === \"blocked\") {\n      return \"!\";\n    }\n    if (tone === \"warning\") {\n      return \"#\";\n    }\n    return \"~\";\n  }\n  function createTextElement(className, text) {\n    const element = document.createElement(\"span\");\n    element.className = className;\n    element.textContent = text;\n    return element;\n  }\n  function setStyleProperty2(element, name, value) {\n    if (element.style && typeof element.style.setProperty === \"function\") {\n      element.style.setProperty(name, value);\n      return;\n    }\n    element.style[name] = value;\n  }\n  function renderRow(item, index) {\n    const row = document.createElement(\"article\");\n    row.className = \"gravity-row gravity-row-\" + item.tone;\n    row.setAttribute(\"aria-label\", item.identifier + \" \" + item.title + \" priority \" + item.gravity.toFixed(2));\n    const glyph = createTextElement(\"gravity-glyph\", glyphForTone(item.tone));\n    const copy = document.createElement(\"span\");\n    copy.className = \"gravity-copy\";\n    copy.append(\n      createTextElement(\"gravity-identifier\", item.identifier),\n      createTextElement(\"gravity-title\", item.title),\n      createTextElement(\"gravity-detail\", item.detail)\n    );\n    const value = document.createElement(\"span\");\n    value.className = \"gravity-value\";\n    value.append(createTextElement(\"gravity-score\", item.gravity.toFixed(2)), createTextElement(\"gravity-unit\", \"priority\"));\n    const strand = document.createElement(\"span\");\n    strand.className = \"gravity-strand\";\n    const orbitalRoutes = [\n      { width: 326, lift: 12, bend: 108, tilt: 13, sweep: -1 },\n      { width: 268, lift: 8, bend: 86, tilt: 8, sweep: -1 },\n      { width: 222, lift: 2, bend: 68, tilt: 3, sweep: -1 },\n      { width: 216, lift: -2, bend: 68, tilt: -3, sweep: 1 },\n      { width: 248, lift: -8, bend: 82, tilt: -8, sweep: 1 },\n      { width: 292, lift: -12, bend: 102, tilt: -13, sweep: 1 }\n    ];\n    const route = orbitalRoutes[index] || orbitalRoutes[orbitalRoutes.length - 1];\n    setStyleProperty2(strand, \"--strand-width\", String(route.width) + \"px\");\n    setStyleProperty2(strand, \"--strand-lift\", String(route.lift) + \"px\");\n    setStyleProperty2(strand, \"--strand-bend\", String(route.bend) + \"px\");\n    setStyleProperty2(strand, \"--strand-tilt\", String(route.tilt) + \"deg\");\n    setStyleProperty2(strand, \"--strand-sweep\", String(route.sweep));\n    strand.appendChild(createTextElement(\"gravity-dot\", \"\"));\n    row.append(glyph, copy, value, strand);\n    return row;\n  }\n  function renderConstellationGravity(model) {\n    if (!elements.constellationIssueList) {\n      return;\n    }\n    const items = collectItems(model || {});\n    const rows = items.map(renderRow);\n    elements.constellationIssueList.replaceChildren(...rows);\n  }\n\n  // src/api/dashboard-client/apple-constellation-interlocks.ts\n  function cleanText3(value) {\n    if (value === null || value === void 0) {\n      return \"\";\n    }\n    return String(value).replace(/\\s+/g, \" \").trim();\n  }\n  function shortText(value, fallback, length = 28) {\n    const text = cleanText3(value) || fallback;\n    return text.length > length ? text.slice(0, length - 3) + \"...\" : text;\n  }\n  function firstArrayItem(...values) {\n    for (const value of values) {\n      if (Array.isArray(value) && value.length) {\n        return value[0];\n      }\n    }\n    return null;\n  }\n  function focusFromModel(model) {\n    const chatty = firstArrayItem(\n      Array.isArray(model.running) ? model.running.filter((entry) => cleanText3(entry.issue_identifier) === \"NIE-300\") : [],\n      Array.isArray(model.blocked) ? model.blocked.filter((entry) => cleanText3(entry.issue_identifier) === \"NIE-300\") : [],\n      Array.isArray(model.retry) ? model.retry.filter((entry) => cleanText3(entry.issue_identifier) === \"NIE-300\") : []\n    );\n    return chatty || model.focus || firstArrayItem(\n      model.running,\n      model.blocked,\n      model.retry\n    ) || {\n      issue_identifier: \"NIE-300\",\n      title: \"Chatty\",\n      thread_id: \"01JX7\",\n      current_phase: \"operator_steering\",\n      run_attempt: 2,\n      pending_input: { reason_code: \"clarify_direction\" }\n    };\n  }\n  function hasTextMatch(value, pattern) {\n    return pattern.test(cleanText3(value));\n  }\n  function hasConflictingRun(model) {\n    const entries = [].concat(model.running || [], model.blocked || [], model.retry || []);\n    return entries.some((entry) => {\n      return hasTextMatch(entry.stop_reason_code, /conflict/i) || hasTextMatch(entry.awaiting_operator_reason_code, /conflict/i) || hasTextMatch(entry.root_cause && entry.root_cause.reason_code, /conflict/i);\n    });\n  }\n  function hasDirtyWorkspace(model) {\n    const entries = [].concat(model.running || [], model.blocked || [], model.retry || []);\n    return entries.some((entry) => {\n      return hasTextMatch(entry.stop_reason_code, /dirty|worktree/i) || hasTextMatch(entry.root_cause && entry.root_cause.reason_code, /dirty|worktree/i) || hasTextMatch(entry.root_cause && entry.root_cause.detail, /dirty|worktree/i);\n    });\n  }\n  function branchUpToDate(payload) {\n    const readiness = payload.runtime_update || payload.runtimeUpdate || {};\n    const counts = readiness.ahead_behind || {};\n    if (readiness.state === \"build_current\") {\n      return true;\n    }\n    if (counts.behind === 0 || counts.behind === \"0\") {\n      return true;\n    }\n    return !readiness.attention_required && readiness.state !== \"runtime_stale\" && readiness.state !== \"remote_update_available\";\n  }\n  function slaWithinLimits(model, payload) {\n    const counts = payload.counts || {};\n    const retry = Array.isArray(model.retry) ? model.retry : [];\n    const overdue = retry.some((entry) => entry && entry.due_state === \"overdue\");\n    return !overdue && !(Number(counts.running_stalled_waiting_count) > 0);\n  }\n  function threadIdForFocus(focus) {\n    return shortText(\n      focus.thread_id || focus.previous_thread_id || focus.threadId || focus.session_thread_id,\n      \"thread_01JX7\",\n      18\n    );\n  }\n  function transcriptPath(focus) {\n    const session = cleanText3(focus.session_id || focus.previous_session_id || focus.sessionId);\n    if (session) {\n      return \"sessions/\" + shortText(session, session, 14) + \"/rollout.jsonl\";\n    }\n    return \"sessions/\" + threadIdForFocus(focus) + \"/rollout.jsonl\";\n  }\n  function receiptId(model, payload) {\n    const audit = payload.audit_receipt || payload.last_audit_receipt || {};\n    const direct = audit.receipt_id || audit.id || audit.nonce || payload.audit_nonce;\n    if (direct) {\n      return shortText(direct, \"receipt_8f2a7c\", 16);\n    }\n    const focus = focusFromModel(model);\n    const seed = cleanText3(focus.issue_identifier || focus.identifier || \"NIE-300\").replace(/\\W/g, \"\").slice(-4);\n    return \"receipt_\" + (seed || \"8f2a\") + \"7c\";\n  }\n  function operatorIntent(focus) {\n    const pending = focus.pending_input || focus.current_operator_block || {};\n    const hint = focus.operator_explainer_hint || {};\n    const reason = pending.reason_code || focus.awaiting_operator_reason_code || focus.stop_reason_code || hint.recommended_action || \"clarify_direction\";\n    return cleanText3(reason).replace(/_/g, \" \");\n  }\n  function endpointPreview(focus) {\n    const thread = threadIdForFocus(focus);\n    const reason = cleanText3(\n      focus.pending_input && focus.pending_input.reason_code || focus.awaiting_operator_reason_code || focus.stop_reason_code || \"clarify_direction\"\n    ).replace(/\\s+/g, \"_\");\n    const nonce = cleanText3(focus.pending_input && focus.pending_input.request_id || focus.request_id || \"91f3b9e2\");\n    return [\"POST /api/v1/agent/threads/\" + thread + \"/resume\", \"mode: steer\", \"reason_code: \" + reason, \"nonce: \" + nonce.slice(0, 12), \"...\"];\n  }\n  function buildSteps(model) {\n    const payload = model.payload || {};\n    const focus = focusFromModel(model);\n    const workspaceClean = !hasDirtyWorkspace(model) && payload.health?.dispatch_validation !== \"failed\";\n    const upToDate = branchUpToDate(payload);\n    const noConflicts = !hasConflictingRun(model);\n    const sla = slaWithinLimits(model, payload);\n    const preconditionRows = [\n      { label: \"Workspace clean\", verified: workspaceClean },\n      { label: \"Branch up to date\", verified: upToDate },\n      { label: \"No conflicting runs\", verified: noConflicts },\n      { label: \"SLA within limits\", verified: sla }\n    ];\n    const verifiedCount = preconditionRows.filter((row) => row.verified).length;\n    return [\n      {\n        number: \"1\",\n        title: \"Preconditions\",\n        subtitle: String(verifiedCount) + \" / \" + String(preconditionRows.length) + \" verified\",\n        tone: verifiedCount === preconditionRows.length ? \"verified\" : \"attention\",\n        rows: preconditionRows\n      },\n      {\n        number: \"2\",\n        title: \"Operator Input\",\n        subtitle: \"Intent to send\",\n        tone: \"attention\",\n        rows: [{ label: operatorIntent(focus), verified: true }],\n        action: \"Send: Clarify direction\"\n      },\n      {\n        number: \"3\",\n        title: \"Request Preview\",\n        subtitle: \"API request to send\",\n        tone: \"attention\",\n        rows: [],\n        preview: endpointPreview(focus)\n      },\n      {\n        number: \"4\",\n        title: \"Audit Receipt\",\n        subtitle: \"Immutable record\",\n        tone: \"receipt\",\n        rows: [\n          {\n            label: receiptId(model, payload),\n            verified: true,\n            detail: \"created\"\n          }\n        ]\n      }\n    ];\n  }\n  function createTextElement2(tag, className, text) {\n    const element = document.createElement(tag);\n    element.className = className;\n    element.textContent = text;\n    return element;\n  }\n  function setStyleProperty3(element, name, value) {\n    if (element.style && typeof element.style.setProperty === \"function\") {\n      element.style.setProperty(name, value);\n      return;\n    }\n    element.style[name] = value;\n  }\n  function renderStep(step, index) {\n    const item = document.createElement(\"article\");\n    item.className = \"interlock-step interlock-step-\" + step.tone;\n    item.setAttribute(\"aria-label\", step.number + \" \" + step.title + \" \" + step.subtitle);\n    const orbitalRoutes = [\n      { width: 178, bend: 88, lift: 10, tilt: -12, sweep: -1 },\n      { width: 210, bend: 108, lift: 4, tilt: -6, sweep: -1 },\n      { width: 208, bend: 106, lift: -4, tilt: 6, sweep: 1 },\n      { width: 176, bend: 88, lift: -10, tilt: 12, sweep: 1 }\n    ];\n    const route = orbitalRoutes[index] || orbitalRoutes[orbitalRoutes.length - 1];\n    setStyleProperty3(item, \"--interlock-route-width\", String(route.width) + \"px\");\n    setStyleProperty3(item, \"--interlock-route-bend\", String(route.bend) + \"px\");\n    setStyleProperty3(item, \"--interlock-route-lift\", String(route.lift) + \"px\");\n    setStyleProperty3(item, \"--interlock-route-tilt\", String(route.tilt) + \"deg\");\n    setStyleProperty3(item, \"--interlock-route-sweep\", String(route.sweep));\n    const node = createTextElement2(\"span\", \"interlock-node\", step.tone === \"attention\" ? \"!\" : \"OK\");\n    const body = document.createElement(\"div\");\n    body.className = \"interlock-step-body\";\n    const header = document.createElement(\"div\");\n    header.className = \"interlock-step-header\";\n    header.append(\n      createTextElement2(\"span\", \"interlock-number\", step.number),\n      createTextElement2(\"span\", \"interlock-title\", step.title)\n    );\n    body.append(header, createTextElement2(\"div\", \"interlock-subtitle\", step.subtitle));\n    if (step.rows.length) {\n      const list = document.createElement(\"div\");\n      list.className = \"interlock-checks\";\n      for (const row of step.rows) {\n        const check = document.createElement(\"div\");\n        check.className = \"interlock-check \" + (row.verified ? \"interlock-check-ok\" : \"interlock-check-attention\");\n        check.append(\n          createTextElement2(\"span\", \"interlock-check-glyph\", row.verified ? \"OK\" : \"!\"),\n          createTextElement2(\"span\", \"interlock-check-label\", row.label),\n          createTextElement2(\"span\", \"interlock-check-detail\", row.detail || \"\")\n        );\n        list.appendChild(check);\n      }\n      body.appendChild(list);\n    }\n    if (step.action) {\n      body.append(createTextElement2(\"button\", \"interlock-action\", step.action));\n    }\n    if (step.preview) {\n      const preview = document.createElement(\"pre\");\n      preview.className = \"interlock-preview\";\n      preview.textContent = step.preview.join(\"\\n\");\n      body.appendChild(preview);\n    }\n    item.append(node, body);\n    return item;\n  }\n  function evidenceNodes(model) {\n    const payload = model.payload || {};\n    const focus = focusFromModel(model);\n    return [\n      { label: \"thread\", detail: threadIdForFocus(focus), tone: \"blue\" },\n      { label: \"transcript\", detail: transcriptPath(focus), tone: \"green\" },\n      { label: \"state snapshot\", detail: shortText(payload.generated_at, \"live state\", 20), tone: \"green\" },\n      { label: \"audit receipt\", detail: receiptId(model, payload), tone: \"amber\" }\n    ];\n  }\n  function renderEvidenceNode(node) {\n    const element = document.createElement(\"div\");\n    element.className = \"evidence-node evidence-node-\" + node.tone;\n    element.title = node.label + \": \" + node.detail;\n    element.append(createTextElement2(\"span\", \"evidence-node-label\", node.label), createTextElement2(\"strong\", \"evidence-node-detail\", node.detail));\n    return element;\n  }\n  function renderEvidence(model) {\n    if (!elements.constellationEvidencePath) {\n      return;\n    }\n    const nodes = evidenceNodes(model);\n    const rail = document.createElement(\"div\");\n    rail.className = \"evidence-rail\";\n    rail.append(...nodes.map(renderEvidenceNode));\n    elements.constellationEvidencePath.replaceChildren(rail);\n  }\n  function renderActions() {\n    if (!elements.constellationActions) {\n      return;\n    }\n    const actions = [\n      [\"Send Input\", \"Provide operator direction\", \"blue\"],\n      [\"Resume Agent\", \"Continue blocked run\", \"blue\"],\n      [\"Open Evidence\", \"View transcript path\", \"green\"],\n      [\"Export Audit\", \"Bundle run artifacts\", \"violet\"],\n      [\"Wait for Drain\", \"Pause new work\", \"amber\"],\n      [\"More\", \"Additional controls\", \"neutral\"]\n    ];\n    elements.constellationActions.replaceChildren(\n      ...actions.map(([label, detail, tone]) => {\n        const button = document.createElement(\"button\");\n        button.type = \"button\";\n        button.className = \"constellation-action constellation-action-\" + tone;\n        button.append(createTextElement2(\"span\", \"constellation-action-orb\", label === \"More\" ? \"...\" : label.slice(0, 1)), createTextElement2(\"strong\", \"constellation-action-label\", label), createTextElement2(\"span\", \"constellation-action-detail\", detail));\n        return button;\n      })\n    );\n  }\n  function localTimeFromPayload(payload) {\n    const raw = payload.generated_at || payload.snapshot_generated_at || payload.last_updated_at;\n    const timestamp = raw ? Date.parse(raw) : Date.now();\n    const date = new Date(Number.isFinite(timestamp) ? timestamp : Date.now());\n    return date.toLocaleTimeString([], { hour: \"2-digit\", minute: \"2-digit\", second: \"2-digit\" });\n  }\n  function apiHealth(payload) {\n    const controlPlane = payload.health && payload.health.control_plane;\n    const worst = controlPlane && controlPlane.worst_health;\n    if (worst && worst !== \"ok\") {\n      return cleanText3(worst).replace(/_/g, \" \");\n    }\n    const dispatch = payload.health && payload.health.dispatch_validation;\n    if (dispatch && dispatch !== \"ok\") {\n      return cleanText3(dispatch).replace(/_/g, \" \");\n    }\n    return \"Healthy\";\n  }\n  function renderFooter(model) {\n    const payload = model.payload || {};\n    const counts = payload.counts || {};\n    const running = Array.isArray(model.running) ? model.running.length : Number(counts.running || 0);\n    const retry = Array.isArray(model.retry) ? model.retry.length : Number(counts.retrying || 0);\n    const blocked = Array.isArray(model.blocked) ? model.blocked.length : Number(counts.blocked || 0);\n    const workerLimit = payload.worker_pool?.max_concurrent_agents_per_host || payload.worker_pool?.configured_slots || Math.max(3, running);\n    if (elements.constellationOperator) {\n      elements.constellationOperator.textContent = shortText(payload.operator?.name || payload.operator || payload.user, \"niels\", 22);\n    }\n    if (elements.constellationRuntimeClock) {\n      elements.constellationRuntimeClock.textContent = localTimeFromPayload(payload);\n    }\n    if (elements.constellationApiHealth) {\n      elements.constellationApiHealth.textContent = apiHealth(payload);\n    }\n    if (elements.constellationWorkerCount) {\n      elements.constellationWorkerCount.textContent = String(running) + \" / \" + String(workerLimit);\n    }\n    if (elements.constellationQueueCount) {\n      elements.constellationQueueCount.textContent = String(retry + blocked);\n    }\n  }\n  function renderConstellationInterlocks(model) {\n    if (!elements.constellationInterlockList) {\n      return;\n    }\n    const safeModel = model || {};\n    elements.constellationInterlockList.replaceChildren(...buildSteps(safeModel).map(renderStep));\n    renderEvidence(safeModel);\n    renderActions();\n    renderFooter(safeModel);\n  }\n\n  // src/api/dashboard-client/apple-constellation.ts\n  function renderAppleConstellation(payload) {\n    if (!elements.constellationCore) {\n      return;\n    }\n    const running = Array.isArray(payload && payload.running) ? payload.running : [];\n    const blocked = Array.isArray(payload && payload.blocked) ? payload.blocked : [];\n    const retry = Array.isArray(payload && payload.retry) ? payload.retry : [];\n    const active = [].concat(running, blocked, retry);\n    const focus = active.find((entry) => {\n      const identifier = String(entry?.issue_identifier || entry?.identifier || entry?.issue_id || \"\").trim();\n      return identifier === \"NIE-300\";\n    }) || running[0] || blocked[0] || retry[0] || null;\n    renderConstellationGravity({ running, blocked, retry, focus });\n    renderConstellationCore({ running, blocked, retry, focus });\n    renderConstellationInterlocks({ running, blocked, retry, focus, payload });\n  }\n\n  // src/api/dashboard-client/connection.ts\n  var issueDetailRefreshInFlight = false;\n  var issueDetailRefreshQueued = false;\n  function normalizeConversationRole(value) {\n    return value === \"system\" || value === \"user\" || value === \"assistant\" || value === \"tool\" || value === \"runtime\" ? value : \"all\";\n  }\n  function normalizeConversationDensity(value) {\n    return value === \"compact\" ? \"compact\" : \"comfortable\";\n  }\n  function refreshSelectedIssueDetail() {\n    if (!state.selectedIssue || !elements.issuePanel || !elements.issuePanel.open) {\n      issueDetailRefreshQueued = false;\n      return;\n    }\n    if (issueDetailRefreshInFlight) {\n      issueDetailRefreshQueued = true;\n      return;\n    }\n    issueDetailRefreshInFlight = true;\n    void loadIssue(state.selectedIssue, { openPanel: false }).finally(function() {\n      issueDetailRefreshInFlight = false;\n      if (!issueDetailRefreshQueued) {\n        return;\n      }\n      issueDetailRefreshQueued = false;\n      refreshSelectedIssueDetail();\n    });\n  }\n  function getConnectionLabel(mode) {\n    switch (mode) {\n      case \"streaming\":\n        return \"Streaming\";\n      case \"polling\":\n        return \"Polling\";\n      case \"connecting\":\n        return \"Connecting\";\n      default:\n        return \"Offline\";\n    }\n  }\n  function getConnectionClass(mode) {\n    switch (mode) {\n      case \"streaming\":\n        return \"badge badge-live\";\n      case \"polling\":\n        return \"badge badge-polling\";\n      case \"connecting\":\n        return \"badge badge-connecting\";\n      default:\n        return \"badge badge-offline\";\n    }\n  }\n  function describeStreamFallback() {\n    if (state.streamConnected && !state.streamSnapshotHealthy) {\n      return \"Stream connected; waiting for the first snapshot.\";\n    }\n    if (state.streamFallbackReason === \"error\") {\n      return \"Stream paused; polling is keeping this view current.\";\n    }\n    if (state.streamFallbackReason === \"connecting\") {\n      return \"Connecting to live updates; polling is active.\";\n    }\n    return \"Live stream unavailable; polling is active.\";\n  }\n  function setConnectionStatus(mode, detail) {\n    state.connection = mode;\n    elements.connectionBadge.textContent = getConnectionLabel(mode);\n    elements.connectionBadge.className = getConnectionClass(mode);\n    elements.connectionDetail.textContent = detail;\n  }\n  function formatHeroSnapshotAge(ageMs) {\n    if (!Number.isFinite(ageMs)) {\n      return null;\n    }\n    const seconds = Math.max(0, Math.floor(Number(ageMs) / 1e3));\n    if (seconds < 60) {\n      return \"Updated just now\";\n    }\n    const minutes = Math.floor(seconds / 60);\n    if (minutes < 60) {\n      return \"Updated \" + minutes + \"m ago\";\n    }\n    const hours = Math.floor(minutes / 60);\n    const remainingMinutes = minutes % 60;\n    return \"Updated \" + hours + \"h\" + (remainingMinutes ? \" \" + remainingMinutes + \"m\" : \"\") + \" ago\";\n  }\n  function formatHeroSnapshotFreshness(value) {\n    const payload = state.payload;\n    const generatedAtMs = payload && typeof payload.snapshot_generated_at_ms === \"number\" ? payload.snapshot_generated_at_ms : Date.parse(value || \"\");\n    if (!Number.isFinite(generatedAtMs)) {\n      return { text: \"Waiting for first snapshot\", title: \"\" };\n    }\n    const snapshotAgeMs = payload && typeof payload.snapshot_age_ms === \"number\" ? payload.snapshot_age_ms : Date.now() - generatedAtMs;\n    const ageText = formatHeroSnapshotAge(snapshotAgeMs) || \"Snapshot time available\";\n    const freshness = payload && payload.snapshot_freshness_state ? String(payload.snapshot_freshness_state).replace(/_/g, \" \") : \"\";\n    return {\n      text: ageText + (freshness ? \" \\u2022 Snapshot \" + freshness : \"\"),\n      title: \"Snapshot generated at \" + formatDate(value)\n    };\n  }\n  function setLastUpdated(value) {\n    const freshness = formatHeroSnapshotFreshness(value);\n    elements.lastUpdated.textContent = freshness.text;\n    elements.lastUpdated.title = freshness.title;\n  }\n  function setRefreshStatus(message, isError) {\n    elements.refreshStatus.textContent = message;\n    elements.refreshStatus.className = isError ? \"status-error\" : \"status-ok\";\n  }\n  function isStreamHealthy() {\n    return state.streamConnected && state.streamSnapshotHealthy;\n  }\n  function clearPollTimer() {\n    clearTimeout(state.pollTimer);\n    state.pollTimer = null;\n  }\n  function schedulePollingFallback() {\n    clearPollTimer();\n    if (isStreamHealthy()) {\n      return;\n    }\n    state.pollTimer = setTimeout(loadStateViaPoll2, state.pollDelayMs);\n  }\n  function applyPayload(payload, source) {\n    if (payload && payload.error) {\n      renderSnapshotError(payload.error);\n      setLastUpdated(payload.generated_at || (/* @__PURE__ */ new Date()).toISOString());\n      return false;\n    }\n    clearSnapshotError();\n    if (state.stoppedRunRecoveryLoaded && state.stoppedRunRecoveryPayload) {\n      payload = mergeStoppedRunRecoveryPayload(payload, state.stoppedRunRecoveryPayload);\n    }\n    state.payload = payload;\n    state.lastGoodPayload = payload;\n    renderOverview(payload);\n    renderActionRequiredBanner(payload);\n    renderApiDegradedBanner(payload);\n    renderThroughput(payload);\n    renderRunning(payload);\n    renderRetry(payload);\n    renderBlocked(payload);\n    renderStoppedRunRecovery(payload);\n    renderRuntimeEvents(payload);\n    renderAppleConstellation(payload);\n    setLastUpdated(payload.generated_at || (/* @__PURE__ */ new Date()).toISOString());\n    refreshSelectedIssueDetail();\n    if (source === \"stream\") {\n      state.streamSnapshotHealthy = true;\n      state.streamStatus = \"streaming\";\n      state.streamFallbackReason = null;\n      setConnectionStatus(\"streaming\", \"Live updates are flowing.\");\n      state.pollDelayMs = DASHBOARD_CONFIG.refresh_ms;\n      clearPollTimer();\n    }\n    return true;\n  }\n  function updateRuntimeClock() {\n    if (state.lastGoodPayload) {\n      renderOverview(state.lastGoodPayload);\n      renderAppleConstellation(state.lastGoodPayload);\n    }\n    const runtimeCells = document.querySelectorAll(\".runtime-cell\");\n    for (const runtimeCell of runtimeCells) {\n      const startedAt = runtimeCell.getAttribute(\"data-started-at\");\n      runtimeCell.textContent = formatDurationFromIso(startedAt);\n    }\n  }\n  async function fetchJson(url, init) {\n    const response = await fetch(url, init);\n    const payload = await response.json();\n    if (!response.ok) {\n      throw new Error(formatApiError(payload, \"Request failed\"));\n    }\n    return payload;\n  }\n  async function loadStateViaPoll2() {\n    try {\n      const payload = await fetchJson(\"/api/v1/state\");\n      const usablePayload = applyPayload(payload, \"poll\");\n      if (isStreamHealthy()) {\n        setConnectionStatus(\"streaming\", \"Live updates are flowing.\");\n      } else if (usablePayload) {\n        setConnectionStatus(\"polling\", describeStreamFallback());\n      } else {\n        setConnectionStatus(\"offline\", \"Polling returned snapshot error\");\n      }\n      state.pollDelayMs = DASHBOARD_CONFIG.refresh_ms;\n    } catch (error) {\n      setConnectionStatus(\"offline\", \"Polling failed\");\n      setRefreshStatus(\"Polling failed: \" + String(error), true);\n      state.pollDelayMs = Math.min(state.pollDelayMs * 2, 3e4);\n    } finally {\n      schedulePollingFallback();\n    }\n  }\n  function scheduleStateSave() {\n    if (!state.uiStateLoaded) {\n      return;\n    }\n    clearTimeout(state.uiStateSaveTimer);\n    state.uiStateSaveTimer = setTimeout(async function() {\n      try {\n        await fetch(\"/api/v1/ui-state\", {\n          method: \"POST\",\n          headers: {\n            \"content-type\": \"application/json\"\n          },\n          body: JSON.stringify({\n            state: {\n              selected_issue: state.selectedIssue || null,\n              filters: {\n                status: state.filter.status,\n                query: state.filter.query,\n                conversation_role: state.filter.conversationRole,\n                conversation_density: state.filter.conversationDensity\n              },\n              event_feed_filter: state.filter.eventFeedSeverity,\n              panels: {\n                throughput_open: !!elements.throughputPanel.open,\n                runtime_events_open: !!elements.runtimeEventsPanel.open\n              },\n              panel_state: {\n                issue_detail_open: !!elements.issuePanel.open\n              }\n            }\n          })\n        });\n      } catch {\n      }\n    }, 250);\n  }\n  async function loadUiState() {\n    try {\n      const response = await fetch(\"/api/v1/ui-state\");\n      if (!response.ok) {\n        state.uiStateLoaded = true;\n        return;\n      }\n      const payload = await response.json();\n      const restored = payload.state;\n      if (!restored) {\n        state.uiStateLoaded = true;\n        return;\n      }\n      state.selectedIssue = restored.selected_issue || \"\";\n      state.filter.query = restored.filters && restored.filters.query ? restored.filters.query : \"\";\n      state.filter.status = restored.filters && restored.filters.status ? restored.filters.status : \"all\";\n      state.filter.conversationRole = normalizeConversationRole(restored.filters && restored.filters.conversation_role);\n      state.filter.conversationDensity = normalizeConversationDensity(restored.filters && restored.filters.conversation_density);\n      state.filter.eventFeedSeverity = restored.event_feed_filter || \"all\";\n      state.panels.throughputOpen = restored.panels && typeof restored.panels.throughput_open === \"boolean\" ? restored.panels.throughput_open : true;\n      state.panels.runtimeEventsOpen = restored.panels && typeof restored.panels.runtime_events_open === \"boolean\" ? restored.panels.runtime_events_open : true;\n      elements.issueInput.value = state.selectedIssue;\n      elements.runningFilter.value = state.filter.query;\n      elements.statusFilter.value = state.filter.status;\n      elements.conversationRoleFilter.value = state.filter.conversationRole;\n      elements.conversationDensity.value = state.filter.conversationDensity;\n      elements.eventFeedFilter.value = state.filter.eventFeedSeverity;\n      elements.throughputPanel.open = state.panels.throughputOpen;\n      elements.runtimeEventsPanel.open = state.panels.runtimeEventsOpen;\n      if (restored.panel_state && typeof restored.panel_state.issue_detail_open === \"boolean\") {\n        elements.issuePanel.open = restored.panel_state.issue_detail_open;\n      }\n      state.uiStateLoaded = true;\n      if (state.selectedIssue && elements.issuePanel.open) {\n        void loadIssue(state.selectedIssue, { openPanel: false });\n      }\n    } catch {\n      state.uiStateLoaded = true;\n    }\n  }\n  async function refreshNow() {\n    try {\n      const payload = await fetchJson(\"/api/v1/refresh\", { method: \"POST\" });\n      setRefreshStatus(payload.coalesced ? \"Refresh request coalesced\" : \"Refresh queued\", false);\n      await loadStateViaPoll2();\n    } catch (error) {\n      setRefreshStatus(\"Refresh failed: \" + String(error), true);\n    }\n  }\n  async function loadDiagnostics() {\n    try {\n      const diagnostics = await fetchJson(\"/api/v1/diagnostics\");\n      state.runtimeResolution = diagnostics.runtime_resolution || null;\n      elements.diagnosticsOutput.textContent = formatCanonicalJsonBlock(\"Diagnostics JSON\", diagnostics);\n      renderRuntimeResolution(state.runtimeResolution);\n    } catch {\n      elements.diagnosticsOutput.textContent = \"Diagnostics unavailable.\";\n      state.runtimeResolution = null;\n      renderRuntimeResolution(null);\n    }\n    try {\n      const historyPayload = await fetchJson(\"/api/v1/history?limit=8\");\n      const discoveredProjectKey = projectKeyFromHistoryPayload(historyPayload);\n      if (discoveredProjectKey && !state.projectHistory.projectKey) {\n        state.projectHistory.projectKey = discoveredProjectKey;\n        void loadProjectHistory(discoveredProjectKey);\n      } else {\n        renderProjectHistory();\n      }\n      const runs = Array.isArray(historyPayload.runs) ? historyPayload.runs : [];\n      if (!runs.length) {\n        const empty = document.createElement(\"li\");\n        empty.className = \"muted\";\n        empty.textContent = \"No run history available.\";\n        elements.historyList.replaceChildren(empty);\n        return;\n      }\n      const nodes = runs.map((entry) => {\n        const item = document.createElement(\"li\");\n        const title = document.createElement(\"strong\");\n        title.textContent = entry.issue_identifier + \" (\" + (entry.terminal_status || \"active\") + \")\";\n        const meta = document.createElement(\"span\");\n        meta.textContent = \" \\u2022 started \" + formatDate(entry.started_at) + (entry.ended_at ? \" \\u2022 ended \" + formatDate(entry.ended_at) : \"\");\n        item.append(title, meta);\n        return item;\n      });\n      elements.historyList.replaceChildren(...nodes);\n    } catch {\n      const empty = document.createElement(\"li\");\n      empty.className = \"muted\";\n      empty.textContent = \"History unavailable.\";\n      elements.historyList.replaceChildren(empty);\n    }\n  }\n  function handleSseEnvelope(envelope) {\n    if (!envelope || typeof envelope !== \"object\") {\n      return;\n    }\n    const type = envelope.type;\n    const payload = envelope.payload;\n    if (type === \"state_snapshot\" && payload && payload.state) {\n      applyPayload(payload.state, \"stream\");\n      return;\n    }\n    if (type === \"refresh_accepted\" && payload && payload.accepted) {\n      setRefreshStatus(payload.accepted.coalesced ? \"Refresh request coalesced\" : \"Refresh queued\", false);\n      return;\n    }\n    if (type === \"runtime_health_changed\" && payload && payload.health) {\n      const dispatch = payload.health.dispatch_validation || \"unknown\";\n      const message = payload.health.last_error ? payload.health.last_error : \"health changed\";\n      setRefreshStatus(\"Health changed: \" + dispatch + \" (\" + message + \")\", dispatch === \"failed\");\n      return;\n    }\n  }\n  function connectStream() {\n    if (state.eventSource) {\n      state.eventSource.close();\n      state.eventSource = null;\n    }\n    const stream = new EventSource(\"/api/v1/events\");\n    state.eventSource = stream;\n    state.streamStatus = \"connecting\";\n    state.streamFallbackReason = \"connecting\";\n    setConnectionStatus(\"connecting\", \"Connecting to live updates; polling is active.\");\n    stream.onopen = function() {\n      state.streamConnected = true;\n      state.streamSnapshotHealthy = false;\n      state.streamStatus = \"connected_waiting_snapshot\";\n      state.streamFallbackReason = \"waiting_first_snapshot\";\n      state.streamRetryMs = 1e3;\n      setConnectionStatus(\"connecting\", \"Stream connected; waiting for the first snapshot.\");\n    };\n    function handleStreamMessage(event) {\n      try {\n        const envelope = JSON.parse(event.data);\n        handleSseEnvelope(envelope);\n      } catch {\n      }\n    }\n    stream.onmessage = handleStreamMessage;\n    stream.addEventListener(\"symphony\", handleStreamMessage);\n    stream.onerror = function() {\n      state.streamConnected = false;\n      state.streamSnapshotHealthy = false;\n      state.streamStatus = \"error\";\n      state.streamFallbackReason = \"error\";\n      setConnectionStatus(\"connecting\", \"Stream paused; polling is keeping this view current.\");\n      stream.close();\n      state.eventSource = null;\n      void loadStateViaPoll2();\n      setTimeout(connectStream, state.streamRetryMs);\n      state.streamRetryMs = Math.min(state.streamRetryMs * 2, 15e3);\n    };\n  }\n\n  // src/api/dashboard-client/app.ts\n  function wireEvents() {\n    elements.refreshButton.addEventListener(\"click\", function() {\n      void refreshNow();\n    });\n    elements.issueLoad.addEventListener(\"click\", function() {\n      void loadIssue(elements.issueInput.value);\n    });\n    if (elements.stoppedRunRecoveryLoad) {\n      elements.stoppedRunRecoveryLoad.addEventListener(\"click\", function() {\n        void loadStoppedRunRecovery();\n      });\n    }\n    if (elements.drainEnterButton) {\n      elements.drainEnterButton.addEventListener(\"click\", function() {\n        void enterDrainMode();\n      });\n    }\n    if (elements.drainExitButton) {\n      elements.drainExitButton.addEventListener(\"click\", function() {\n        void exitDrainMode();\n      });\n    }\n    if (elements.drainWaitButton) {\n      elements.drainWaitButton.addEventListener(\"click\", function() {\n        void waitForDrainQuiescence();\n      });\n    }\n    if (elements.drainShutdownButton) {\n      elements.drainShutdownButton.addEventListener(\"click\", function() {\n        void requestDrainSafeShutdown();\n      });\n    }\n    [\n      elements.runtimeUpdatePrepareButton,\n      elements.runtimeUpdatePreparePanelButton\n    ].filter(Boolean).forEach(function(button) {\n      button.addEventListener(\"click\", function() {\n        void prepareRuntimeUpdate();\n      });\n    });\n    [\n      elements.runtimeUpdateApplyButton,\n      elements.runtimeUpdateApplyPanelButton\n    ].filter(Boolean).forEach(function(button) {\n      button.addEventListener(\"click\", function() {\n        void applyRuntimeUpdate();\n      });\n    });\n    elements.projectHistoryLoad.addEventListener(\"click\", function() {\n      void loadProjectHistory(elements.projectHistoryProjectKey.value);\n    });\n    elements.projectHistoryProjectKey.addEventListener(\"keydown\", function(event) {\n      if (event.key === \"Enter\") {\n        void loadProjectHistory(elements.projectHistoryProjectKey.value);\n      }\n    });\n    elements.issueInput.addEventListener(\"keydown\", function(event) {\n      if (event.key === \"Enter\") {\n        void loadIssue(elements.issueInput.value);\n      }\n    });\n    elements.issueOpenJson.addEventListener(\"click\", function() {\n      const identifier = (elements.issueInput.value || state.selectedIssue || \"\").trim();\n      if (!identifier) {\n        setRefreshStatus(\"Provide an issue identifier first\", true);\n        return;\n      }\n      window.open(\"/api/v1/\" + encodeURIComponent(identifier), \"_blank\", \"noopener\");\n    });\n    elements.runningFilter.addEventListener(\"input\", function(event) {\n      state.filter.query = event.target && event.target.value ? event.target.value : \"\";\n      if (state.payload) {\n        renderRunning(state.payload);\n        renderRetry(state.payload);\n        renderBlocked(state.payload);\n      }\n      scheduleStateSave();\n    });\n    elements.statusFilter.addEventListener(\"change\", function(event) {\n      state.filter.status = event.target && event.target.value ? event.target.value : \"all\";\n      if (state.filter.status !== \"blocked\") {\n        state.filter.blockedReason = \"all\";\n      }\n      if (state.payload) {\n        renderRunning(state.payload);\n        renderRetry(state.payload);\n        renderBlocked(state.payload);\n      }\n      scheduleStateSave();\n    });\n    elements.eventFeedFilter.addEventListener(\"change\", function(event) {\n      state.filter.eventFeedSeverity = event.target && event.target.value ? event.target.value : \"all\";\n      if (state.payload) {\n        renderRuntimeEvents(state.payload);\n      }\n      scheduleStateSave();\n    });\n    elements.conversationRoleFilter.addEventListener(\"change\", function(event) {\n      state.filter.conversationRole = event.target && event.target.value ? event.target.value : \"all\";\n      if (state.selectedIssue && elements.issuePanel.open) {\n        void loadIssue(state.selectedIssue, { openPanel: false });\n      }\n      scheduleStateSave();\n    });\n    elements.conversationDensity.addEventListener(\"change\", function(event) {\n      state.filter.conversationDensity = event.target && event.target.value ? event.target.value : \"comfortable\";\n      if (state.selectedIssue && elements.issuePanel.open) {\n        void loadIssue(state.selectedIssue, { openPanel: false });\n      }\n      scheduleStateSave();\n    });\n    elements.issuePanel.addEventListener(\"toggle\", function() {\n      if (state.suppressIssuePanelToggleLoad) {\n        state.suppressIssuePanelToggleLoad = false;\n        scheduleStateSave();\n        return;\n      }\n      if (elements.issuePanel.open && state.selectedIssue) {\n        void loadIssue(state.selectedIssue, { openPanel: false });\n      }\n      scheduleStateSave();\n    });\n    elements.throughputPanel.addEventListener(\"toggle\", function() {\n      state.panels.throughputOpen = !!elements.throughputPanel.open;\n      scheduleStateSave();\n    });\n    elements.runtimeEventsPanel.addEventListener(\"toggle\", function() {\n      state.panels.runtimeEventsOpen = !!elements.runtimeEventsPanel.open;\n      scheduleStateSave();\n    });\n    document.addEventListener(\"keydown\", function(event) {\n      if (event.key === \"/\" && document.activeElement !== elements.runningFilter) {\n        event.preventDefault();\n        elements.runningFilter.focus();\n      }\n      if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === \"r\") {\n        event.preventDefault();\n        void refreshNow();\n      }\n    });\n  }\n  function startDashboardClient() {\n    setDashboardElements(resolveDashboardElements(document));\n    wireEvents();\n    void loadUiState();\n    void loadDiagnostics();\n    if (DASHBOARD_CONFIG.dashboard_enabled) {\n      void loadStateViaPoll2();\n      connectStream();\n      state.runtimeTicker = setInterval(updateRuntimeClock, DASHBOARD_CONFIG.render_interval_ms);\n    } else {\n      setConnectionStatus(\"offline\", \"Dashboard refresh disabled by configuration\");\n      void loadStateViaPoll2();\n    }\n  }\n\n  // src/api/dashboard-client/entry.ts\n  startDashboardClient();\n})();\n";
