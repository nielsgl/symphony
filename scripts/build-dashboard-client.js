const fs = require('node:fs');
const path = require('node:path');

const esbuild = require('esbuild');

const root = path.resolve(__dirname, '..');
const entryPoint = path.join(root, 'src/api/dashboard-client/entry.ts');
const outputFile = path.join(root, 'src/api/dashboard-assets/generated-client.ts');
const checkOnly = process.argv.includes('--check');

const banner = [
  '// Generated by scripts/build-dashboard-client.js.',
  '// Source of truth: src/api/dashboard-client/entry.ts and its imports.',
  '// Do not edit this file by hand.',
  ''
].join('\n');

const result = esbuild.buildSync({
  entryPoints: [entryPoint],
  bundle: true,
  format: 'iife',
  platform: 'browser',
  target: 'es2020',
  legalComments: 'none',
  write: false
});

const bundle = result.outputFiles[0]?.text;
if (!bundle) {
  throw new Error('esbuild did not produce a dashboard client bundle');
}

const source = `${banner}export const GENERATED_DASHBOARD_CLIENT_JS = ${JSON.stringify(bundle)};\n`;

if (checkOnly) {
  const current = fs.existsSync(outputFile) ? fs.readFileSync(outputFile, 'utf8') : '';
  if (current !== source) {
    console.error(
      'Dashboard client generated asset is stale. Run `npm run build:dashboard-client` and commit src/api/dashboard-assets/generated-client.ts.'
    );
    process.exit(1);
  }
  console.log('Dashboard client generated asset is fresh.');
  process.exit(0);
}

fs.writeFileSync(outputFile, source);
