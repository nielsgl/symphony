# Dashboard Client Packaging

## Source Of Truth

The dashboard browser client is authored under `src/api/dashboard-client/`.
`entry.ts` is the browser entrypoint and calls `startDashboardClient()` from
`app.ts`. Domain modules such as `formatting.ts`, `connection.ts`, `overview.ts`,
`runtime.ts`, `issues.ts`, `issue-detail.ts`, `operator-actions.ts`,
`project-history.ts`, and `stopped-runs.ts` own importable functions for direct
tests. Browser runtime config stays in `config.ts`; server-side asset injection
config stays in `server-config.ts` so the browser bundle does not pull server
view-model modules into `/dashboard/client.js`.

The served asset is generated by:

```bash
npm run build:dashboard-client
```

That script runs `scripts/build-dashboard-client.js`, which uses `esbuild` to
bundle `src/api/dashboard-client/entry.ts` into
`src/api/dashboard-assets/generated-client.ts`. The generated file is committed
so the runtime `renderDashboardClientJs(config)` API can return a string without
requiring a browser build step at request time.

## Runtime Wrapper

`src/api/dashboard-assets/client.ts` remains the public asset facade. It
normalizes `DashboardClientConfig`, writes the small runtime config globals used
by the browser bundle, and appends `GENERATED_DASHBOARD_CLIENT_JS`.

Those config globals are the only behavior-bearing wrapper strings left in the
dashboard client path. HTML and CSS assets still use template strings because
they are separate static assets and are not browser-client behavior.

## Line Counts

Before this change, the dashboard client behavior lived in
`src/api/dashboard-assets/client/*` string sections:

- `src/api/dashboard-assets/client.ts`: 31 lines
- `src/api/dashboard-assets/client/*`: 3,267 lines
- dashboard asset area total including HTML/CSS/types: 4,419 lines

After this change:

- `src/api/dashboard-assets/client.ts`: 15 lines
- `src/api/dashboard-assets/generated-client.ts`: 4 generated lines
- `src/api/dashboard-client/*.ts`: 3,340 authored lines
- `scripts/build-dashboard-client.js`: 33 lines

## Coverage Map

- Config normalization and server-injected constants:
  `tests/api/dashboard-client.test.ts`
- Formatting, date/duration/number display, state labels, action-required
  labels, API errors, token fallback, and aggregate-only split behavior:
  `tests/api/dashboard-client.test.ts`
- Connection state labels/classes, polling fallback scheduling, and SSE refresh
  envelope handling: `tests/api/dashboard-client.test.ts`
- Running issue filter behavior: `tests/api/dashboard-client.test.ts`
- Operator action request payload construction for resume, cancel, requeue-style
  actions, and blocked-input submission: `tests/api/dashboard-client.test.ts`
- Stopped-run recovery normalization, merge behavior, and status labels:
  `tests/api/dashboard-client.test.ts`
- Project history project-key discovery, summary helpers, health fallback text,
  fact badge classes, and empty-row rendering:
  `tests/api/dashboard-client.test.ts`
- Generated asset execution, full SSE/polling fallback behavior, refresh
  requests, diagnostics loading, running/retry/blocked row rendering, issue
  drilldown/timeline/raw event rendering, stopped-run lazy loading, project
  history list/detail behavior, operator action submission, UI state
  persistence, and aggregate-only token UI behavior:
  `tests/api/dashboard-assets.test.ts`
- `/dashboard/client.js` route and content type:
  `tests/api/server-dashboard-assets.test.ts`
